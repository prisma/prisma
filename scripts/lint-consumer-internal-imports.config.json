{
  "target": "zero — a consumer names its @prisma/orm-* facade and its extension packs, nothing else (ADR 242). Each threshold below counts lines still naming an internal @prisma-next/* package, and may only ever go down. TML-3129 drives them to zero.",
  "scopes": [
    { "path": "examples", "threshold": 678 },
    { "path": "apps", "threshold": 32 },
    { "path": "test", "threshold": 2214 }
  ]
}
