// Faithful translation of prisma/prisma@a6d0155
// packages/client/tests/functional/issues/20261-group-by-shortcut (postgres matrix entry).
//
// Upstream schema:
//   model Round { id String @id; teamName String; points Int }
//
// idForProvider(postgresql) = String @id

model Round {
  id       String @id @default(cuid(2))
  teamName String
  points   Int
}
