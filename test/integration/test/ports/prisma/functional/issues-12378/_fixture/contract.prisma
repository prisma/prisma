// Faithful translation of prisma/prisma@a6d0155
// packages/client/tests/functional/issues/12378 (postgres matrix entry).
//
// Upstream schema:
//   model User {
//     id         String              @id @default(cuid())
//     email      String              @unique
//     workspaces UsersOnWorkspaces[]
//     name       String?
//   }
//   model Workspace {
//     id        String              @id @default(cuid())
//     users     UsersOnWorkspaces[]
//     name      String
//   }
//   model UsersOnWorkspaces {
//     user        User      @relation(fields: [userId], references: [id])
//     userId      String
//     workspace   Workspace @relation(fields: [workspaceId], references: [id])
//     workspaceId String
//     @@id([userId, workspaceId])
//   }

model User {
  id         String              @id @default(cuid(2))
  email      String              @unique
  workspaces UsersOnWorkspaces[]
  name       String?
}

model Workspace {
  id    String              @id @default(cuid(2))
  users UsersOnWorkspaces[]
  name  String
}

model UsersOnWorkspaces {
  user        User      @relation(fields: [userId], references: [id])
  userId      String
  workspace   Workspace @relation(fields: [workspaceId], references: [id])
  workspaceId String

  @@id([userId, workspaceId])
}
