// Faithful translation of prisma/prisma@a6d0155
// packages/client/tests/functional/issues/16535-select-enum (postgres matrix entry).
//
// Upstream schema:
//   model User { id String @id; role UserRole }
//   enum UserRole { ADMIN USER }
//
// prisma-next: text-backed enum uses @@type("pg/text@1").

enum UserRole {
  @@type("pg/text@1")
  ADMIN = "ADMIN"
  USER  = "USER"
}

model User {
  id   String   @id @default(cuid(2))
  role UserRole
}
