// Faithful translation of prisma/prisma@a6d0155
// packages/client/tests/functional/decimal/list (postgres matrix entry only;
// mongodb/mysql/sqlite/sqlserver opted out).
//
// Upstream schema:
//   model User {
//     id       String    @id @default(cuid())
//     decimals Decimal[]
//   }
//
// `Decimal[]` maps to `numeric[]` in Postgres (no precision/scale override needed).

types {
  D = Numeric(65, 30)
}

model User {
  id       String @id @default(cuid(2))
  decimals D[]
}
