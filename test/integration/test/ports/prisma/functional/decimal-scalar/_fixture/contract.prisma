// Faithful translation of prisma/prisma functional suite `decimal/scalar`
// (packages/client/tests/functional/decimal/scalar, postgres matrix entry):
//
//   model User {
//     id    String  @id @default(cuid())
//     money Decimal
//   }
model User {
  id    String  @id @default(cuid(2))
  money Decimal
}
