// Faithful translation of prisma/prisma@a6d0155
// packages/client/tests/functional/enum-array (postgres matrix entry).
//
// Upstream schema:
//   model User { id String @id @default(cuid()); plans Plan[] }
//   enum Plan { FREE PAID CUSTOM }
//
// prisma-next PSL: text-backed enum with list column uses Enum[]
// (scalar list + text-backed enum → pg/text array column).

enum Plan {
  @@type("pg/text@1")
  FREE   = "FREE"
  PAID   = "PAID"
  CUSTOM = "CUSTOM"
}

model User {
  id    String @id @default(cuid(2))
  plans Plan[]
}
