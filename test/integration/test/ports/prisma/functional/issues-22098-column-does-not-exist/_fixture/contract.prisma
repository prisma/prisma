// Faithful translation of prisma/prisma@a6d0155
// packages/client/tests/functional/issues/22098-column_does_not_exist (postgres matrix entry).
//
// Upstream schema (idForProvider(postgresql) = "String @id @default(cuid())"):
//
//   model test {
//     id             String @id @default(cuid())
//     TESTE_N_MERICO String @unique @map("TESTE_NÚMERICO")
//   }
//
// Subject: a query against a model with a @map column (non-ASCII mapped column name)
// does not throw "column does not exist". The model name is lowercase `test`.
model test {
  id             String @id @default(cuid(2))
  TESTE_N_MERICO String @unique @map("TESTE_NÚMERICO")
}
