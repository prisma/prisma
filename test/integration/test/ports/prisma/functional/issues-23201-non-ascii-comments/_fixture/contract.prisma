// Faithful translation of prisma/prisma@a6d0155
// packages/client/tests/functional/issues/23201-non-ascii-comments (postgres matrix entry;
// allProviders — this is the postgres port).
//
// Upstream schema (idForProvider(postgresql) = "String @id @default(cuid())"):
//
//   model User {
//     id    String @id @default(cuid())
//     email String @unique // привет, 世界
//   }
//
// Subject: a schema with non-ASCII characters in comments still connects and queries.
model User {
  id    String @id @default(cuid(2))
  email String @unique // привет, 世界
}
