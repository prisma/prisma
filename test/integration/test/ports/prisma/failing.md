# Failing ports — prisma/prisma

One entry per `test.fails` port: a faithful port that runs but does not pass against today's prisma-next. Format:

`` - `<ported test file>` › `<test name>` — <what it verifies> — <the gap it hits / what prisma-next does instead> ``

Every entry here corresponds one-to-one with a `test.fails` marker in the corpus.

<!-- entries appended per batch -->

- `test/ports/prisma/functional/default-selection/default-selection.test.ts` › `includes scalars` — default selection returns scalar fields (id/value/otherId) — the faithful schema includes an `Enum[]` enum-list column; the postgres emitter emits an invalid `CHECK (col IN (...))` for that array column, so the contract push fails (sqlState 22P02 "malformed array literal") before any ORM op runs. (The more-faithful native-enum-array authoring pushes but then fails at query time with `type "<enum>[]" does not exist`, code 42704 — either authoring hits a real gap.)
- `test/ports/prisma/functional/default-selection/default-selection.test.ts` › `does not include relations` — default selection excludes relation fields — same enum-list emitter gap.
- `test/ports/prisma/functional/default-selection/default-selection.test.ts` › `includes enums` — default selection includes the enum field — same enum-list emitter gap.
- `test/ports/prisma/functional/default-selection/default-selection.test.ts` › `includes lists` — default selection includes the `String[]` list field — same enum-list emitter gap (the `Enum[]` column in the same model blocks the push).
- `test/ports/prisma/functional/default-selection/default-selection.test.ts` › `includes enum lists` — default selection includes the `Enum[]` enum-list field — same enum-list emitter gap.
- `test/ports/prisma/functional/enum-array/enum-array.test.ts` › `can create data with an enum array` — create a record with an enum array field — text-backed enum-list column emits `CHECK (plans IN ('FREE','PAID','CUSTOM'))`, invalid for a postgres array column; push fails with sqlState 22P02 "malformed array literal". (Native-enum-array authoring pushes but fails at query time with code 42704 `type "plan[]" does not exist` — enum arrays are unsupported end-to-end today.)
- `test/ports/prisma/functional/enum-array/enum-array.test.ts` › `can retrieve data with an enum array` — create then read back the enum array — same enum-list emitter gap.
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findMany with cursor 1 unique (PK)` — verifies `.orderBy(p=>p.id.asc()).cursor({ id: postId2 }).all()` returns 2 rows — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findMany with cursor 2 uniques (PK & non-PK)` — verifies `.orderBy([id.asc, title.asc]).cursor({ id, title }).all()` returns 2 rows — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findMany with cursor 1 unique (non-PK)` — verifies `.orderBy(p=>p.title.asc()).cursor({ title: 'Hello World 2' }).all()` returns 2 rows — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findFirst with cursor 1 unique (PK)` — verifies `.orderBy(p=>p.id.asc()).cursor({ id: postId2 }).first()` returns postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findFirst with cursor 2 uniques (PK & non-PK)` — verifies `.orderBy([id.asc, title.asc]).cursor({ id, title }).first()` returns postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findFirst with cursor 1 unique (non-PK)` — verifies `.orderBy(p=>p.title.asc()).cursor({ title: 'Hello World 2' }).first()` returns postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findFirstOrThrow with cursor 1 unique (PK)` — verifies `.orderBy(p=>p.id.asc()).cursor({ id: postId2 }).first()` non-null and id === postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findFirstOrThrow with cursor 2 uniques (PK & non-PK)` — verifies `.orderBy([id.asc, title.asc]).cursor({ id, title }).first()` non-null and id === postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findFirstOrThrow with cursor 1 unique (non-PK)` — verifies `.orderBy(p=>p.title.asc()).cursor({ title: 'Hello World 2' }).first()` non-null and id === postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/bytes-upsert/bytes-upsert.test.ts` › `bytes upsert should work correctly` — verifies second upsert call with same Bytes @unique conflict does not throw — second `upsertByteRow()` fails with `ORM.MUTATION_ROW_MISSING`: `ON CONFLICT DO NOTHING RETURNING *` emits zero rows on conflict, and the follow-up reload via `#buildUpsertConflictCriterion` fails to match the existing row (Uint8Array equality in the shorthand WHERE filter does not round-trip through the reload path)
- `test/integration/test/ports/prisma/functional/legacy-aggregations/legacy-aggregations.test.ts` › `invalid count` — aggregate with a field argument to count() — type-rejects the arg (@ts-expect-error holds) but at runtime prisma-next ignores it and returns COUNT(*) instead of throwing (Prisma validates and throws)
- `test/integration/test/ports/prisma/functional/composites-object-create/composites-object-create.test.ts` › `set null` (required content) — null on a required embedded field — type-rejects it (@ts-expect-error holds) but mongo has no runtime validation, so it does not throw (Prisma throws "must not be null")
- `test/integration/test/ports/prisma/functional/composites-object-create/composites-object-create.test.ts` › `set null shorthand` (required content) — same as set null for the required variant — type-rejected but no runtime throw
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `update with where 1 unique (PK)` — faithful empty-data update({}) (upstream update.ts:15-22) — prisma-next update({}) skips the SQL round-trip and returns null, so data?.id is undefined and the assertion fails
- `test/ports/prisma/functional/methods-createManyAndReturn/methods-createManyAndReturn.test.ts` › `include _count should fail` — createManyAndReturn rejects `include: { _count: true }` — prisma-next type-rejects `include('_count')` (inline `@ts-expect-error` holds) but ignores the unknown relation at runtime instead of throwing, so the `.rejects.toThrow()` assertion fails
- `test/ports/prisma/functional/methods-updateManyAndReturn/methods-updateManyAndReturn.test.ts` › `include _count should fail` — updateManyAndReturn rejects `include: { _count: true }` — prisma-next type-rejects `include('_count')` (inline `@ts-expect-error` holds) but ignores the unknown relation at runtime instead of throwing, so the `.rejects.toThrow()` assertion fails
- `test/integration/test/ports/prisma/functional/composites-object-createMany/composites-object-createMany.test.ts` › `required content` › `set null` — createMany with null on a required embedded `content` field — type-rejects it (inline `@ts-expect-error` holds) but mongo has no runtime validation, so it does not throw (Prisma throws "Argument `set` must not be null")
- `test/integration/test/ports/prisma/functional/composites-object-createMany/composites-object-createMany.test.ts` › `required content` › `set null shorthand` — same as `set null` for the required variant — type-rejected but no runtime throw
- `test/integration/test/ports/prisma/functional/composites-object-upsert-create/composites-object-upsert-create.test.ts` › `required content` › `set null` — upsert (create path) with null on a required embedded `content` field — type-rejects it (inline `@ts-expect-error` holds) but mongo has no runtime validation, so it does not throw (Prisma throws "Argument `set` must not be null")
- `test/integration/test/ports/prisma/functional/composites-object-upsert-create/composites-object-upsert-create.test.ts` › `required content` › `set null shorthand` — same as `set null` for the required variant — type-rejected but no runtime throw
- `test/integration/test/ports/prisma/functional/composites-list-create/composites-list-create.test.ts` › `set null` — create with null on a required embedded `contents` list — type-rejects it (inline `@ts-expect-error` holds) but mongo has no runtime validation, so it does not throw (Prisma throws "Argument `set` must not be null")
- `test/integration/test/ports/prisma/functional/composites-list-create/composites-list-create.test.ts` › `set null shorthand` — same as `set null` for the required list — type-rejected but no runtime throw
- `test/integration/test/ports/prisma/functional/composites-list-createMany/composites-list-createMany.test.ts` › `set null` — createMany with null on a required embedded `contents` list — type-rejects it (inline `@ts-expect-error` holds) but mongo has no runtime validation, so it does not throw (Prisma throws "Argument `set` must not be null")
- `test/integration/test/ports/prisma/functional/composites-list-createMany/composites-list-createMany.test.ts` › `set null shorthand` — same as `set null` for the required list — type-rejected but no runtime throw
- `test/integration/test/ports/prisma/functional/composites-list-update/composites-list-update.test.ts` › `set null` — update with null on a required embedded `contents` list — type-rejects it (inline `@ts-expect-error` holds) but mongo has no runtime validation, so it does not throw (Prisma throws "Argument `set` must not be null")
- `test/integration/test/ports/prisma/functional/composites-list-update/composites-list-update.test.ts` › `set null shorthand` — same as `set null` for the required list — type-rejected but no runtime throw
- `test/integration/test/ports/prisma/functional/composites-list-upsert-create/composites-list-upsert-create.test.ts` › `set null` — upsert (create path) with null on a required embedded `contents` list — type-rejects it (inline `@ts-expect-error` holds) but mongo has no runtime validation, so it does not throw (Prisma throws "Argument `set` must not be null")
- `test/integration/test/ports/prisma/functional/composites-list-upsert-create/composites-list-upsert-create.test.ts` › `set null shorthand` — same as `set null` for the required list — type-rejected but no runtime throw
- `test/ports/prisma/functional/issues-29267-uint8array-in-json/issues-29267-uint8array-in-json.test.ts` › `serializes Uint8Array nested in object as base64` — prisma-next's JSON codec (`packages/3-extensions/arktype-json/src/core/arktype-json-codec.ts`, `serializeWire = JSON.stringify`) serialises a `Uint8Array` to an index-keyed object (`{"0":72,...}`), not base64; no Uint8Array→base64 hook
- `test/ports/prisma/functional/issues-29267-uint8array-in-json/issues-29267-uint8array-in-json.test.ts` › `serializes Uint8Array nested in array as base64` — same gap: JSON codec `JSON.stringify` renders a `Uint8Array` as an index-keyed object, not base64
- `test/ports/prisma/functional/issues-29267-uint8array-in-json/issues-29267-uint8array-in-json.test.ts` › `serializes Uint8Array directly as base64` — same gap: JSON codec `JSON.stringify` renders a `Uint8Array` as an index-keyed object, not base64
- `test/ports/prisma/functional/issues-29267-uint8array-in-json/issues-29267-uint8array-in-json.test.ts` › `serializes deeply nested Uint8Array as base64` — same gap: JSON codec `JSON.stringify` renders a `Uint8Array` as an index-keyed object, not base64

- `test/ports/prisma/functional/relation-mode-gh-m-to-n/update.test.ts` › `onUpdate DEFAULT/SetNull (map=false) › [update] post id` — updating a referenced parent post id under the DEFAULT/SetNull m:n schema — prisma-next's implicit default `onUpdate` (no action line) is NO ACTION, so the FK-referenced parent-id update throws instead of cascading (Prisma's implicit default for a required relation is Cascade).
- `test/ports/prisma/functional/relation-mode-gh-m-to-n/update.test.ts` › `onUpdate DEFAULT/SetNull (map=false) › [update] category id` — same gap, category side.
- `test/ports/prisma/functional/relation-mode-gh-m-to-n/emit-map.test.ts` › `[emit @map] (index-name-length gap) › default-map › emitting the faithful @map contract succeeds` — emitting the DEFAULT/SetNull @map m:n schema — prisma-next #1047 rejects the mapped m:n join-table auto-index name `CategoriesOnPostsManyToMany_AtAtMap_categoryId_AtMap_idx` (>54-char prefix limit); the faithful @map schema is not emittable. Prisma truncates/hashes long index names. Blocks the m-to-n @map matrix variants.
- `test/ports/prisma/functional/relation-mode-gh-m-to-n/emit-map.test.ts` › `[emit @map] (index-name-length gap) › cascade-map › emitting the faithful @map contract succeeds` — emitting the Cascade @map m:n schema — prisma-next #1047 rejects the mapped m:n join-table auto-index name `CategoriesOnPostsManyToMany_AtAtMap_categoryId_AtMap_idx` (>54-char prefix limit); the faithful @map schema is not emittable. Prisma truncates/hashes long index names. Blocks the m-to-n @map matrix variants.
- `test/ports/prisma/functional/relation-mode-gh-m-to-n/emit-map.test.ts` › `[emit @map] (index-name-length gap) › noaction-map › emitting the faithful @map contract succeeds` — emitting the NoAction @map m:n schema — prisma-next #1047 rejects the mapped m:n join-table auto-index name `CategoriesOnPostsManyToMany_AtAtMap_categoryId_AtMap_idx` (>54-char prefix limit); the faithful @map schema is not emittable. Prisma truncates/hashes long index names. Blocks the m-to-n @map matrix variants.
- `test/ports/prisma/functional/relation-mode-gh-m-to-n/emit-map.test.ts` › `[emit @map] (index-name-length gap) › restrict-map › emitting the faithful @map contract succeeds` — emitting the Restrict @map m:n schema — prisma-next #1047 rejects the mapped m:n join-table auto-index name `CategoriesOnPostsManyToMany_AtAtMap_categoryId_AtMap_idx` (>54-char prefix limit); the faithful @map schema is not emittable. Prisma truncates/hashes long index names. Blocks the m-to-n @map matrix variants.
- `test/integration/test/ports/prisma/functional/composites-object-update/composites-object-update.test.ts` › `required content` › `set null` — update with null on a required embedded `content` field — type-rejects it (inline `@ts-expect-error` holds) but mongo has no runtime validation, so it does not throw (Prisma throws "Argument `set` must not be null")
- `test/integration/test/ports/prisma/functional/composites-object-update/composites-object-update.test.ts` › `required content` › `set null shorthand` — same as `set null` for the required variant — type-rejected but no runtime throw
- `test/integration/test/ports/prisma/functional/composites-object-update/composites-object-update.test.ts` › `optional content` › `unset` — `update((u) => [u.content.unset()])` on an optional composite — prisma-next's `$unset` removes the field entirely rather than writing `null`, so the returned document lacks the `content` key and the `{ content: null }` assertion fails (Prisma returns `content: null`)
- `test/integration/test/ports/prisma/functional/composites-object-updateMany/composites-object-updateMany.test.ts` › `required content` › `set null` — updateMany with null on a required embedded `content` field — type-rejected (inline `@ts-expect-error` holds) but mongo has no runtime validation, so it does not throw
- `test/integration/test/ports/prisma/functional/composites-object-updateMany/composites-object-updateMany.test.ts` › `required content` › `set null shorthand` — same as `set null` for the required variant — type-rejected but no runtime throw
- `test/integration/test/ports/prisma/functional/composites-object-upsert-update/composites-object-upsert-update.test.ts` › `required content` › `set null` — upsert (update path) with null on a required embedded `content` field — type-rejected (inline `@ts-expect-error` holds) but mongo has no runtime validation, so it does not throw
- `test/integration/test/ports/prisma/functional/composites-object-upsert-update/composites-object-upsert-update.test.ts` › `required content` › `set null shorthand` — same as `set null` for the required variant — type-rejected but no runtime throw
- `test/integration/test/ports/prisma/functional/composites-object-upsert-update/composites-object-upsert-update.test.ts` › `optional content` › `unset` — `upsert({ update: (u) => [u.content.unset()] })` on an optional composite — prisma-next's `$unset` removes the field entirely rather than writing `null`, so the returned document lacks the `content` key and the `{ content: null }` assertion fails
- `test/integration/test/ports/prisma/functional/composites-list-updateMany/composites-list-updateMany.test.ts` › `set null` — updateMany with null on a required embedded `contents` list — type-rejected (inline `@ts-expect-error` holds) but mongo has no runtime validation, so it does not throw
- `test/integration/test/ports/prisma/functional/composites-list-updateMany/composites-list-updateMany.test.ts` › `set null shorthand` — same as `set null` for the required list — type-rejected but no runtime throw
- `test/integration/test/ports/prisma/functional/composites-list-upsert-update/composites-list-upsert-update.test.ts` › `set null` — upsert (update path) with null on a required embedded `contents` list — type-rejected (inline `@ts-expect-error` holds) but mongo has no runtime validation, so it does not throw
- `test/integration/test/ports/prisma/functional/composites-list-upsert-update/composites-list-upsert-update.test.ts` › `set null shorthand` — same as `set null` for the required list — type-rejected but no runtime throw

- `test/ports/prisma/functional/issues-29176-cursor-parameter-regression/issues-29176-cursor-parameter-regression.test.ts` › `correctly handles a cursor with parameterised values` — cursor pagination with parameterised (runtime UUID) composite-cursor values — prisma-next's cursor is exclusive; it skips the cursor row and returns `[{ result_index: 2 }]` instead of Prisma's inclusive `[{ result_index: 1 }, { result_index: 2 }]`.
- `test/ports/prisma/functional/issues-29254-query-plan-cache-mutation/issues-29254-query-plan-cache-mutation.test.ts` › `correctly handles two subsequent queries with a different cursor` — two sequential cursor-paginated queries with different cursors each return the correct page — prisma-next's exclusive cursor + skip=1 shifts both pages (query 1 → id3 not id2; query 2 → `[]` not id3) vs Prisma's inclusive cursor.
- `test/ports/prisma/functional/issues-29309-datetime-cursor/issues-29309-datetime-cursor.test.ts` › `retrieves a cursor against a DATE column` — cursor pagination against a `pg/date` DATE column (composite `@@id([appId, createdAt])`) — prisma-next's exclusive cursor + skip=1 yields Jan 5/6/7 instead of Prisma's inclusive Jan 4/5/6.

- `test/ports/prisma/functional/issues-28151-broken-nested-set/issues-28151-broken-nested-set.test.ts` › `nested set should result in all expected linked rows` — verifies nested `set([...])` on a 1:N relation in `update` replaces relation membership so all three post ids end up linked — prisma-next's `RelationMutator` exposes only `create`/`connect`/`disconnect`; `p.set` is absent (type-rejected via inline `@ts-expect-error`) and undefined at runtime, so the mutation throws.
- `test/ports/prisma/functional/issues-28192-pg-historical-dates/issues-28192-pg-historical-dates.test.ts` › `historical dates with 2-digit years (00-99 AD) > correctly parses 31 AD timestamp` — verifies the `pg/date` DATE column round-trips year 31 AD — `pg/date@1` misparses PGlite's wire-format 2-digit year (century dropped: 31 → 1931); the `timestamp`/`timestamptz` columns return the correct year, only `date` is affected.
- `test/ports/prisma/functional/issues-28192-pg-historical-dates/issues-28192-pg-historical-dates.test.ts` › `historical dates with 2-digit years (00-99 AD) > correctly parses 32 AD timestamp` — verifies the DATE column round-trips year 32 AD — same `pg/date@1` century-drop gap (32 → 1932).
- `test/ports/prisma/functional/issues-28192-pg-historical-dates/issues-28192-pg-historical-dates.test.ts` › `historical dates with 2-digit years (00-99 AD) > correctly parses 40 AD timestamp` — verifies the DATE column round-trips year 40 AD — same `pg/date@1` century-drop gap (40 → 1940).
- `test/ports/prisma/functional/issues-28192-pg-historical-dates/issues-28192-pg-historical-dates.test.ts` › `historical dates with 2-digit years (00-99 AD) > correctly parses 99 AD timestamp` — verifies the DATE column round-trips year 99 AD — same `pg/date@1` century-drop gap (99 → 1999).
- `test/ports/prisma/functional/issues-28591-mapped-enums/issues-28591-mapped-enums.test.ts` › `create with mapped enum` — verifies `@map`-on-enum-member name translation (DB stores `'pending'`, ORM returns `'PENDING'`) — prisma-next's `native_enum` member values ARE the PG labels and `pg/enum@1` returns them verbatim (`'pending'`); there is no TS-name ↔ DB-label mapping.

- `test/ports/prisma/functional/issues-TML-1664-unknown-enum-value-read-error/issues-TML-1664-unknown-enum-value-read-error.test.ts` › `returns a structured error when reading an enum value unknown to the schema` — reading a row whose native PG enum column holds a value added to the DB after schema push (unknown to the contract) should surface a structured error (Prisma P2023) — prisma-next's `pg/enum@1` codec is a text passthrough; `decode()` returns the wire string verbatim without validating against the known member set, so the unknown value is returned as-is rather than raising `RUNTIME.DECODE_FAILED`.
- `test/ports/prisma/functional/issues-27455-bytes-id/issues-27455-bytes-id.test.ts` › `retrieves records after a create with Bytes IDs` — nested `create` on a model with a `Bytes @id` returns the created rows via include — the post-insert reload (`buildPrimaryKeyFilterFromRow`) does not round-trip the `Uint8Array` PK through the WHERE filter, so the reload finds no row and throws `ORM.MUTATION_ROW_MISSING` (same gap as bytes-upsert).
- `test/ports/prisma/functional/issues-27511-include-enum-array/issues-27511-include-enum-array.test.ts` › `findMany with include on many-to-many relationship with enum array should work` — findMany + include over an M:N relation where a model has a text-backed enum-array column (`permissions workspace_permission[]`) — the postgres emitter emits an invalid `CHECK (col IN (...))` for the enum-array column (same enum[] emitter gap as the enum-array/default-selection suites); the contract push fails with sqlState 22P02 ("malformed array literal") before any ORM query runs.
