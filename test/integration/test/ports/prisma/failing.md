# Failing ports — prisma/prisma

One entry per `test.fails` port: a faithful port that runs but does not pass against today's prisma-next. Format:

`` - `<ported test file>` › `<test name>` — <what it verifies> — <the gap it hits / what prisma-next does instead> ``

Every entry here corresponds one-to-one with a `test.fails` marker in the corpus.

<!-- entries appended per batch -->

- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findMany with cursor 1 unique (PK)` — verifies `.orderBy(p=>p.id.asc()).cursor({ id: postId2 }).all()` returns 2 rows — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findMany with cursor 2 uniques (PK & non-PK)` — verifies `.orderBy([id.asc, title.asc]).cursor({ id, title }).all()` returns 2 rows — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findMany with cursor 1 unique (non-PK)` — verifies `.orderBy(p=>p.title.asc()).cursor({ title: 'Hello World 2' }).all()` returns 2 rows — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findFirst with cursor 1 unique (PK)` — verifies `.orderBy(p=>p.id.asc()).cursor({ id: postId2 }).first()` returns postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findFirst with cursor 2 uniques (PK & non-PK)` — verifies `.orderBy([id.asc, title.asc]).cursor({ id, title }).first()` returns postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findFirst with cursor 1 unique (non-PK)` — verifies `.orderBy(p=>p.title.asc()).cursor({ title: 'Hello World 2' }).first()` returns postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findFirstOrThrow with cursor 1 unique (PK)` — verifies `.orderBy(p=>p.id.asc()).cursor({ id: postId2 }).first()` non-null and id === postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findFirstOrThrow with cursor 2 uniques (PK & non-PK)` — verifies `.orderBy([id.asc, title.asc]).cursor({ id, title }).first()` non-null and id === postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `findFirstOrThrow with cursor 1 unique (non-PK)` — verifies `.orderBy(p=>p.title.asc()).cursor({ title: 'Hello World 2' }).first()` non-null and id === postId2 — prisma-next cursor is exclusive (keyset-after) vs Prisma inclusive; faithful `.cursor()` port returns a different result
- `test/integration/test/ports/prisma/functional/legacy-aggregations/legacy-aggregations.test.ts` › `invalid count` — aggregate with a field argument to count() — type-rejects the arg (@ts-expect-error holds) but at runtime prisma-next ignores it and returns COUNT(*) instead of throwing (Prisma validates and throws)
- `test/integration/test/ports/prisma/functional/extended-where/extended-where.test.ts` › `update with where 1 unique (PK)` — faithful empty-data update({}) (upstream update.ts:15-22) — prisma-next update({}) skips the SQL round-trip and returns null, so data?.id is undefined and the assertion fails
- `test/ports/prisma/functional/methods-createManyAndReturn/methods-createManyAndReturn.test.ts` › `include _count should fail` — createManyAndReturn rejects `include: { _count: true }` — prisma-next type-rejects `include('_count')` (inline `@ts-expect-error` holds) but ignores the unknown relation at runtime instead of throwing, so the `.rejects.toThrow()` assertion fails
- `test/ports/prisma/functional/methods-updateManyAndReturn/methods-updateManyAndReturn.test.ts` › `include _count should fail` — updateManyAndReturn rejects `include: { _count: true }` — prisma-next type-rejects `include('_count')` (inline `@ts-expect-error` holds) but ignores the unknown relation at runtime instead of throwing, so the `.rejects.toThrow()` assertion fails
- `test/ports/prisma/functional/issues-29267-uint8array-in-json/issues-29267-uint8array-in-json.test.ts` › `serializes Uint8Array nested in object as base64` — prisma-next's JSON codec (`packages/3-extensions/arktype-json/src/core/arktype-json-codec.ts`, `serializeWire = JSON.stringify`) serialises a `Uint8Array` to an index-keyed object (`{"0":72,...}`), not base64; no Uint8Array→base64 hook
- `test/ports/prisma/functional/issues-29267-uint8array-in-json/issues-29267-uint8array-in-json.test.ts` › `serializes Uint8Array nested in array as base64` — same gap: JSON codec `JSON.stringify` renders a `Uint8Array` as an index-keyed object, not base64
- `test/ports/prisma/functional/issues-29267-uint8array-in-json/issues-29267-uint8array-in-json.test.ts` › `serializes Uint8Array directly as base64` — same gap: JSON codec `JSON.stringify` renders a `Uint8Array` as an index-keyed object, not base64
- `test/ports/prisma/functional/issues-29267-uint8array-in-json/issues-29267-uint8array-in-json.test.ts` › `serializes deeply nested Uint8Array as base64` — same gap: JSON codec `JSON.stringify` renders a `Uint8Array` as an index-keyed object, not base64

- `test/ports/prisma/functional/relation-mode-gh-m-to-n/update.test.ts` › `onUpdate DEFAULT/SetNull (map=false) › [update] post id` — updating a referenced parent post id under the DEFAULT/SetNull m:n schema — prisma-next's implicit default `onUpdate` (no action line) is NO ACTION, so the FK-referenced parent-id update throws instead of cascading (Prisma's implicit default for a required relation is Cascade).
- `test/ports/prisma/functional/relation-mode-gh-m-to-n/update.test.ts` › `onUpdate DEFAULT/SetNull (map=false) › [update] category id` — same gap, category side.
- `test/integration/test/ports/prisma/functional/composites-object-update/composites-object-update.test.ts` › `optional content` › `unset` — `update((u) => [u.content.unset()])` on an optional composite — prisma-next's `$unset` removes the field entirely rather than writing `null`, so the returned document lacks the `content` key and the `{ content: null }` assertion fails (Prisma returns `content: null`)
- `test/integration/test/ports/prisma/functional/composites-object-upsert-update/composites-object-upsert-update.test.ts` › `optional content` › `unset` — `upsert({ update: (u) => [u.content.unset()] })` on an optional composite — prisma-next's `$unset` removes the field entirely rather than writing `null`, so the returned document lacks the `content` key and the `{ content: null }` assertion fails

- `test/ports/prisma/functional/issues-29176-cursor-parameter-regression/issues-29176-cursor-parameter-regression.test.ts` › `correctly handles a cursor with parameterised values` — cursor pagination with parameterised (runtime UUID) composite-cursor values — prisma-next's cursor is exclusive; it skips the cursor row and returns `[{ result_index: 2 }]` instead of Prisma's inclusive `[{ result_index: 1 }, { result_index: 2 }]`.
- `test/ports/prisma/functional/issues-29254-query-plan-cache-mutation/issues-29254-query-plan-cache-mutation.test.ts` › `correctly handles two subsequent queries with a different cursor` — two sequential cursor-paginated queries with different cursors each return the correct page — prisma-next's exclusive cursor + skip=1 shifts both pages (query 1 → id3 not id2; query 2 → `[]` not id3) vs Prisma's inclusive cursor.
- `test/ports/prisma/functional/issues-29309-datetime-cursor/issues-29309-datetime-cursor.test.ts` › `retrieves a cursor against a DATE column` — cursor pagination against a `pg/date` DATE column (composite `@@id([appId, createdAt])`) — prisma-next's exclusive cursor + skip=1 yields Jan 5/6/7 instead of Prisma's inclusive Jan 4/5/6.

- `test/ports/prisma/functional/issues-28151-broken-nested-set/issues-28151-broken-nested-set.test.ts` › `nested set should result in all expected linked rows` — verifies nested `set([...])` on a 1:N relation in `update` replaces relation membership so all three post ids end up linked — prisma-next's `RelationMutator` exposes only `create`/`connect`/`disconnect`; `p.set` is absent (type-rejected via inline `@ts-expect-error`) and undefined at runtime, so the mutation throws.
- `test/ports/prisma/functional/issues-28192-pg-historical-dates/issues-28192-pg-historical-dates.test.ts` › `historical dates with 2-digit years (00-99 AD) > correctly parses 31 AD timestamp` — verifies the `pg/date` DATE column round-trips year 31 AD — `pg/date@1` misparses PGlite's wire-format 2-digit year (century dropped: 31 → 1931); the `timestamp`/`timestamptz` columns return the correct year, only `date` is affected.
- `test/ports/prisma/functional/issues-28192-pg-historical-dates/issues-28192-pg-historical-dates.test.ts` › `historical dates with 2-digit years (00-99 AD) > correctly parses 32 AD timestamp` — verifies the DATE column round-trips year 32 AD — same `pg/date@1` century-drop gap (32 → 1932).
- `test/ports/prisma/functional/issues-28192-pg-historical-dates/issues-28192-pg-historical-dates.test.ts` › `historical dates with 2-digit years (00-99 AD) > correctly parses 40 AD timestamp` — verifies the DATE column round-trips year 40 AD — same `pg/date@1` century-drop gap (40 → 1940).
- `test/ports/prisma/functional/issues-28192-pg-historical-dates/issues-28192-pg-historical-dates.test.ts` › `historical dates with 2-digit years (00-99 AD) > correctly parses 99 AD timestamp` — verifies the DATE column round-trips year 99 AD — same `pg/date@1` century-drop gap (99 → 1999).
- `test/ports/prisma/functional/issues-28591-mapped-enums/issues-28591-mapped-enums.test.ts` › `create with mapped enum` — verifies `@map`-on-enum-member name translation (DB stores `'pending'`, ORM returns `'PENDING'`) — prisma-next's `native_enum` member values ARE the PG labels and `pg/enum@1` returns them verbatim (`'pending'`); there is no TS-name ↔ DB-label mapping.

- `test/ports/prisma/functional/issues-TML-1664-unknown-enum-value-read-error/issues-TML-1664-unknown-enum-value-read-error.test.ts` › `returns a structured error when reading an enum value unknown to the schema` — reading a row whose native PG enum column holds a value added to the DB after schema push (unknown to the contract) should surface a structured error (Prisma P2023) — prisma-next's `pg/enum@1` codec is a text passthrough; `decode()` returns the wire string verbatim without validating against the known member set, so the unknown value is returned as-is rather than raising `RUNTIME.DECODE_FAILED`.

- `test/integration/test/ports/prisma/functional/driver-adapters-error-forwarding/driver-adapters-error-forwarding.test.ts` › `correctly forwards error for queryRaw` — a driver query error raised during `findFirst` is returned by identity — prisma-next converts the original pg-client error into a distinct `SqlConnectionError`, so identity is not preserved.
- `test/integration/test/ports/prisma/functional/enums/enums.test.ts` › `fails at runtime when an invalid entry is entered manually in Mongo` — a raw-command-inserted enum value unknown to the contract is rejected when the ORM reads it — prisma-next installs a Mongo JSON-schema validator that rejects the raw invalid enum insert itself (`Document failed validation`), so the invalid document never reaches the ORM read/decode path asserted upstream.
