# Non-ported — composites-object-update

- `packages/client/tests/functional/composites/object/update.ts` › `optional > update` — optional composite partial update (`content: { upsert: { update: { text }, set: null } }`) — prisma-next has no partial composite-field `update` sub-operator; only full-replacement `$set` or field-ops are available.
- `packages/client/tests/functional/composites/object/update.ts` › `optional > update push nested list` — optional composite partial update pushing to nested upvotes list (`content: { upsert: { update: { upvotes: { push: [...] } }, set: null } }`) — same gap: no partial composite `update` sub-operator.
- `packages/client/tests/functional/composites/object/update.ts` › `optional > update set nested list` — optional composite partial update replacing nested upvotes list (`content: { upsert: { update: { upvotes: { set: [...] } }, set: null } }`) — same gap.
- `packages/client/tests/functional/composites/object/update.ts` › `required > update` — required composite partial update (`content: { update: { text } }`) — no partial composite `update` sub-operator in prisma-next; only full `$set` replacement available.
- `packages/client/tests/functional/composites/object/update.ts` › `required > update push nested list` — required composite partial update pushing to nested upvotes list (`content: { update: { upvotes: { push: [...] } } }`) — same gap.
- `packages/client/tests/functional/composites/object/update.ts` › `required > update set nested list` — required composite partial update replacing nested upvotes list (`content: { update: { upvotes: { set: [...] } } }`) — same gap.
- `packages/client/tests/functional/composites/object/update.ts` › `unset` (required variant) — asserts Prisma-specific "Unknown argument `unset`" throw when `content: { unset: true }` is used on a required composite field — prisma-next has no such validation error.
- `packages/client/tests/functional/composites/object/update.ts` › `upsert set` (required variant) — asserts Prisma-specific "Unknown argument `upsert`" throw for `content: { upsert: {...} }` on a required composite — prisma-next has no such validation.
- `packages/client/tests/functional/composites/object/update.ts` › `upsert set` (optional variant) — composite-level `content: { upsert: { update: {...}, set: {...} } }` operator — no equivalent in prisma-next's mongo ORM update path.
- `packages/client/tests/functional/composites/object/update.ts` › `upsert update` (required variant) — asserts Prisma-specific "Unknown argument `upsert`" throw for `content: { upsert: {...} }` — no such validation in prisma-next.
- `packages/client/tests/functional/composites/object/update.ts` › `upsert update` (optional variant) — composite-level `content: { upsert: { update: { upvotes: { push: {...} } }, set: null } }` operator — no equivalent in prisma-next's mongo ORM.
