# Non-ported — issues-28213-relation-join-batch-crash

- `packages/client/tests/functional/issues/28213-relation-join-batch-crash/tests.ts` › `should not crash when submitting a batch with relationLoadStrategy join` — submitting two concurrent findUniques with `relationLoadStrategy: 'join'` (a Prisma `relationJoins` preview-feature option) does not crash — the subject is the `relationLoadStrategy: 'join'` option itself and the crash it caused in that specific code path; prisma-next has no `relationLoadStrategy` option (it uses `include()` which is always a join, but via a different plan path that does not have this crash). The test also carries an explicit `skip` gate on `previewFeatures.includes('relationJoins')` so it is entirely gated on this Prisma-specific flag. Swapping the mechanism (plain include batch with no join-strategy option) would test something different.
