# Non-ported — issues-17797-no-env-error-init

Matrix: all providers, `skipDefaultClientInstance: true`, `skipDb: true` (skip when `clientEngineExecutor === 'remote'`). 1 test. Subject = instantiating `new PrismaClient({})` (via the upstream `newPrismaClient` harness factory) with no environment-variable configuration does not throw — i.e. a missing `DATABASE_URL` at construction time is not an eager error. prisma-next's ORM handle is not constructed by class instantiation with env-var discovery; it is obtained via `postgres({ contract, pg })` with an explicit connection reference, and there is no `DATABASE_URL`/`process.env` discovery in the ORM-client construction path. There is no equivalent construction path whose "doesn't throw" behavior could be faithfully tested → non-ported.

- `packages/client/tests/functional/issues/17797-no-env-error-init/tests.ts` › `instantiate works without failing` — verifies `new PrismaClient({})` does not throw when no `DATABASE_URL` env var is set — prisma-next has no equivalent env-var-based client construction path; the ORM handle is obtained via the explicit `postgres()` factory with a `pg` connection, never via env-var discovery
