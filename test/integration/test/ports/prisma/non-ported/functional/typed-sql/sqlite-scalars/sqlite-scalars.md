# Non-ported — typed-sql/sqlite-scalars

- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `int - output` — typed SQL `getInt` returns an Int column round-tripped as a number [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `int - input` — typed SQL `findInt` accepts a number param and matches the row [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `double - output` — typed SQL `getDouble` returns a Double column as a number [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `double - input` — typed SQL `findDouble` accepts a double param and matches the row [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `string - output` — typed SQL `getString` returns a String column round-tripped [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `string - input` — typed SQL `findString` accepts a string param and matches the row [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `BigInt - output` — typed SQL `getBigInt` returns a BigInt column as a bigint [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `BigInt - input` — typed SQL `findBigInt` accepts both number and bigint params and matches the row [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `DateTime - output` — typed SQL `getDateTime` returns a DateTime column as a Date [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `DateTime - input` — typed SQL `findDateTime` accepts a Date param and matches the row [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `Decimal - output` — typed SQL `getDecimal` returns a Decimal column as a Prisma.Decimal instance [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `Decimal - input` — typed SQL `findDecimal` accepts both Decimal and number params and matches the row [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `bytes - output` — typed SQL `getBytes` returns a Bytes column as a Uint8Array [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `bytes - input` — typed SQL `findBytes` accepts a Uint8Array param and matches the row [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `json - output` — typed SQL `getJson` returns a Json column typed as JsonValue [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars/test.ts` › `json - input` — typed SQL `findJson` accepts a json param and matches the row [providers: sqlite-only] — the case is SQLite-only and prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
