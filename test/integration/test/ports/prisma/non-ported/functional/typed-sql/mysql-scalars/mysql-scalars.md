# Non-ported — typed-sql/mysql-scalars

- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `int - output` — typed SQL `getInt` returns an Int column round-tripped as a JS number [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `int - input` — typed SQL `findInt` accepts a number param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `float - output` — typed SQL `getFloat` returns a Float column as a number (allowing Float32 precision loss) [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `float - input` — typed SQL `findFloat` accepts a float param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `double - output` — typed SQL `getDouble` returns a Double column as a number [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `double - input` — typed SQL `findDouble` accepts a double param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `string - output` — typed SQL `getString` returns a String column round-tripped [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `string - input` — typed SQL `findString` accepts a string param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `BigInt - output` — typed SQL `getBigInt` returns a BigInt column as a bigint [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `BigInt - input` — typed SQL `findBigInt` accepts both number and bigint params and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `DateTime - output` — typed SQL `getDateTime` returns a DateTime column as a Date [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `DateTime - input` — typed SQL `findDateTime` accepts a Date param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `Date - output` — typed SQL `getDate` returns a Date column as a Date [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `Date - input` — typed SQL `findDate` accepts a Date param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `Time - output` — typed SQL `getTime` returns a Time column as a Date [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `Time - input` — typed SQL `findTime` accepts a Time Date param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `Decimal - output` — typed SQL `getDecimal` returns a Decimal column as a Prisma.Decimal instance [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `Decimal - input` — typed SQL `findDecimal` accepts both Decimal and number params and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `bytes - output` — typed SQL `getBytes` returns a Bytes column as a Uint8Array [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `bytes - input` — typed SQL `findBytes` accepts a Uint8Array param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars/test.ts` › `json - output` — typed SQL `getJson` returns a Json column typed as JsonValue [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
