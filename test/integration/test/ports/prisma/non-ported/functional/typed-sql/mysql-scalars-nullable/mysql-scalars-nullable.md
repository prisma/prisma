# Non-ported — typed-sql/mysql-scalars-nullable

- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `int - output` — typed SQL `getInt` returns an Int column typed as `number | null` [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `int - input` — typed SQL `findInt` accepts a number param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `float - output` — typed SQL `getFloat` returns a Float column typed as `number | null` (Float32 precision tolerated) [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `float - input` — typed SQL `findFloat` accepts a float param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `double - output` — typed SQL `getDouble` returns a Double column typed as `number | null` [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `double - input` — typed SQL `findDouble` accepts a double param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `string - output` — typed SQL `getString` returns a String column typed as `string | null` [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `string - input` — typed SQL `findString` accepts a string param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `BigInt - output` — typed SQL `getBigInt` returns a BigInt column typed as `bigint | null` [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `BigInt - input` — typed SQL `findBigInt` accepts both number and bigint params and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `DateTime - output` — typed SQL `getDateTime` returns a DateTime column typed as `Date | null` [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `DateTime - input` — typed SQL `findDateTime` accepts a Date param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `Date - output` — typed SQL `getDate` returns a Date column typed as `Date | null` [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `Date - input` — typed SQL `findDate` accepts a Date param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `Time - output` — typed SQL `getTime` returns a Time column typed as `Date | null` [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `Time - input` — typed SQL `findTime` accepts a Time Date param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `Decimal - output` — typed SQL `getDecimal` returns a Decimal column as `Prisma.Decimal | null` instance [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `Decimal - input` — typed SQL `findDecimal` accepts both Decimal and number params and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `bytes - output` — typed SQL `getBytes` returns a Bytes column typed as `Uint8Array | null` [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `bytes - input` — typed SQL `findBytes` accepts a Uint8Array param and matches the row [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
- `packages/client/tests/functional/typed-sql/mysql-scalars-nullable/test.ts` › `json - output` — typed SQL `getJson` returns a Json column typed as JsonValue [providers: mysql-only] — the case is MySQL-only, while MySQL is an unsupported target; additionally prisma-next has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface, and substituting ORM or another provider would change the mechanism and storage semantics
