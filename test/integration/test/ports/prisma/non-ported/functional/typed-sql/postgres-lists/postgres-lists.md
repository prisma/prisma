# Non-ported — typed-sql-postgres-lists

- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `int - output` — that `$queryRawTyped` returns `number[] | null` from a postgres `INT[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `int - input` — that `$queryRawTyped` accepts a `number[]` parameter for an int-array-typed generated query — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `real - output` — that `$queryRawTyped` returns `number[] | null` from a postgres `REAL[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `real - input` — that `$queryRawTyped` accepts a `number[]` for a real-array-typed generated query — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `double - output` — that `$queryRawTyped` returns `number[] | null` from a postgres `DOUBLE PRECISION[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `double - input` — that `$queryRawTyped` accepts a `number[]` for a double-array-typed generated query — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `string - output` — that `$queryRawTyped` returns `string[] | null` from a postgres `TEXT[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `string - input` — that `$queryRawTyped` accepts a `string[]` for a text-array-typed generated query — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `BigInt - output` — that `$queryRawTyped` returns `bigint[] | null` from a postgres `BIGINT[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `BigInt - input` — that `$queryRawTyped` accepts a `number[]` or `bigint[]` for a bigint-array-typed generated query — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `DateTime - output` — that `$queryRawTyped` returns `Date[] | null` from a postgres `TIMESTAMP[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `DateTime - input` — that `$queryRawTyped` accepts a `Date[]` for a timestamp-array-typed generated query — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `Date - output` — that `$queryRawTyped` returns `Date[] | null` from a postgres `DATE[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `Date - input` — that `$queryRawTyped` accepts a `Date[]` for a date-array-typed generated query — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `Time - output` — that `$queryRawTyped` returns `Date[] | null` from a postgres `TIME[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `Time - input` — that `$queryRawTyped` accepts a `Date[]` for a time-array-typed generated query — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `Decimal - output` — that `$queryRawTyped` returns `Prisma.Decimal[] | null` from a postgres `NUMERIC[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `Decimal - input` — that `$queryRawTyped` accepts `Prisma.Decimal[]` or `number[]` for a decimal-array-typed generated query — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `xml - output` — that `$queryRawTyped` returns `string[] | null` from a postgres `XML[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `uuid - output` — that `$queryRawTyped` returns `string[] | null` from a postgres `UUID[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `uuid - input` — that `$queryRawTyped` accepts a `string[]` UUID array for a uuid-array-typed generated query — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `bytes - output` — that `$queryRawTyped` returns `Uint8Array[] | null` from a postgres `BYTEA[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `bytes - input` — that `$queryRawTyped` accepts a `Uint8Array[]` for a bytea-array-typed generated query — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
- `packages/client/tests/functional/typed-sql/postgres-lists/test.ts` › `json - output` — that `$queryRawTyped` returns `JsonValue[] | null` from a postgres `JSON[]` column — prisma-next has no `$queryRawTyped` / typed-SQL codegen surface
