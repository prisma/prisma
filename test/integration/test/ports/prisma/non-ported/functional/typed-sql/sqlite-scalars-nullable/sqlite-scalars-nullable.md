# Non-ported — typed-sql/sqlite-scalars-nullable

- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `int - output` — typed SQL `getInt` returns an Int column typed as `number | null` [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `int - input` — typed SQL `findInt` accepts a number param and matches the row [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `double - output` — typed SQL `getDouble` returns a Double column typed as `number | null` [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `double - input` — typed SQL `findDouble` accepts a double param and matches the row [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `string - output` — typed SQL `getString` returns a String column typed as `string | null` [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `string - input` — typed SQL `findString` accepts a string param and matches the row [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `BigInt - output` — typed SQL `getBigInt` returns a BigInt column typed as `bigint | null` [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `BigInt - input` — typed SQL `findBigInt` accepts both number and bigint params and matches the row [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `DateTime - output` — typed SQL `getDateTime` returns a DateTime column typed as `Date | null` [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `DateTime - input` — typed SQL `findDateTime` accepts a Date param and matches the row [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `Decimal - output` — typed SQL `getDecimal` returns a Decimal column as `Prisma.Decimal | null` instance [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `Decimal - input` — typed SQL `findDecimal` accepts both Decimal and number params and matches the row [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `bytes - output` — typed SQL `getBytes` returns a Bytes column typed as `Uint8Array | null` [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `bytes - input` — typed SQL `findBytes` accepts a Uint8Array param and matches the row [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `json - output` — typed SQL `getJson` returns a Json column typed as JsonValue [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `json - input` — typed SQL `findJson` accepts a json param and matches the row [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `forced nullable param` — typed SQL `nullableParam` accepts a forced `number | null` param and returns bigint value 0n [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
- `packages/client/tests/functional/typed-sql/sqlite-scalars-nullable/test.ts` › `forced nullable column` — typed SQL `nullableColumn` returns a forced-nullable `value?` column typed as `bigint | null` [providers: sqlite-only] — the case is SQLite-only, but this project excludes SQLite because prisma-next has no self-contained SQLite integration harness; prisma-next also has no generated typed-SQL `$queryRawTyped`/`generated/prisma/sql` surface
