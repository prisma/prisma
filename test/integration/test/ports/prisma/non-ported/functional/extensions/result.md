# non-ported: extensions/result

Source: `packages/client/tests/functional/extensions/result.ts`

Every test in this file exercises the Prisma Client `$extends({ result: {...} })` result-component
extension mechanism (computed fields, dependencies, shadowing, propagation to nested/fluent reads
and writes). prisma-next has no `$extends` client-extension surface (zero hits for `$extends`/
`getExtensionContext`/`defineExtension` across `packages/` outside node_modules/dist;
`packages/3-extensions/` is database extensions, not client extensions). The subject of each test
IS the result-extension mechanism itself.

- `packages/client/tests/functional/extensions/result.ts` › `findFirst` — subject: a result extension computed field is added to `findFirst` results — non-ported (no `$extends` client-extension surface; subject is result component extension applied to findFirst results)
- `packages/client/tests/functional/extensions/result.ts` › `findFirst using $allModels` — subject: a `$allModels` result extension computed field is added to `findFirst` results — non-ported (no `$extends` client-extension surface; subject is `$allModels` result component extension applied to findFirst results)
- `packages/client/tests/functional/extensions/result.ts` › `findUnique` — subject: a result extension computed field is added to `findUnique` results — non-ported (no `$extends` client-extension surface; subject is result component extension applied to findUnique results)
- `packages/client/tests/functional/extensions/result.ts` › `findMany` — subject: a result extension computed field is added to all `findMany` results — non-ported (no `$extends` client-extension surface; subject is result component extension applied to findMany results)
- `packages/client/tests/functional/extensions/result.ts` › `create` — subject: a result extension computed field is added to `create` results — non-ported (no `$extends` client-extension surface; subject is result component extension applied to create results)
- `packages/client/tests/functional/extensions/result.ts` › `update` — subject: a result extension computed field is added to `update` results — non-ported (no `$extends` client-extension surface; subject is result component extension applied to update results)
- `packages/client/tests/functional/extensions/result.ts` › `upsert - update` — subject: a result extension computed field is added to `upsert` (update path) results — non-ported (no `$extends` client-extension surface; subject is result component extension applied to upsert update branch results)
- `packages/client/tests/functional/extensions/result.ts` › `upsert - create` — subject: a result extension computed field is added to `upsert` (create path) results — non-ported (no `$extends` client-extension surface; subject is result component extension applied to upsert create branch results)
- `packages/client/tests/functional/extensions/result.ts` › `when using select` — subject: a result extension computed field is present when using `select` (with the needed dependency fields selected) — non-ported (no `$extends` client-extension surface; subject is result extension field presence when using select)
- `packages/client/tests/functional/extensions/result.ts` › `when using select and $allModels` — subject: a `$allModels` result extension computed field is present when using `select` — non-ported (no `$extends` client-extension surface; subject is `$allModels` result extension field presence with select)
- `packages/client/tests/functional/extensions/result.ts` › `relationships: with include` — subject: result extension computed fields are added when using `include` — non-ported (no `$extends` client-extension surface; subject is result extension fields on included relations)
- `packages/client/tests/functional/extensions/result.ts` › `relationships: with select` — subject: result extension computed fields are added when using `select` on related models — non-ported (no `$extends` client-extension surface; subject is result extension fields on selected relation fields)
- `packages/client/tests/functional/extensions/result.ts` › `relationships: with deep select` — subject: result extension computed fields are added to deeply nested selected relations — non-ported (no `$extends` client-extension surface; subject is result extension fields on deeply nested selected relations)
- `packages/client/tests/functional/extensions/result.ts` › `relationships: mixed include and select` — subject: result extension computed fields are added when mixing `include` and `select` — non-ported (no `$extends` client-extension surface; subject is result extension fields when mixing include and select)
- `packages/client/tests/functional/extensions/result.ts` › `nested reads: include applies result extensions to nested models` — subject: result extensions are applied to nested models returned via `include` — non-ported (no `$extends` client-extension surface; subject is result extensions propagated to nested include results)
- `packages/client/tests/functional/extensions/result.ts` › `nested reads: select applies result extensions to nested models` — subject: result extensions are applied to nested models returned via `select` — non-ported (no `$extends` client-extension surface; subject is result extensions propagated to nested select results)
- `packages/client/tests/functional/extensions/result.ts` › `nested writes (create): include applies result extensions to nested models` — subject: result extensions are applied to nested models created inline and returned via `include` — non-ported (no `$extends` client-extension surface; subject is result extensions on nested-create results returned via include)
- `packages/client/tests/functional/extensions/result.ts` › `nested writes (create): select applies result extensions to nested models` — subject: result extensions are applied to nested models created inline and returned via `select` — non-ported (no `$extends` client-extension surface; subject is result extensions on nested-create results returned via select)
- `packages/client/tests/functional/extensions/result.ts` › `nested writes (update): include applies result extensions to nested models` — subject: result extensions are applied to nested updated models returned via `include` — non-ported (no `$extends` client-extension surface; subject is result extensions on nested-update results returned via include)
- `packages/client/tests/functional/extensions/result.ts` › `nested writes (update): select applies result extensions to nested models` — subject: result extensions are applied to nested updated models returned via `select` — non-ported (no `$extends` client-extension surface; subject is result extensions on nested-update results returned via select)
- `packages/client/tests/functional/extensions/result.ts` › `fluent reads apply result extensions to nested models` — subject: result extensions are applied to fluent-API read results — non-ported (no `$extends` client-extension surface; subject is result extensions applied to fluent-API read chaining results)
- `packages/client/tests/functional/extensions/result.ts` › `fluent writes (create) apply result extensions to nested models` — subject: result extensions are applied when using the fluent API for nested creates — non-ported (no `$extends` client-extension surface; subject is result extensions applied to fluent-API nested create results)
- `packages/client/tests/functional/extensions/result.ts` › `fluent writes (update) apply result extensions to nested models` — subject: result extensions are applied when using the fluent API for nested updates — non-ported (no `$extends` client-extension surface; subject is result extensions applied to fluent-API nested update results)
- `packages/client/tests/functional/extensions/result.ts` › `dependencies between computed fields` — subject: one computed field depends on another computed field in a result extension — non-ported (no `$extends` client-extension surface; subject is inter-field dependencies within result extensions)
- `packages/client/tests/functional/extensions/result.ts` › `shadowing dependency` — subject: a later result extension shadows a computed field from an earlier one, with the dependency resolved against the shadowed field — non-ported (no `$extends` client-extension surface; subject is shadowing of computed fields across chained result extensions)
- `packages/client/tests/functional/extensions/result.ts` › `shadowing dependency multiple times` — subject: shadowing of a computed field works correctly when done multiple times across chained extensions — non-ported (no `$extends` client-extension surface; subject is multiple successive shadowing of computed fields in chained result extensions)
- `packages/client/tests/functional/extensions/result.ts` › `empty extension does nothing` — subject: an empty result extension is a no-op — non-ported (no `$extends` client-extension surface; subject is no-op result extension behavior)
- `packages/client/tests/functional/extensions/result.ts` › `with null result` — subject: a result extension handles null query results correctly (no crash) — non-ported (no `$extends` client-extension surface; subject is result extension behavior when query returns null)
- `packages/client/tests/functional/extensions/result.ts` › `error in computed field` — subject: an error thrown in a computed field function propagates — non-ported (no `$extends` client-extension surface; subject is error propagation from computed field functions in result extensions)
- `packages/client/tests/functional/extensions/result.ts` › `error in computed field with no name` — subject: an error thrown in a computed field function of an unnamed extension propagates — non-ported (no `$extends` client-extension surface; subject is error propagation in computed fields of unnamed result extensions)
- `packages/client/tests/functional/extensions/result.ts` › `nested includes should include scalars and relations` — subject: when result extensions depend on relation fields, the related models are included in the result — non-ported (no `$extends` client-extension surface; subject is automatic inclusion of relation dependencies in result extensions)
- `packages/client/tests/functional/extensions/result.ts` › `when any type is passed as an input default selection type is returned` — subject: type-level — passing `any` as args returns the default result selection type — non-ported (no `$extends` client-extension surface; subject is type inference when `any` args are passed to result-extended methods)
- `packages/client/tests/functional/extensions/result.ts` › `when args have both include and select and one of them is optional, result includes both` — subject: type-level — type inference when args have both `include` and `select` — non-ported (no `$extends` client-extension surface; subject is type inference combining include and select in result extension method signatures)
