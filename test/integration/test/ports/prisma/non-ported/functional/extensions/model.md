# non-ported: extensions/model

Source: `packages/client/tests/functional/extensions/model.ts`

Every test in this file exercises the Prisma Client `$extends({ model: {...} })` / `$allModels`
model-component extension mechanism (per-model and `$allModels` methods, precedence, overriding
built-in model methods, `Prisma.getExtensionContext(this)` typing, `$parent`, Prisma type-utility
usage). prisma-next has no `$extends` client-extension surface (zero hits for `$extends`/
`getExtensionContext`/`defineExtension`/`$parent` across `packages/` outside node_modules/dist;
`packages/3-extensions/` is database extensions, not client extensions). The subject of each test
IS the model-extension mechanism itself.

- `packages/client/tests/functional/extensions/model.ts` › `extend specific model` — subject: a model-component extension method is available only on the targeted model — non-ported (no `$extends` client-extension surface; subject is model component extension scoped to a specific model)
- `packages/client/tests/functional/extensions/model.ts` › `chain $on with $extends` — subject: chaining `.$on(...).$extends(...)` preserves the extension method — non-ported (no `$extends` client-extension surface; subject is chaining `$on` event-listener with `$extends`)
- `packages/client/tests/functional/extensions/model.ts` › `extend all models` — subject: an `$allModels` extension method is available on every model — non-ported (no `$extends` client-extension surface; subject is `$allModels` model component extension)
- `packages/client/tests/functional/extensions/model.ts` › `pass arguments to ext method` — subject: arguments passed to a model extension method are forwarded — non-ported (no `$extends` client-extension surface; subject is argument forwarding in model component extensions)
- `packages/client/tests/functional/extensions/model.ts` › `return value to ext method` — subject: the return value of a model extension method is propagated — non-ported (no `$extends` client-extension surface; subject is return value propagation in model component extensions)
- `packages/client/tests/functional/extensions/model.ts` › `specific model extension has precedence over $allModels` — subject: a per-model extension method overrides the same-named `$allModels` method — non-ported (no `$extends` client-extension surface; subject is specific-model vs `$allModels` precedence)
- `packages/client/tests/functional/extensions/model.ts` › `last extension takes precedence over earlier ones` — subject: when two extensions define the same model method, the later one wins — non-ported (no `$extends` client-extension surface; subject is model extension method precedence ordering)
- `packages/client/tests/functional/extensions/model.ts` › `allows to override built-in methods` — subject: a model extension overrides built-in model methods like `findMany`/`findFirst` — non-ported (no `$extends` client-extension surface; subject is overriding built-in model methods via model extensions)
- `packages/client/tests/functional/extensions/model.ts` › `non-conflicting extensions can co-exist` — subject: two extensions defining different model methods both work simultaneously — non-ported (no `$extends` client-extension surface; subject is non-conflicting model extensions coexisting)
- `packages/client/tests/functional/extensions/model.ts` › `extension methods can call each other` — subject: one model extension method invokes another on the same model — non-ported (no `$extends` client-extension surface; subject is intra-extension method calling within model extensions)
- `packages/client/tests/functional/extensions/model.ts` › `extension methods can call model methods` — subject: a model extension method calls built-in model methods like `findMany` — non-ported (no `$extends` client-extension surface; subject is calling built-in model methods from within model extension methods)
- `packages/client/tests/functional/extensions/model.ts` › `extension methods can call methods of other extensions` — subject: a method from a later extension calls a method defined by an earlier extension — non-ported (no `$extends` client-extension surface; subject is cross-extension method invocation in model extensions)
- `packages/client/tests/functional/extensions/model.ts` › `empty extension does nothing` — subject: `$extends({ model: {} })` is a no-op and existing model methods still work — non-ported (no `$extends` client-extension surface; subject is no-op model extension behavior)
- `packages/client/tests/functional/extensions/model.ts` › `only accepts methods` — subject: type-level rejection of non-function properties in a model extension — non-ported (no `$extends` client-extension surface; subject is type-level rejection of non-function properties in model extensions)
- `packages/client/tests/functional/extensions/model.ts` › `error in extension methods` — subject: synchronous errors thrown inside a model extension method propagate — non-ported (no `$extends` client-extension surface; subject is synchronous error propagation in model extension methods)
- `packages/client/tests/functional/extensions/model.ts` › `error in async methods` — subject: asynchronous errors rejected inside an async model extension method propagate — non-ported (no `$extends` client-extension surface; subject is asynchronous error propagation in model extension methods)
- `packages/client/tests/functional/extensions/model.ts` › `error in async PrismaPromise methods` — subject: errors in extension methods returning `PrismaPromise` propagate (across provider + driverAdapter combinations) — non-ported (no `$extends` client-extension surface; subject is error propagation in PrismaPromise-returning model extension methods)
- `packages/client/tests/functional/extensions/model.ts` › `error in extension methods without name` — subject: errors thrown in unnamed model extension methods propagate — non-ported (no `$extends` client-extension surface; subject is error propagation in unnamed model extension methods)
- `packages/client/tests/functional/extensions/model.ts` › `custom method re-using input types to augment them via intersection` — subject: a model extension method uses `Prisma.Args`/`Prisma.Exact` to augment input types via intersection — non-ported (no `$extends` client-extension surface; subject is Prisma type-utility usage in model extension method signatures)
- `packages/client/tests/functional/extensions/model.ts` › `custom method re-using input types to augment them via mapped type` — subject: a model extension method uses `Prisma.Args`/mapped type to augment input types — non-ported (no `$extends` client-extension surface; subject is Prisma mapped-type usage in model extension method signatures)
- `packages/client/tests/functional/extensions/model.ts` › `custom method re-using output to augment it via intersection` — subject: a model extension method uses `Prisma.Result` to augment return types via intersection — non-ported (no `$extends` client-extension surface; subject is Prisma Result type-utility usage in model extension return types)
- `packages/client/tests/functional/extensions/model.ts` › `custom method re-using payload output types` — subject: a model extension method uses `Prisma.Payload` types to manipulate output types — non-ported (no `$extends` client-extension surface; subject is Prisma Payload type-utility usage in model extension return types)
- `packages/client/tests/functional/extensions/model.ts` › `custom method that uses exact for narrowing inputs` — subject: `Prisma.Exact` narrows input types to prevent passing extra properties — non-ported (no `$extends` client-extension surface; subject is Prisma.Exact input narrowing in model extension methods)
- `packages/client/tests/functional/extensions/model.ts` › `custom method that uses exact for narrowing generic inputs` — subject: `Prisma.Exact` narrows generic input types in model extension methods — non-ported (no `$extends` client-extension surface; subject is Prisma.Exact generic narrowing in model extension methods)
- `packages/client/tests/functional/extensions/model.ts` › `getExtension context on specific model and non-generic this` — subject: the type of `Prisma.getExtensionContext(this)` for a non-generic specific-model extension — non-ported (no `$extends` client-extension surface; subject is getExtensionContext typing for specific model non-generic this)
- `packages/client/tests/functional/extensions/model.ts` › `getExtension context on generic model and non-generic this` — subject: the type of `Prisma.getExtensionContext(this)` for a non-generic `$allModels` extension — non-ported (no `$extends` client-extension surface; subject is getExtensionContext typing for `$allModels` non-generic this)
- `packages/client/tests/functional/extensions/model.ts` › `getExtension context on specific model and generic this` — subject: the type of `Prisma.getExtensionContext(this)` for a generic specific-model extension — non-ported (no `$extends` client-extension surface; subject is getExtensionContext typing for specific model generic this)
- `packages/client/tests/functional/extensions/model.ts` › `getExtension context on generic model and generic this` — subject: the type of `Prisma.getExtensionContext(this)` for a generic `$allModels` extension — non-ported (no `$extends` client-extension surface; subject is getExtensionContext typing for `$allModels` generic this)
- `packages/client/tests/functional/extensions/model.ts` › `one specific user extension along a generic $allModels model extension` — subject: a specific-model extension and a `$allModels` extension coexist with correct type narrowing — non-ported (no `$extends` client-extension surface; subject is coexistence of specific and generic model extensions)
- `packages/client/tests/functional/extensions/model.ts` › `does not allow to pass invalid properties` — subject: runtime rejection when passing an unexpected property — non-ported (no `$extends` client-extension surface; subject is runtime rejection of invalid model extension properties)
- `packages/client/tests/functional/extensions/model.ts` › `input type should be able to be passed to method accepting same input types` — subject: a model extension method accepts the same input type as an underlying model method — non-ported (no `$extends` client-extension surface; subject is input-type compatibility between extension methods and underlying model methods)
- `packages/client/tests/functional/extensions/model.ts` › `an extension can also reference a previous one via parent on a specific model` — subject: a later specific-model extension calls an earlier extension's method via `ctx.$parent` — non-ported (no `$extends` client-extension surface; subject is `$parent` chaining in specific-model extensions)
- `packages/client/tests/functional/extensions/model.ts` › `an extension can also reference a previous one via parent on $allModels` — subject: a later `$allModels` extension calls an earlier extension's method via `ctx.$parent` — non-ported (no `$extends` client-extension surface; subject is `$parent` chaining in `$allModels` extensions)
- `packages/client/tests/functional/extensions/model.ts` › `batching of PrismaPromise returning custom model methods` — subject: PrismaPromises returned by custom model-extension methods are batched into one transaction (query log asserted) — non-ported (no `$extends` client-extension surface; additionally no query-log/batch-coalescing observability)
- `packages/client/tests/functional/extensions/model.ts` › `batching of PrismaPromise returning custom model methods and query` — subject: batching of custom model-method PrismaPromises combined with a query extension (query log asserted) — non-ported (no `$extends` client-extension surface; additionally no query-log/batch-coalescing observability)
