# Non-ported — issues-21136-extensions-mocking-library

Matrix: SQLite-only (`optOut` from all other providers), `skipDb: true`. All 6 tests are type-level only — they verify that `prisma.$extends({...})` result types are correctly inferred when the extended client is wrapped in a deep-mocking-library proxy (`DeepMockProxy<T>`), covering both a full model-override extension and an empty extension. The subject is `$extends` result/query-extension type-inference compatibility with mocking-library proxy types. prisma-next has no `$extends` surface, so there is no extended-client type to feed into the mock proxy → non-ported.

- `packages/client/tests/functional/issues/21136-extensions-mocking-library/tests.ts` › `with full override extension > output inference (via \`mockResolvedValue\`)` — verifies `$extends` model-override extension output types propagate to the mock's `mockResolvedValue` parameter — no `$extends` surface in prisma-next
- `packages/client/tests/functional/issues/21136-extensions-mocking-library/tests.ts` › `with full override extension > input inference (via \`calledWith\`)` — verifies `$extends` model-override extension input types propagate to the mock's `calledWith` parameter — no `$extends` surface in prisma-next
- `packages/client/tests/functional/issues/21136-extensions-mocking-library/tests.ts` › `with full override extension > call still work as usual` — verifies `$extends` model-override extension result type is still correctly inferred for direct calls — no `$extends` surface in prisma-next
- `packages/client/tests/functional/issues/21136-extensions-mocking-library/tests.ts` › `with empty extension > output inference (via \`mockResolvedValue\`)` — verifies `$extends({})` empty extension preserves output types for the mock's `mockResolvedValue` — no `$extends` surface in prisma-next
- `packages/client/tests/functional/issues/21136-extensions-mocking-library/tests.ts` › `with empty extension > input inference (via \`calledWith\`)` — verifies `$extends({})` empty extension preserves input types for the mock's `calledWith` — no `$extends` surface in prisma-next
- `packages/client/tests/functional/issues/21136-extensions-mocking-library/tests.ts` › `with empty extension > call still work as usual` — verifies `$extends({})` empty extension preserves runtime call type inference — no `$extends` surface in prisma-next
