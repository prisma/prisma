# Non-ported — globalOmitJSGenerator

- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `throws if omit is not an object` — that passing a non-object to the `omit` constructor option (JS generator client) throws a specific validation error — prisma-next has no global-omit constructor option; JS-generator specificity not reproducible
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `throws if omit is null` — that passing `null` to the `omit` constructor option (JS generator) throws a specific validation error — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `throws if unknown model is mentioned in omit` — that referencing a non-existent model in the constructor `omit` option (JS generator) throws "Unknown model name" — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `throws if unknown field is mentioned in omit` — that referencing a non-existent field in the constructor `omit` option (JS generator) throws "does not have a field named" — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `throws if non boolean field is used in omit` — that passing a non-boolean omit value (JS generator) throws "Omit field option value must be a boolean" — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `throws if relation field is used in omit` — that specifying a relation field in the constructor `omit` option (JS generator) throws "Relations are already excluded by default" — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `omitting every field` — that globally omitting all fields (JS generator) causes a specific error on query — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `findFirstOrThrow` — that `findFirstOrThrow()` on a JS-generator client with global omit returns a row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `findUniqueOrThrow` — that `findUniqueOrThrow()` on a JS-generator client with global omit returns a row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `findFirst` — that `findFirst()` on a JS-generator client with global omit returns a row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `findUnique` — that `findUnique()` on a JS-generator client with global omit returns a row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `findMany` — that `findMany()` on a JS-generator client with global omit returns rows without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `create` — that `create()` on a JS-generator client with global omit returns the created row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `delete` — that `delete()` on a JS-generator client with global omit returns the deleted row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `createMany does not crash` — that `createMany()` on a JS-generator client with global omit completes without error — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `deleteMany does not crash` — that `deleteMany()` on a JS-generator client with global omit completes without error — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `updateMany does not crash` — that `updateMany()` on a JS-generator client with global omit completes without error — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `groupBy does not crash` — that `groupBy()` on a JS-generator client with global omit completes without error — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `count does not crash` — that `count()` on a JS-generator client with global omit completes without error — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `aggregate does not crash` — that `aggregate()` on a JS-generator client with global omit completes without error — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `createManyAndReturn` — that `createManyAndReturn()` on a JS-generator client with global omit returns rows without the omitted field (postgres/cockroachdb only) — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `update` — that `update()` on a JS-generator client with global omit returns the updated row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `upsert` — that `upsert()` on a JS-generator client with global omit returns the upserted row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `excluding more than one field at a time` — that globally omitting multiple fields (JS generator) excludes all of them from results — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `allows to include globally omitted field with omit: false` — that per-query `omit: { password: false }` re-includes a globally omitted field (JS generator) — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `allows to include globally omitted field with select: true` — that per-query `select` re-includes a globally omitted field (JS generator) — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `works for nested relations (include)` — that globally omitted fields are excluded from nested relation rows via `include` (JS generator) — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `works for nested relations (select)` — that globally omitted fields are excluded from nested relation rows via `select` (JS generator) — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `works for fluent api` — that globally omitted fields are excluded from the fluent API (JS generator) — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `works after extending the client` — that global omit persists after `$extends({})` (JS generator) — prisma-next has no global-omit constructor option and no `$extends`
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `works with fluent api after extending the client` — that global omit persists in the fluent API after `$extends({})` (JS generator) — prisma-next has no global-omit constructor option and no `$extends`
- `packages/client/tests/functional/globalOmitJSGenerator/test.ts` › `works with result extension, depending on explicitly omitted field` — that a result extension needing the globally omitted field can compute a derived field (JS generator) — prisma-next has no global-omit constructor option and no `$extends` result extensions
