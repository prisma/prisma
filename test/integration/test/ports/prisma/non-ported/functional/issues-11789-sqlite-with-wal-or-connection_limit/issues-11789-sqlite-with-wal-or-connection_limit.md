# Non-ported — issues-11789-sqlite-with-wal-or-connection_limit

Every test is SQLite/D1-specific. `_matrix.ts` declares `provider: Providers.SQLITE` only, and the `optOut` block excludes `sqlserver`, `mongodb`, `postgresql`, `cockroachdb`, and `mysql` ("Test is made for SQLite only"). The non-D1 tests are further gated to `driverAdapter === 'js_better_sqlite3'`. The SQLite corpus is out of scope per spec § No SQLite corpus.

- `packages/client/tests/functional/issues/11789-sqlite-with-wal-or-connection_limit/tests.ts` › `D1 does not support journal_mode = WAL` — verifies the D1 driver adapter rejects `PRAGMA journal_mode = WAL` with a `D1_ERROR: not authorized` message — SQLite/D1-specific; gated by `testIf(driverAdapter === 'js_d1')`; SQLite corpus out of scope
- `packages/client/tests/functional/issues/11789-sqlite-with-wal-or-connection_limit/tests.ts` › `5 concurrent upsert should succeed with journal_mode = WAL` — verifies 5 concurrent profile upserts complete without error under SQLite `journal_mode=WAL` via `PrismaBetterSqlite3` — SQLite-only; gated by `describeIf(driverAdapter === undefined)` + matrix `provider: Providers.SQLITE`; SQLite corpus out of scope
- `packages/client/tests/functional/issues/11789-sqlite-with-wal-or-connection_limit/tests.ts` › `5 concurrent upsert should succeed with connection_limit=1 & journal_mode = WAL` — verifies 5 concurrent profile upserts complete without error under SQLite `connection_limit=1` + `journal_mode=WAL` — SQLite-only; gated by `describeIf(driverAdapter === undefined)` + matrix `provider: Providers.SQLITE`; SQLite corpus out of scope
- `packages/client/tests/functional/issues/11789-sqlite-with-wal-or-connection_limit/tests.ts` › `5 concurrent upsert should succeed with connection_limit=1` — verifies 5 concurrent profile upserts complete without error under SQLite `connection_limit=1` — SQLite-only; gated by `describeIf(driverAdapter === undefined)` + matrix `provider: Providers.SQLITE`; SQLite corpus out of scope
- `packages/client/tests/functional/issues/11789-sqlite-with-wal-or-connection_limit/tests.ts` › `5 concurrent delete should succeed with connection_limit=1` — verifies 5 concurrent user deletes complete without error under SQLite `connection_limit=1` — SQLite-only; gated by `describeIf(driverAdapter === undefined)` + matrix `provider: Providers.SQLITE`; SQLite corpus out of scope
