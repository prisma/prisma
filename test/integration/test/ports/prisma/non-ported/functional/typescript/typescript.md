# Non-ported — typescript

- `packages/client/tests/functional/typescript/tests.ts` › `typescript > No test suites found` — no-op guard test emitted only when zero generated suite files are found (early return) [providers: n/a] — this is upstream functional-harness infrastructure that compiles Prisma-generated `.generated/**/*.ts` suite files, not a query/runtime behavior; prisma-next has neither those generated Prisma Client suite files nor an equivalent generated-client compilation harness
- `packages/client/tests/functional/typescript/tests.ts` › `typescript > %s` — `[each]` — for each generated `.generated/**/*.ts` suite file, runs the TS compiler semantic diagnostics and `assert.fail`s if any type error's path falls within that suite's directory [providers: n/a] — this is upstream functional-harness infrastructure that compiles Prisma-generated `.generated/**/*.ts` suite files, not a query/runtime behavior; prisma-next has neither those generated Prisma Client suite files nor an equivalent generated-client compilation harness
