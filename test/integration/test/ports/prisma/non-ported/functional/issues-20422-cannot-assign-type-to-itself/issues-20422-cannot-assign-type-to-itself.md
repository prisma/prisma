# Non-ported: issues/20422-cannot-assign-type-to-itself

Source: `packages/client/tests/functional/issues/20422-cannot-assign-type-to-itself/tests.ts`

- `packages/client/tests/functional/issues/20422-cannot-assign-type-to-itself/tests.ts` › `return types must be compatible with returned data types in classes (type test only)` — verifies that `PrismaNamespace.UserGetPayload<{ select: T }>` is assignable as the return type of a class method that calls `prismaClient.user.findUnique({ where: { id }, select })` — the subject is the compatibility of Prisma-generated utility types (`PrismaNamespace.UserGetPayload`, `PrismaNamespace.UserSelect`, `PrismaClient`) in a generic class method; these are Prisma codegen types with no prisma-next equivalents. The constraint cannot be re-expressed against prisma-next types because the test verifies that Prisma's generated `GetPayload` type (which encodes the `select` projection into the return type via a type parameter) compiles without error — a prisma-next equivalent would require re-expressing the entire Prisma select-payload generic mechanism against prisma-next's structural ORM types, which are fundamentally different in shape and do not have a `GetPayload`-style utility.
