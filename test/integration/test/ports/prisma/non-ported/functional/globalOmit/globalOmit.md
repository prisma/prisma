# Non-ported — globalOmit

- `packages/client/tests/functional/globalOmit/test.ts` › `throws if omit is not an object` — that passing a non-object to the `omit` constructor option throws a specific validation error — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `throws if omit is null` — that passing `null` to the `omit` constructor option throws a specific validation error — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `throws if unknown model is mentioned in omit` — that referencing a non-existent model in the constructor `omit` option throws with "Unknown model name" — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `throws if unknown field is mentioned in omit` — that referencing a non-existent field in the constructor `omit` option throws with "Model does not have a field named" — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `throws if non boolean field is used in omit` — that passing a non-boolean omit value throws "Omit field option value must be a boolean" — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `throws if relation field is used in omit` — that specifying a relation field in the constructor `omit` option throws "Relations are already excluded by default" — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `omitting every field` — that globally omitting all fields causes a specific error on query ("The global omit configuration excludes every field") — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `findFirstOrThrow` — that `findFirstOrThrow()` on a client constructed with `omit: { user: { password: true } }` returns a row without `password` — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `findUniqueOrThrow` — that `findUniqueOrThrow()` on a client with global omit returns a row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `findFirst` — that `findFirst()` on a client with global omit returns a row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `findUnique` — that `findUnique()` on a client with global omit returns a row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `findMany` — that `findMany()` on a client with global omit returns rows without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `create` — that `create()` on a client with global omit returns the created row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `delete` — that `delete()` on a client with global omit returns the deleted row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `createMany does not crash` — that `createMany()` on a client with global omit completes without error (count assertion) — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `deleteMany does not crash` — that `deleteMany()` on a client with global omit completes without error (count assertion) — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `updateMany does not crash` — that `updateMany()` on a client with global omit completes without error (count assertion) — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `groupBy does not crash` — that `groupBy()` on a client with global omit completes without error — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `count does not crash` — that `count()` on a client with global omit completes without error — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `aggregate does not crash` — that `aggregate()` on a client with global omit completes without error — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `createManyAndReturn` — that `createManyAndReturn()` on a client with global omit returns rows without the omitted field (postgres/cockroachdb only) — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `update` — that `update()` on a client with global omit returns the updated row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `upsert` — that `upsert()` on a client with global omit returns the upserted row without the omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `excluding more than one field at a time` — that globally omitting multiple fields excludes all of them from results — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `allows to include globally omitted field with omit: false` — that a per-query `omit: { password: false }` re-includes a globally omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `allows to include globally omitted field with select: true` — that a per-query `select: { id: true, password: true }` re-includes a globally omitted field — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `works for nested relations (include)` — that globally omitted fields are also excluded from nested relation rows fetched via `include` — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `works for nested relations (select)` — that globally omitted fields are also excluded from nested relation rows fetched via `select` — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `works for fluent api` — that globally omitted fields are excluded from rows returned via the fluent API — prisma-next has no global-omit constructor option
- `packages/client/tests/functional/globalOmit/test.ts` › `works after extending the client` — that global omit persists after `$extends({})` — prisma-next has no global-omit constructor option and no `$extends`
- `packages/client/tests/functional/globalOmit/test.ts` › `works with fluent api after extending the client` — that global omit persists in the fluent API after `$extends({})` — prisma-next has no global-omit constructor option and no `$extends`
- `packages/client/tests/functional/globalOmit/test.ts` › `works with result extension, depending on explicitly omitted field` — that a result extension that `needs` the globally omitted field can still compute a derived field — prisma-next has no global-omit constructor option and no `$extends` result extensions
