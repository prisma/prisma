# Non-ported — string-filters

- `packages/client/tests/functional/string-filters/tests.ts` › `startsWith matches prefix` — `value: { startsWith: 'foo' }` returns rows prefixed with 'foo' — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `startsWith with no match` — `startsWith: 'xyz'` returns 0 rows — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `startsWith with empty string matches all` — `startsWith: ''` returns all 6 rows — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `endsWith matches suffix` — `endsWith: 'baz'` returns rows suffixed with 'baz' — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `endsWith with no match` — `endsWith: 'xyz'` returns 0 rows — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `endsWith with empty string matches all` — `endsWith: ''` returns all 6 rows — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `contains matches substring` — `contains: 'bar'` returns rows containing 'bar' — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `contains with no match` — `contains: 'xyz'` returns 0 rows — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `contains with empty string matches all` — `contains: ''` returns all 6 rows — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `combined startsWith + endsWith` — `startsWith:'foo', endsWith:'baz'` returns only 'foo bar baz' — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `combined startsWith + contains` — `startsWith:'foo', contains:'bar'` returns only 'foo bar baz' — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `combined contains + endsWith` — `contains:'bar', endsWith:'baz'` returns only 'foo bar baz' — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `NOT startsWith` — `NOT: { value: { startsWith:'foo' } }` returns 4 rows — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `NOT contains` — `NOT: { value: { contains:'bar' } }` returns 4 rows — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `NOT endsWith` — `NOT: { value: { endsWith:'baz' } }` returns 4 rows — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `mode: insensitive > contains case-insensitive` — `contains: 'bar', mode: 'insensitive'` returns case-insensitive matches — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `mode: insensitive > startsWith case-insensitive` — `startsWith: 'foo', mode: 'insensitive'` returns case-insensitive prefix matches — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
- `packages/client/tests/functional/string-filters/tests.ts` › `mode: insensitive > endsWith case-insensitive` — `endsWith: 'baz', mode: 'insensitive'` returns case-insensitive suffix matches — Prisma 8 has no `startsWith`/`endsWith`/`contains`/`mode:insensitive` string operators; `.like()`/`.ilike()` are different operators (raw LIKE patterns), not faithful substitutes
