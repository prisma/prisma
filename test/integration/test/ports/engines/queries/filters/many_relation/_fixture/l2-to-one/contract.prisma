model Blog { id Int @id name String posts Post[] }
model Post { id Int @id blog_id Int blog Blog @relation(fields: [blog_id], references: [id]) comment Comment? }
model Comment { id Int @id popularity Int postId Int @unique post Post @relation(fields: [postId], references: [id]) }
