## queries/aggregation/avg.rs

PASS `test/integration/test/ports/engines/queries/aggregation/avg/avg.test.ts` › `returns null numeric averages with no records` — `queries::aggregation::avg::avg_no_records`: numeric averages are null for an empty input set
PASS `test/integration/test/ports/engines/queries/aggregation/avg/avg.test.ts` › `computes numeric averages across two rows` — `queries::aggregation::avg::avg_some_records`: averages Int, BigInt, and Float values over two rows
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/avg.rs` › `queries::aggregation::avg::avg_with_all_sorts_of_query_args` — applies take, negative take, where, skip, and cursor before numeric aggregation — prisma-next's aggregate terminal compiles collection filters but not take/skip/cursor, and its cursor surface additionally requires an explicit order axis, so the complete argument matrix cannot be expressed faithfully
PASS `test/integration/test/ports/engines/queries/aggregation/avg/avg.test.ts` › `returns null decimal average with no records` — `queries::aggregation::avg::avg_no_records`: Decimal average is null for an empty input set
PASS `test/integration/test/ports/engines/queries/aggregation/avg/avg.test.ts` › `computes decimal average across two rows` — `queries::aggregation::avg::avg_some_records`: averages two Decimal values
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/avg.rs` › `queries::aggregation::avg::avg_with_all_sorts_of_query_args` — applies take, negative take, where, skip, and cursor before Decimal aggregation — prisma-next's aggregate terminal compiles collection filters but not take/skip/cursor, and its cursor surface additionally requires an explicit order axis, so the complete argument matrix cannot be expressed faithfully

## queries/aggregation/combination_spec.rs

- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::no_records` — combined count/sum/avg/min/max on an empty numeric input with a `String @id @default(cuid())` schema — prisma-next explicitly does not support the source `cuid()` default; substituting `cuid(2)` changes the generator and branded field type, while omitting the default changes the schema
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::some_records` — inserts two numeric rows without ids so `@default(cuid())` supplies them, then computes count/sum/avg/min/max — prisma-next explicitly does not support the source `cuid()` default; adding explicit ids or substituting `cuid(2)` changes the tested input/default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::with_query_args` — applies take, negative take, where, skip, and cursor to all numeric aggregate functions — prisma-next's aggregate terminal does not apply collection take/skip/cursor state, and its cursor requires an explicit order axis, so the complete matrix is not expressible
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::unstable_cursor` — rejects aggregation that combines an id cursor with ordering by a non-unique float — the shared source schema requires the CUID1 `String @id @default(cuid())` default, but prisma-next authoring accepts only `cuid(2)`; substituting CUID2 changes the schema and generated field type
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::no_records` — combined count/sum/avg/min/max on an empty Decimal input with a `String @id @default(cuid())` schema — prisma-next explicitly does not support the source `cuid()` default; substituting `cuid(2)` changes the generator and branded field type, while omitting the default changes the schema
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::some_records` — inserts two Decimal rows without ids so `@default(cuid())` supplies them, then computes count/sum/avg/min/max — prisma-next explicitly does not support the source `cuid()` default; adding explicit ids or substituting `cuid(2)` changes the tested input/default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::with_query_args` — applies take, negative take, where, skip, and cursor to all Decimal aggregate functions — prisma-next's aggregate terminal does not apply collection take/skip/cursor state, and its cursor requires an explicit order axis, so the complete matrix is not expressible

## queries/aggregation/count.rs

PASS `test/integration/test/ports/engines/queries/aggregation/count/count.test.ts` › `returns zero count with no records` — `queries::aggregation::count::count_no_records`: count-all returns zero for an empty input set
PASS `test/integration/test/ports/engines/queries/aggregation/count/count.test.ts` › `counts non-null values in nullable fields` — `queries::aggregation::count::count_nullable_fields`: SQL count expressions count non-null String and Int values independently
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/count.rs` › `queries::aggregation::count::count_with_all_sorts_of_query_args` — applies take, negative take, where, orderBy, skip, and cursor before count-all — prisma-next's aggregate terminal compiles filters but not order/take/skip/cursor state, and its cursor requires an explicit order axis, so the complete argument matrix cannot be expressed faithfully
PASS `test/integration/test/ports/engines/queries/aggregation/count/count.test.ts` › `preserves requested field order for empty counts` — `queries::aggregation::count::count_empty_result`: SQL count expressions return zero for count-all and nullable fields in both requested selection orders

## queries/aggregation/group_by.rs

PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `returns no groups with no records` — `queries::aggregation::group_by::group_by_no_records`: SQL grouping selects count(id), float, and sum(int) over the three source grouping keys and returns no groups
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `groups records with field count and sum` — `queries::aggregation::group_by::group_by_some_records`: groups by String, selects field count and Float sum, and orders ascending
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `orders grouped records in reverse` — `queries::aggregation::group_by::group_by_rev_ordering`: orders grouped field-count and sum results descending by String
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `orders groups by multiple fields` — `queries::aggregation::group_by::group_by_multiple_ordering`: groups by String and Int and orders by both fields
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_take_skip` — paginates ordered grouped results with positive/negative take and skip — prisma-next's SQL builder supports limit/offset but has no negative-take semantics, so the complete three-query matrix cannot be expressed faithfully
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `applies scalar filters before grouping` — `queries::aggregation::group_by::group_by_scalar_filters`: applies Int equality and Float less-than filters before ordered grouping
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `applies relation filters before grouping` — `queries::aggregation::group_by::group_by_relation_filters`: filters through the optional to-one relation before ordered grouping for both source predicates
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `orders groups by count aggregation` — `queries::aggregation::group_by::group_by_ordering_count_aggregation`: orders groups ascending and descending by field count
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `orders groups by sum aggregation` — `queries::aggregation::group_by::group_by_ordering_sum_aggregation`: orders groups ascending and descending by Float sum
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `orders groups by avg aggregation` — `queries::aggregation::group_by::group_by_ordering_avg_aggregation`: orders groups ascending and descending by Float average
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `orders groups by min aggregation` — `queries::aggregation::group_by::group_by_ordering_min_aggregation`: orders groups ascending and descending by Float minimum
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `orders groups by max aggregation` — `queries::aggregation::group_by::group_by_ordering_max_aggregation`: orders groups ascending and descending by Float maximum
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `orders groups by multiple aggregations` — `queries::aggregation::group_by::group_by_ordering_aggr_multiple_fields`: orders by field count descending and Int sum ascending
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `combines aggregate ordering with having` — `queries::aggregation::group_by::group_by_ordering_aggr_and_having`: combines two aggregate order keys with a grouped Float having predicate
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `orders by an aggregation without selecting it` — `queries::aggregation::group_by::group_by_ordering_aggr_without_selecting`: orders by field count while selecting only Int sum
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `computes enum extrema globally and per group` — `queries::aggregation::group_by::regression_21789`: computes native-enum min/max globally and per ordered group
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::regression_28084` — groups through a nested relation filter using the String contains operator — prisma-next has no contains operator, and substituting LIKE is forbidden
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `rejects scalar selection with no grouping fields` — `queries::aggregation::group_by::group_by_without_by_selection`: submits scalar selection with an empty grouping list and receives the equivalent structured argument rejection
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `rejects a selected scalar absent from grouping fields` — `queries::aggregation::group_by::group_by_mismatch_by_args_query_sel`: selects String, count(String), and sum(Float) while grouping only by Int and receives the equivalent PostgreSQL grouping rejection
PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `rejects an ordered scalar absent from grouping fields` — `queries::aggregation::group_by::group_by_by_args_order_by`: selects count(Int) and sum(Float), groups only by Int, orders by String descending, and receives the equivalent PostgreSQL grouping rejection
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_empty_aggregation_selection` — submits `_sum: {}` and asserts the query-engine validation error for an empty aggregate object — prisma-next's public grouped builder can omit an aggregate selector but cannot submit an explicitly empty sum selector, so the invalid source input cannot be expressed faithfully
