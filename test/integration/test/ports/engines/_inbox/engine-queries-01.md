## queries/aggregation/avg.rs

PASS `test/integration/test/ports/engines/queries/aggregation/avg/avg.test.ts` › `returns null numeric averages with no records` — `queries::aggregation::avg::avg_no_records`: numeric averages are null for an empty input set
PASS `test/integration/test/ports/engines/queries/aggregation/avg/avg.test.ts` › `computes numeric averages across two rows` — `queries::aggregation::avg::avg_some_records`: averages Int, BigInt, and Float values over two rows
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/avg.rs` › `queries::aggregation::avg::avg_with_all_sorts_of_query_args` — applies take, negative take, where, skip, and cursor before numeric aggregation — prisma-next's aggregate terminal compiles collection filters but not take/skip/cursor, and its cursor surface additionally requires an explicit order axis, so the complete argument matrix cannot be expressed faithfully
PASS `test/integration/test/ports/engines/queries/aggregation/avg/avg.test.ts` › `returns null decimal average with no records` — `queries::aggregation::avg::avg_no_records`: Decimal average is null for an empty input set
PASS `test/integration/test/ports/engines/queries/aggregation/avg/avg.test.ts` › `computes decimal average across two rows` — `queries::aggregation::avg::avg_some_records`: averages two Decimal values
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/avg.rs` › `queries::aggregation::avg::avg_with_all_sorts_of_query_args` — applies take, negative take, where, skip, and cursor before Decimal aggregation — prisma-next's aggregate terminal compiles collection filters but not take/skip/cursor, and its cursor surface additionally requires an explicit order axis, so the complete argument matrix cannot be expressed faithfully

## queries/aggregation/combination_spec.rs

- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::no_records` — combined count/sum/avg/min/max on an empty numeric input with a `String @id @default(cuid())` schema — prisma-next explicitly does not support the source `cuid()` default; substituting `cuid(2)` changes the generator and branded field type, while omitting the default changes the schema
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::some_records` — inserts two numeric rows without ids so `@default(cuid())` supplies them, then computes count/sum/avg/min/max — prisma-next explicitly does not support the source `cuid()` default; adding explicit ids or substituting `cuid(2)` changes the tested input/default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::with_query_args` — applies take, negative take, where, skip, and cursor to all numeric aggregate functions — prisma-next's aggregate terminal does not apply collection take/skip/cursor state, and its cursor requires an explicit order axis, so the complete matrix is not expressible
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::unstable_cursor` — rejects aggregation that combines an id cursor with ordering by a non-unique float — prisma-next exposes no equivalent aggregation argument validation: aggregate ignores order/cursor state and cursor requires values for the active order axes
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::no_records` — combined count/sum/avg/min/max on an empty Decimal input with a `String @id @default(cuid())` schema — prisma-next explicitly does not support the source `cuid()` default; substituting `cuid(2)` changes the generator and branded field type, while omitting the default changes the schema
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::some_records` — inserts two Decimal rows without ids so `@default(cuid())` supplies them, then computes count/sum/avg/min/max — prisma-next explicitly does not support the source `cuid()` default; adding explicit ids or substituting `cuid(2)` changes the tested input/default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/combination_spec.rs` › `queries::aggregation::combination_spec::with_query_args` — applies take, negative take, where, skip, and cursor to all Decimal aggregate functions — prisma-next's aggregate terminal does not apply collection take/skip/cursor state, and its cursor requires an explicit order axis, so the complete matrix is not expressible

## queries/aggregation/count.rs

PASS `test/integration/test/ports/engines/queries/aggregation/count/count.test.ts` › `returns zero count with no records` — `queries::aggregation::count::count_no_records`: count-all returns zero for an empty input set
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/count.rs` › `queries::aggregation::count::count_nullable_fields` — counts non-null values independently for nullable String and Int fields — prisma-next's public AggregateBuilder exposes only count-all and has no field-specific count selector
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/count.rs` › `queries::aggregation::count::count_with_all_sorts_of_query_args` — applies take, negative take, where, orderBy, skip, and cursor before count-all — prisma-next's aggregate terminal compiles filters but not order/take/skip/cursor state, and its cursor requires an explicit order axis, so the complete argument matrix cannot be expressed faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/count.rs` › `queries::aggregation::count::count_empty_result` — reports count-all and field-specific zero counts for an empty set — prisma-next has count-all but no field-specific count selector, so the complete asserted result cannot be produced

## queries/aggregation/group_by.rs

PASS `test/integration/test/ports/engines/queries/aggregation/group_by/group_by.test.ts` › `returns no groups with no records` — `queries::aggregation::group_by::group_by_no_records`: grouped aggregate returns an empty array with no rows
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_some_records` — groups by String and orders groups ascending while selecting count and sum — prisma-next's GroupedCollection has no orderBy surface, and dropping the asserted group order would weaken the test
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_rev_ordering` — orders grouped count/sum results descending by the grouped String — prisma-next's GroupedCollection has no orderBy surface
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_multiple_ordering` — groups on String and Int with multi-field ordering — prisma-next's GroupedCollection has no orderBy surface
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_take_skip` — paginates ordered grouped results with positive/negative take and skip — prisma-next's GroupedCollection has no orderBy, take, or skip surface
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_scalar_filters` — applies scalar filters before grouping and orders the resulting groups — prisma-next can filter before grouping but GroupedCollection cannot express the required descending group order, and in-memory sorting is forbidden
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_relation_filters` — applies to-one relation filters before grouping and orders the groups — prisma-next's GroupedCollection cannot express the required descending group order, and dropping it would weaken both exact snapshots
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_ordering_count_aggregation` — orders groups ascending and descending by aggregate count — prisma-next's GroupedCollection has no orderBy-aggregate surface
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_ordering_sum_aggregation` — orders groups ascending and descending by aggregate sum — prisma-next's GroupedCollection has no orderBy-aggregate surface
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_ordering_avg_aggregation` — orders groups ascending and descending by aggregate average — prisma-next's GroupedCollection has no orderBy-aggregate surface
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_ordering_min_aggregation` — orders groups ascending and descending by aggregate minimum — prisma-next's GroupedCollection has no orderBy-aggregate surface
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_ordering_max_aggregation` — orders groups ascending and descending by aggregate maximum — prisma-next's GroupedCollection has no orderBy-aggregate surface
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_ordering_aggr_multiple_fields` — orders groups by count then sum aggregate keys — prisma-next's GroupedCollection has no multi-key orderBy-aggregate surface
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_ordering_aggr_and_having` — combines aggregate ordering with a scalar grouped-field having filter — prisma-next GroupedCollection has no orderBy surface and its having builder accepts aggregate metrics rather than grouped scalar-field predicates
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_ordering_aggr_without_selecting` — orders by aggregate count without selecting that aggregate — prisma-next's GroupedCollection has no orderBy-aggregate surface
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::regression_21789` — computes enum min/max globally and per group with deterministic group ordering — prisma-next supports enum extrema but GroupedCollection has no orderBy surface, so the complete two-query regression assertion cannot be ported faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::regression_28084` — groups through a nested relation filter using the String contains operator — prisma-next has no contains operator, and substituting LIKE is forbidden
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_without_by_selection` — rejects an empty by argument while selecting a scalar field — prisma-next's typed groupBy requires at least one field and exposes only aggregate selection after grouping, so the invalid query shape cannot be submitted through the public API
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_mismatch_by_args_query_sel` — rejects selecting a non-aggregated scalar absent from by — prisma-next GroupedCollection automatically projects only the declared group fields and exposes no independent scalar selection that could express the invalid input
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_by_args_order_by` — rejects orderBy on a scalar absent from by — prisma-next GroupedCollection exposes no orderBy surface, so the invalid combination cannot be submitted
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/aggregation/group_by.rs` › `queries::aggregation::group_by::group_by_empty_aggregation_selection` — submits `_sum: {}` and asserts the query-engine validation error for an empty aggregate object — prisma-next's public grouped builder can omit an aggregate selector but cannot submit an explicitly empty sum selector, so the invalid source input cannot be expressed faithfully
