## query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/equals.rs

- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/equals.rs` › `queries::filters::composite::equals::basic_equality` — to-many composite list equals and implicit equal with NOT — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/equals.rs` › `queries::filters::composite::equals::field_order_matters` — to-many composite equals is order-sensitive on object fields — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/equals.rs` › `queries::filters::composite::equals::object_order_matters` — to-many composite equals is order-sensitive on list elements — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/equals.rs` › `queries::filters::composite::equals::empty_comparison` — to-many composite equals empty list and implicit with NOT — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/equals.rs` › `queries::filters::composite::equals::single_object` — to-many composite equals with single object errors 2009 — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/equals.rs` › `queries::filters::composite::equals::basic` — to-one composite equals full nested object with NOT — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/equals.rs` › `queries::filters::composite::equals::field_order_matters` — to-one composite equals is order-sensitive on fields — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully

## query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs

- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::basic` — to-many composite every gt condition — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::empty` — to-many composite every empty filter matches all, with NOT — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::empty_logical_conditions` — to-many composite every empty AND/OR/NOT logical conditions — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::locical_and` — to-many composite every implicit and explicit AND — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::insensitive` — to-many composite every insensitive contains — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::logical_or` — to-many composite every explicit OR contains — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::logical_not` — to-many composite every explicit NOT contains — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::nested_every` — to-many composite every into nested to-many composite every gte — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully

## query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is.rs

- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is.rs` › `queries::filters::composite::is::basic` — to-one composite is/isNot lt condition — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is.rs` › `queries::filters::composite::is::empty` — to-one composite is/isNot empty filter matches all — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is.rs` › `queries::filters::composite::is::multiple_and` — to-one composite is implicit and explicit AND — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is.rs` › `queries::filters::composite::is::multiple_or` — to-one composite is explicit OR — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is.rs` › `queries::filters::composite::is::not_combinations` — to-one composite NOT/isNot combinations with AND and OR — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is.rs` › `queries::filters::composite::is::multiple_hops` — to-one composite is over multiple nested hops with OR and NOT — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is.rs` › `queries::filters::composite::is::insensitive_must_work` — to-one composite is insensitive contains — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully

## query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_empty.rs

- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_empty.rs` › `queries::filters::composite::is_empty::basic_empty_check` — to-many composite isEmpty true/false — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_empty.rs` › `queries::filters::composite::is_empty::negation` — to-many composite isEmpty negated with NOT — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_empty.rs` › `queries::filters::composite::is_empty::silly_combinations` — to-many composite isEmpty AND/OR combinations — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully

## query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs

- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::basic` — to-one composite isSet true/false — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::negation` — to-one composite isSet negated with NOT — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::with_null_check` — to-one composite isSet combined with is/isNot null — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::negation_with_null_check` — to-one composite isSet negated combined with is/isNot null — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::with_equality_check` — to-one composite isSet combined with is/isNot equality — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::silly_logical_combinations` — to-one composite isSet OR/AND/NOT combinations — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::fails_on_required` — isSet on required to-one composite errors 2009 — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::basic` — scalar field isSet true/false — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::negation` — scalar field isSet negated with NOT — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::with_null_check` — scalar field isSet combined with not/equals null — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::negation_with_null_check` — scalar field isSet negated combined with not/equals null — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::with_equality_check` — scalar field isSet combined with equals/not value — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::silly_logical_combinations` — scalar field isSet OR/AND/NOT combinations — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::fails_on_required` — isSet on required scalar field errors 2009 — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::basic` — to-many composite isSet true/false — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::negation` — to-many composite isSet negated with NOT — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::with_equality_check` — to-many composite isSet combined with some/none/equals — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/is_set.rs` › `queries::filters::composite::is_set::silly_logical_combinations` — to-many composite isSet OR/AND/NOT combinations — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
