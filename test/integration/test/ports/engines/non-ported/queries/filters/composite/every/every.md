# Non-ported — queries/filters/composite/every.rs

- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::basic` — to-many composite every gt condition — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::empty` — to-many composite every empty filter matches all, with NOT — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::empty_logical_conditions` — to-many composite every empty AND/OR/NOT logical conditions — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::locical_and` — to-many composite every implicit and explicit AND — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::insensitive` — to-many composite every insensitive contains — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::logical_or` — to-many composite every explicit OR contains — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::logical_not` — to-many composite every explicit NOT contains — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/every.rs` › `queries::filters::composite::every::nested_every` — to-many composite every into nested to-many composite every gte — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but its field builder only exposes nullability/list modifiers and cannot preserve the source mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
