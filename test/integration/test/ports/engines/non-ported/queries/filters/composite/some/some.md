# Non-ported — queries/filters/composite/some.rs

- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/some.rs` › `queries::filters::composite::some::basic` — filters documents whose embedded-object list has an element with `a_2 < 0` — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the fields required to be mapped and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/some.rs` › `queries::filters::composite::some::empty` — applies empty `some` and its top-level negation to embedded-object lists — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the fields required to be mapped and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/some.rs` › `queries::filters::composite::some::empty_logical_conditions` — applies empty AND/OR/NOT predicates inside embedded-list `some` — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the fields required to be mapped and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/some.rs` › `queries::filters::composite::some::locical_and` — applies implicit and explicit AND inside embedded-list `some` — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the fields required to be mapped and defaults, so neither authoring surface can express the complete source schema faithfully; this case also uses Prisma's escaped `contains` operator, which Prisma Next does not expose
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/some.rs` › `queries::filters::composite::some::insensitive` — applies insensitive `contains` inside embedded-list `some` — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the fields required to be mapped and defaults, so neither authoring surface can express the complete source schema faithfully; this case also uses Prisma's escaped `contains` operator with `mode: insensitive`, which Prisma Next does not expose
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/some.rs` › `queries::filters::composite::some::logical_or` — applies OR branches inside embedded-list `some` — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the fields required to be mapped and defaults, so neither authoring surface can express the complete source schema faithfully; this case also uses Prisma's escaped `contains` operator, which Prisma Next does not expose
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/some.rs` › `queries::filters::composite::some::logical_not` — applies a NOT list inside embedded-list `some` — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the fields required to be mapped and defaults, so neither authoring surface can express the complete source schema faithfully; this case also uses Prisma's escaped `contains` operator, which Prisma Next does not expose
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/some.rs` › `queries::filters::composite::some::nested_some` — nests `some` across two embedded-object lists — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the fields required to be mapped and defaults, so neither authoring surface can express the complete source schema faithfully
