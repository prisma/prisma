# Non-ported — queries/filters/many_relation.rs

- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/many_relation.rs` › `queries::filters::many_relation::simple_scalar_filter` — nested scalar relation filtering — source Blog, Post, and Comment ids use `@default(cuid())`; prisma-next cannot author the upstream CUID1 storage default
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/many_relation.rs` › `queries::filters::many_relation::l1_1_rel` — filtering through a to-one relation — source Blog, Post, and Comment ids use `@default(cuid())`; prisma-next cannot author the upstream CUID1 storage default
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/many_relation.rs` › `queries::filters::many_relation::l1_m_rel_some` — to-many `some` filtering and AND combinations — source Blog, Post, and Comment ids use `@default(cuid())`; prisma-next cannot author the upstream CUID1 storage default
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/many_relation.rs` › `queries::filters::many_relation::l1_m_rel_every` — to-many `every` filtering — source Blog, Post, and Comment ids use `@default(cuid())`; prisma-next cannot author the upstream CUID1 storage default
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/many_relation.rs` › `queries::filters::many_relation::l1_m_rel_none` — to-many `none` filtering — source Blog, Post, and Comment ids use `@default(cuid())`; prisma-next cannot author the upstream CUID1 storage default
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/many_relation.rs` › `queries::filters::many_relation::l2_m_rel_some_some` — two levels of `some` relation filters — source Blog, Post, and Comment ids use `@default(cuid())`; prisma-next cannot author the upstream CUID1 storage default
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/many_relation.rs` › `queries::filters::many_relation::l2_m_rel_all` — two-level `some`/`every`/`none` combinations — source Blog, Post, and Comment ids use `@default(cuid())`; prisma-next cannot author the upstream CUID1 storage default
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/many_relation.rs` › `queries::filters::many_relation::crazy_filters` — combined relation filters plus Prisma string contains — prisma-next has no contains operator, and substituting like would change escaping semantics
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/many_relation.rs` › `queries::filters::many_relation::m2m_join_relation_1level` — M:N relation filter combined with startsWith and endsWith — prisma-next has neither string operator, and like substitution is forbidden
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/many_relation.rs` › `queries::filters::many_relation::prisma_25104` — nested M:N `every` filter — prisma-next recognizes explicit M:N junctions only with a compound primary key, but the upstream implicit junction has no primary key and instead uses a unique `(A, B)` index plus a separate `B` index
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/many_relation.rs` › `queries::filters::many_relation::prisma_23742` — nested M:N filter regression — prisma-next recognizes explicit M:N junctions only with a compound primary key, but the upstream implicit junction has no primary key and instead uses a unique `(A, B)` index plus a separate `B` index
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/many_relation.rs` › `queries::filters::many_relation::nested_some_filter_m2m_different_pk` — nested M:N filter with different primary-key names — prisma-next recognizes explicit M:N junctions only with a compound primary key, but the upstream implicit junction has no primary key and instead uses a unique `(A, B)` index plus a separate `B` index
