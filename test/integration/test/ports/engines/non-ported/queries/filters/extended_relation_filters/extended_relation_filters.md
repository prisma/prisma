# Non-ported — queries/filters/extended_relation_filters.rs

- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::basic_scalar_filter` — filters artists by scalar ArtistId equality — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::rel_filter_l1_depth1` — filters albums by their to-one artist's name — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::mysql_rel1_many_filters` — verifies MySQL's case-insensitive relation string filters — the case is MySQL-only and MySQL is an unsupported target
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::pg_rel1_some_filter` — filters artists with some album whose title starts with a prefix — prisma-next has no escaped `startsWith` string operator; substituting SQL LIKE would change the mechanism and metacharacter semantics
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::pg_rel1_every_filter` — filters artists where every album title contains or does not contain a substring — prisma-next has no escaped `contains` string operator; substituting SQL LIKE would change semantics
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::pg_rel1_none_filter` — filters artists where no album title contains a substring — prisma-next has no escaped `contains` string operator; substituting SQL LIKE would change semantics
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::rel_filter_l2_some_some` — filters artists with nested albums/tracks using two-level `some` predicates — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::rel_filter_l2_all_filters` — verifies every combination of nested some/every/none track filters, including an exact nested `startsWith` assertion — prisma-next has no escaped `startsWith` operator, so the complete assertion set cannot be ported without dropping or substituting that assertion
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::rel_filter_l2_implicit_and_some` — applies an implicit AND of two to-one relation predicates inside track `some` — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::rel_filter_l2_implicit_and_every` — applies an implicit AND of two to-one relation predicates inside track `every` — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::rel_filter_l2_explicit_and_some` — applies explicit AND lists, including empty AND, inside track `some` — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::rel_filter_l2_explicit_and_every` — applies explicit AND lists, including empty AND, inside track `every` — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::rel_filter_l2_explicit_or_all` — applies explicit OR lists, including empty OR, inside track `some` and `every` — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::rel_filter_l2_explicit_not_all` — applies explicit NOT lists, including empty NOT, inside track `some` and `every` — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::rel_filter_l3` — filters genres through track, album, and artist relations — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::rel_scalar_filter` — combines a nested relation predicate with a scalar TrackId predicate — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::empty_none` — filters genres with no related tracks using an empty `none` predicate — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/extended_relation_filters.rs` › `queries::filters::extended_relation_filters::empty_some` — filters genres with any related track using an empty `some` predicate — source Artist, Album, Track, Genre, and MediaType ids use `@default(cuid())`, and setup creates records without ids; prisma-next cannot author the upstream CUID1 storage defaults, while supplying ids would change the tested inputs and default behavior
