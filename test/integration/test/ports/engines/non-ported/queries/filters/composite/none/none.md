# Non-ported — queries/filters/composite/none.rs

- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/none.rs` › `queries::filters::composite::none::basic` — filters documents whose embedded-object list has no element with `a_2 < 0` — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the required mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/none.rs` › `queries::filters::composite::none::empty` — applies empty `none` and its top-level negation to embedded-object lists — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the required mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/none.rs` › `queries::filters::composite::none::empty_logical_conditions` — applies empty AND/OR/NOT predicates inside embedded-list `none` — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the required mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/none.rs` › `queries::filters::composite::none::locical_and` — applies implicit and explicit AND inside embedded-list `none` — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the required mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully; this case also uses Prisma's escaped `contains` operator, which Prisma Next does not expose
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/none.rs` › `queries::filters::composite::none::insensitive` — applies insensitive `contains` inside embedded-list `none` — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the required mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully; this case also uses Prisma's escaped `contains` operator with `mode: insensitive`, which Prisma Next does not expose
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/none.rs` › `queries::filters::composite::none::logical_or` — applies OR branches inside embedded-list `none` — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the required mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully; this case also uses Prisma's escaped `contains` operator, which Prisma Next does not expose
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/none.rs` › `queries::filters::composite::none::logical_not` — applies a NOT list inside embedded-list `none` — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the required mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully; this case also uses Prisma's escaped `contains` operator, which Prisma Next does not expose
- `query-engine/connector-test-kit-rs/query-engine-tests/tests/queries/filters/composite/none.rs` › `queries::filters::composite::none::nested_none` — nests `none` across two embedded-object lists — non-portable because Prisma Next Mongo PSL rejects the faithful `id Int @id @map("_id")` schema with `PSL_MONGO_ID_REQUIRED`; the public Mongo TypeScript builder permits an Int id, but only exposes nullability/list field modifiers and cannot preserve the required mapped fields and defaults, so neither authoring surface can express the complete source schema faithfully
