model Reading {
  id             Int    @id
  label          String
  createdAt      temporal.createdAt()
  updatedAt      temporal.updatedAt()
  createdAtText  temporal.createdAtString()
  updatedAtText  temporal.updatedAtString()
}
