datasource db {
  provider = "postgresql"
  url      = env("TEST_POSTGRES_URI")
}

generator gen {
  provider      = "predefined-generator"
  binaryTargets = ["darwin"]
}

model User {
  id   Int    @id
  name String
}