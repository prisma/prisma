datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator gen {
  provider      = "predefined-generator"
  binaryTargets = ["marvin"]
}

model User {
  id   Int    @id
  name String
}