generator client {
  provider        = "prisma-client"
  engineType      = "client"
  output          = "../generated/client"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  id String @id @default(uuid())
}
