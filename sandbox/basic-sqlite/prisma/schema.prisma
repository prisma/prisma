generator client {
  provider        = "prisma-client"
  engineType      = "client"
  output          = "../generated/client"
}

datasource db {
  provider = "sqlite"
}

model User {
  id String @id @default(uuid())
}
