generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model PaymentGateway {
  id    Int     @id @default(autoincrement())
  name  String
  value String  // This column causes Studio bug
  active Boolean @default(true)
}