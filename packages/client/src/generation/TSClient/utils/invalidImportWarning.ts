export const invalidImportWarning = [
  'Add your client as a `db` dependency to `package.json` and import it as a dependency instead',
  'If you have a `tsconfig.json`, set `moduleResolution` to `node16`, `nodenext`, or `bundler`',
  'For further information, please refer to the message shown on `prisma generate`',
]
