generator client {
  provider = "prisma-client-js"
  previewFeatures = [#FEATURES#EXTRA_FEATURES]
}

datasource db {
  provider = "#PROVIDER"
  url      = env("DATABASE_URI_#PROVIDER")
}

model User {
  id #ID
}