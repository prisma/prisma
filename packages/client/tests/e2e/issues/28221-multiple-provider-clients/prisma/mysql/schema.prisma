generator client {
  provider     = "prisma-client"
  output       = "../../generated/mysql-client"
  engineType   = "client"
}

datasource db {
  provider = "mysql"
  url      = env("MYSQL_URL")
}

model User {
  id    Int    @id @default(autoincrement())
  email String @unique
  name  String
}
