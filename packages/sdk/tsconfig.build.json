{
  "extends": "../../tsconfig.build.json",
  "compilerOptions": {
    "outDir": "dist",
    "emitDeclarationOnly": true,
    "baseUrl": "./src"
  },
  "include": ["src"],
  // to not copy local engines to dist
  // so npm doesn't publish them
  "exclude": ["src/get-generators/engines"]
}
