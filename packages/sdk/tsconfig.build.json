{
  "extends": "../../tsconfig.build.regular.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"],
  // to not copy local engines to dist
  // so that npm does not publish them
  "exclude": ["src/get-generators/engines"]
}
