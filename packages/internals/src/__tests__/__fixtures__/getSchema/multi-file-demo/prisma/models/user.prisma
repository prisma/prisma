// User model
model User {
        id        Int       @id @default(autoincrement())
        email     String    @unique
        name      String?
        payments  Payment[]
        createdAt DateTime  @default(now())
}
