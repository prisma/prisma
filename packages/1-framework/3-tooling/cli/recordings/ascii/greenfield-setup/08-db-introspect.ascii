>






























────────────────────────────────────────────────────────────────────────────────
> # Inspect the live database schema tree [initialized with user(id, email)]
>





























────────────────────────────────────────────────────────────────────────────────
> # Inspect the live database schema tree [initialized with user(id, email)]
>





























────────────────────────────────────────────────────────────────────────────────
> # Inspect the live database schema tree [initialized with user(id, email)]
> prisma-next db schema





























────────────────────────────────────────────────────────────────────────────────
> # Inspect the live database schema tree [initialized with user(id, email)]
> prisma-next db schema





























────────────────────────────────────────────────────────────────────────────────
> # Inspect the live database schema tree [initialized with user(id, email)]
> prisma-next db schema





























────────────────────────────────────────────────────────────────────────────────
> # Inspect the live database schema tree [initialized with user(id, email)]
> prisma-next db schema
prisma-next db schema → Inspect the live database schema
│
│ config:               prisma-next.config.ts
│ database:             postgres://****:****@127.0.0.1:5433/postgres
│
│ Read more             https://pris.ly/db-schema
└

│
│  contract
│  ├─ table user
│  │  ├─ columns
│  │  │  ├─ email: text (not nullable)
│  │  │  └─ id: int4 (not nullable)
│  │  └─ primary key: id
│  └─ plpgsql dependency is installed
>












────────────────────────────────────────────────────────────────────────────────
