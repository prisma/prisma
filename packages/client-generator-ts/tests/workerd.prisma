datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-ts"
  engineType = "client"
  runtime = "workerd"
  output = "./generated"
}

model User {
  id   Int    @id
  name String
}
