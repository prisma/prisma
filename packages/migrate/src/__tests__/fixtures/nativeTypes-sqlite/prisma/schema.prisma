datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Cat {
  id   Int    @id
  name String @db.VarChar(100)
}

model Dog {
  id           Int  @id
  bestFriendId Int?
}
