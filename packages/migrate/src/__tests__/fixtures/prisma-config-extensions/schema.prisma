generator client {
  provider = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider = "postgres"
  extensions = [vector]
}

model User {
  id       Int @id @default(autoincrement())
  name     String
  position Vector3
}
