datasource db {
  provider   = "sqlserver"
  url        = env("TEST_MSSQL_JDBC_URI_MIGRATE")
  namespaces = ["base"]
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
}
