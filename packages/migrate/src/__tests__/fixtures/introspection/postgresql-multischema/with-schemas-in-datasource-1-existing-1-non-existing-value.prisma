datasource db {
  provider   = "postgres"
  url        = env("TEST_POSTGRES_URI_MIGRATE")
  namespaces = ["does-not-exist", "base"]
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
}
