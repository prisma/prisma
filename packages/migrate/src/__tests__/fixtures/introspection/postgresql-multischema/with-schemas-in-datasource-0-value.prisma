datasource db {
  provider   = "postgres"
  url        = env("TEST_POSTGRES_URI_MIGRATE")
  namespaces = []
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
}
