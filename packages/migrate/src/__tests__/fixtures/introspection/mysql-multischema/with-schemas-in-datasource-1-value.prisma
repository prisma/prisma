datasource db {
  provider = "mysql"
  url      = env("TEST_MYSQL_URI_MIGRATE")
  schemas  = ["base"]
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
}
