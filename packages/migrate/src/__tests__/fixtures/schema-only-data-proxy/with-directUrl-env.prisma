generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider  = "postgresql"
    url       = env("TEST_DATA_PROXY_URL")
    directUrl = env("TEST_POSTGRES_URI_MIGRATE") // like this
}
