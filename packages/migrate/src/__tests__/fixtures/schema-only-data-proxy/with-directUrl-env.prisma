generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider  = "postgresql"
    url       = env("PROXY_URL")
    directUrl = env("DIRECT_URL") // like this
}
    // directUrl = "postgres://prisma:prisma@localhost:5432/tests-migrateHELLOOO"
    // url       = "prisma://aws-us-east-1.prisma-data.com/?api_key=MY_API_KEY"
