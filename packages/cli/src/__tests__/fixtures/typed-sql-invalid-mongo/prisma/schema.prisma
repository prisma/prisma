generator client {
  provider = "prisma-client-js"
  previewFeatures = ["typedSql"]
}

datasource db {
  provider = "mongodb"
}

model User {
  id      String      @default(auto()) @id @map("_id")  @db.ObjectId
  email   String   @unique
  name    String?
}
