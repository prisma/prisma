generator client {
  provider = "prisma-client-js"
  output   = "../generated/client"
}

datasource db {
  provider = "postgres"
}

model User {
  id   Int @id @default(autoincrement())
  name String
}
