generator client {
  provider = "prisma-client-js"
  previewFeatures = ["typedSql"]
}

datasource db {
  provider = "sqlserver"
}

model User {
  id      Int      @default(autoincrement()) @id
  email   String   @unique
  name    String?
}
