datasource db {
  provider = "postgres"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id Int @id
}

model UserClient {
  id   Int    @id
  text String
}

model UserArgs {
  id Int @id
}

model Prisma {
  id Int @id
}

model Earth {
  id Int @id
}