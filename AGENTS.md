# Prisma Repository – Agent Field Notes

> **Meta note**: This is the primary agent knowledge base file. `CLAUDE.md` and `GEMINI.md` are symlinks to this file—always edit `AGENTS.md` directly. When learning something new about the codebase that would help with future tasks, update this file immediately.

- **Repository scope**: Monorepo for Prisma ORM, CLI, client, tests, etc. Many packages use TypeScript, Rust and WebAssembly (via engines), TSX, and Jest; automation relies on `pnpm`. Expect large fixture directories and generated files.
- **Internal format compatibility**: Internal data structures are version-lockstep across producers and consumers in this monorepo. When changing an internal plan/protocol/IR shape, replace it outright and update all in-repo readers/writers together; do not keep old-format/new-format compatibility branches unless there is a concrete external persistence or cross-version boundary.
- **Workspace layout**: Managed via pnpm workspaces and Turborepo (`turbo.json`). Node ^20.19 || ^22.12 || >=24.0 and pnpm >=10.15 <11 are required (see root `package.json`). Top-level scripts (`pnpm build`, `pnpm dev`, `pnpm test`) delegate into `scripts/ci/publish.ts`; package-specific commands run with `pnpm --filter @prisma/<pkg> <script>`. Turborepo caches builds; run `pnpm build` from root to build all packages in dependency order.
- **Key packages**: `packages/cli` (Prisma CLI entry point), `packages/migrate` (migrate/db namespace + fixtures), `packages/client` (client runtime), `packages/client-generator-ts` (new `prisma-client` generator), `packages/client-generator-js` (traditional `prisma-client-js` generator), `packages/client-generator-registry` (generator registry for managing generators), `packages/client-engine-runtime` (the core part of the new Rust binary free client based on Wasm query compiler, used by `ClientEngine` class in `packages/client`), `packages/client-common` (shared client utilities), `packages/client-runtime-utils` (utility types and singletons for Prisma Client), `packages/config` (`PrismaConfigInternal` + loader), `packages/internals` (shared CLI + engine glue), `packages/engines` (Rust binaries download wrapper), `packages/integration-tests` (matrix suites), `packages/query-plan-executor` (standalone query plan executor service for Prisma Accelerate), `packages/credentials-store` (credential storage utilities), sqlcommenter packages under `packages/sqlcommenter*`, and numerous driver adapters under `packages/adapter-*`.
- **Driver adapters & runtimes**: `packages/bundled-js-drivers` plus the `adapter-*` packages ship JS driver adapters: `adapter-pg`, `adapter-neon`, `adapter-libsql`, `adapter-planetscale`, `adapter-d1`, `adapter-better-sqlite3`, `adapter-mssql`, `adapter-mariadb`, `adapter-ppg` (Prisma Postgres Serverless). These are built on helpers in `driver-adapter-utils`; migrate/client fixtures exercise them, so adapter changes typically require fixture/test updates. CI tests driver adapters with flavors: `js_pg`, `js_neon`, `js_libsql`, `js_planetscale`, `js_d1`, `js_better_sqlite3`, `js_mssql`, `js_mariadb`, `js_pg_cockroachdb`.
- **Build & tooling**: Typescript-first repo with WASM/Rust assets (downloaded by `@prisma/engines`). Multiple `tsconfig.*` drive bundle vs runtime builds. Lint via `pnpm lint`, format via `pnpm format`. Maintenance scripts live in `scripts/` (e.g. `bump-engines.ts`, `bench.ts`, `ci/publish.ts` orchestrates build/test/publish flows). Build configuration uses esbuild via `helpers/compile/build.ts` with configs in `helpers/compile/configs.ts`. Most packages use `bundledConfig` which bundles to both CJS and ESM with type declarations.
- **Benchmarking**: Comprehensive performance benchmarks using Benchmark.js with CodSpeed integration. See `docs/benchmarking.md` for full documentation. Key commands:
  - `pnpm bench` - Run all benchmarks (outputs to `output.txt`)
  - `pnpm bench <pattern>` - Run benchmarks matching pattern
  - Benchmark locations:
    - End-to-end query performance: `packages/client/src/__tests__/benchmarks/query-performance/query-performance.bench.ts`
    - Query compilation/cache behavior: `packages/client/src/__tests__/benchmarks/query-performance/caching.bench.ts`
    - Query interpreter/data mapper: `packages/client-engine-runtime/bench/interpreter.bench.ts`
    - Query plan cache memory probe: `pnpm exec node --expose-gc --import tsx packages/client/src/__tests__/benchmarks/query-performance/query-plan-cache-memory.ts`
    - Local `ClientEngine` cache-hit timing probe with empty SQLite adapter: `pnpm exec node --expose-gc --import tsx packages/client/src/__tests__/benchmarks/query-performance/client-engine-cache-timing.ts`
    - Miniflare/workerd query compiler memory probe: `pnpm exec node --expose-gc --import tsx packages/client/src/__tests__/benchmarks/query-performance/workerd-query-compiler-memory.ts`
    - Client generation: `packages/client/src/__tests__/benchmarks/huge-schema/`, `packages/client/src/__tests__/benchmarks/lots-of-relations/`
  - Benchmarks run automatically on CI via `.github/workflows/benchmark.yml`; CodSpeed tracks performance over time and alerts on >100% regression.
  - The ongoing Prisma Client performance investigation journal is `wip/client-performance-journal.md`; update it after each accepted change, rejected experiment, measurement, and new lead so work survives context compactions and harness restarts.
  - Larger Prisma Client performance architecture leads live in `wip/client-performance-architecture-leads.md`. Use it for the JS-owned query/cache-hit design, Rust borrowing/arena direction, and the current "what not to retry" boundaries before starting broad worktrees.
  - For the ongoing performance work, ambitious architecture spikes are acceptable. Isolate risky or large experiments in side worktrees, use subagents for scouting/review/prototyping when that can reduce serial exploration time, and commit durable accepted/rejected outcomes back to the journal.
  - Internal runtime/query-plan formats are version-lockstep across producers and consumers. Do not preserve old/new dual-format compatibility for internal data structures unless the boundary is genuinely external. When a compact/internal format replaces an older shape, remove the old shape from types, interpreters, tests, docs, and benchmark fixtures instead of leaving compatibility branches on hot paths.
  - `client-engine-cache-timing.ts` supports `CLIENT_ENGINE_CACHE_TIMING_FILTER=<substring>` to run only matching rows and `CLIENT_ENGINE_CACHE_TIMING_ITERATIONS=<n>` to override row iterations. This is useful for CPU profiling one nested cached-plan row, e.g. `CLIENT_ENGINE_CACHE_TIMING_FILTER='direct plan after phase warmup' CLIENT_ENGINE_CACHE_TIMING_ITERATIONS=50000 node --cpu-prof --import tsx packages/client/src/__tests__/benchmarks/query-performance/client-engine-cache-timing.ts`.
  - `client-engine-cache-timing.ts` includes generated-client rows for public `PrismaClient` API warmed-cache calls. Use `errorFormat: 'minimal'` for these Node source rows when comparing to edge/Workerd behavior; default Node callsite capture can dominate simple generated-client timings and hide real proxy/runtime changes.
  - `client-engine-cache-timing.ts` also includes a non-unique `Post.findMany` blog-feed row. As of 2026-06-11, query-compiler raw-nested emission can carry relation-level no-cursor pagination ops, so this feed shape can use the product raw-nested path instead of the older compact-join `d/l/j` plan. A full Wasm/client reverse/reapply A/B first moved generated default / request-precomputed / descriptor-bound static / exact-helper from 21.97 / 20.35 / 16.89 / 16.58 us/op to 12.82 / 12.59 / 11.46 / 11.64, and direct plan from 13.70 to 9.20. The follow-up non-unique final-owner executor then moved close 100k generated default / request-precomputed / exact-helper from 12.90 / 12.64 / 11.30 us/op to 10.31 / 10.62 / 9.31, with 300k generated/direct confirmations at 10.17 / 6.95 us/op. Future feed work should target this raw-nested final-owner path, relation-op execution, or generated/static writer schedules; the old compact-join feed plan is no longer the product hot path for this shape.
  - `client-engine-cache-timing.ts` includes `cached request wrapper exact descriptor blog feed by author / nested rows` as the lower-bound comparator for generated exact descriptor helper feed-by-author work. A 2026-06-11 100k run measured generated exact-helper / cached-wrapper exact / cached-wrapper lazy at 9.74 / 8.80 / 10.90 us/op; use this row before changing exact-helper request plumbing.
  - `client-engine-cache-timing.ts` also includes benchmark-only trusted author-id-only rows: `generated client trusted descriptor helper blog feed by author / nested rows warmed cache` and `cached request wrapper trusted descriptor blog feed by author / nested rows`. A 2026-06-11 300k run measured generated full exact-helper / trusted helper at 9.10 / 8.36 us/op and cached-wrapper exact / trusted at 8.20 / 7.65. Treat this as a lower bound only; do not skip full shape validation for arbitrary user-owned args without a real generated-shape proof.
  - The generated blog exact-descriptor templates intentionally validate the hot static select tree with straight-line checks rather than generic selection-wrapper loops. A 2026-06-11 300k close A/B moved generated feed-by-author exact-helper 9.24 -> 9.16 us/op and cached-wrapper exact 8.26 -> 8.21, with solo confirmations at 9.16 / 8.06. Preserve full shape validation; the trusted author-id-only rows are still benchmark-only.
  - Do not replace the generated exact-helper fixed-width `Object.keys()` key-order validators with `for...in` / `Object.hasOwn()` scans without new evidence. A 2026-06-12 benchmark-only spike in `client-engine-cache-timing.ts` regressed the 300k by-author product-safe rows: generated exact-helper `8.98 -> 9.60 us/op`, hoisted exact `8.78 -> 9.35`, and cached-wrapper exact `8.12 -> 8.88`, even though trusted benchmark-only rows were roughly neutral.
  - Internal exact descriptor helpers support flat `findFirst` / `findFirstOrThrow` `where + select` specs for exact one-field scalar/enum equality, behind `internalExactDescriptorHelpers` and descriptor self-test only. A 2026-06-12 Node `findFirst users` benchmark row measured request-precomputed / hand exact / runtime exact at `2.25 / 2.07 / 2.08 us/op`, then `2.25 / 2.09 / 2.14` on repeat. The matching Workerd row repeated at host dispatch default / request-precomputed / hand exact / runtime exact `15.56 / 3.24 / 3.01 / 2.93 us/op`, with all rows hitting `20000/0` cache hits. JS/TS generator tests now have real serializer/parameterizer/cache-key oracle coverage for flat `User.findFirst:email:id,email,name` and `User.findFirstOrThrow:email:id,email`, and `applyModel.test.ts` covers storing the runtime exact `findFirst` matcher after the slow-path self-test. Keep this narrow; do not turn it into a broad filter matcher.
  - Do not replace the precomputed-hit spread in `tryExtractLazyDescriptor()` with an explicit `{ cacheKey, placeholderValues }` builder as a standalone exact-helper cleanup. A 2026-06-11 300k by-author A/B was neutral-to-negative on exact-helper (9.19 patched vs 9.16 control) and unchanged on request-precomputed.
  - Do not special-case zero-placeholder lazy descriptors to return the stored precomputed hit object as a standalone cleanup. A 2026-06-11 close A/B for the constant blog-feed row rejected it: patched 10.30 us/op vs reverted control 10.22, despite an older 10.76 baseline.
  - Do not add a one-root branch to the non-unique raw-nested final-owner executor without a new profile. A 2026-06-11 spike reused the unique-root assembly path for `rootResultSet.rows.length === 1`, but adjacent evidence was mixed: isolated phase-warmed direct improved 6.89 -> 6.68 us/op while exact-helper worsened 8.85 -> 9.00, and the full 100k reapply softened generated default. Keep the non-unique fallback/filter correctness fixes, but avoid duplicated one-row executor branches.
  - Do not change non-unique final-owner second-wave scheduling as a standalone cleanup. Replacing the `Promise.all` + `.then(resultSet => resultSet.rows)` shape with explicit wrapper/child-target branches helped request-precomputed and direct rows in isolation, but the 300k close control beat patched on exact-helper (8.99 vs 9.27 us/op) and generated default was neutral/slightly worse.
  - Do not pre-index non-unique final-owner relation rowsets with Maps as a standalone cleanup. A 2026-06-11 spike indexed unique children, wrapper lists, and child-list nested unique rows, passed focused runtime tests/build, but regressed the direct product feed row from 7.16 to 7.62 us/op over 300k iterations.
  - Do not replace the non-unique final-owner per-root relation assembly with a standalone single-pass root-slot writer. A 2026-06-11 prototype consumed wrapper/comment rows once without Maps and passed focused interpreter tests/build, but close direct by-author controls beat patched (`7.11 -> 7.16 us/op` in the full 300k table, then `6.93 -> 7.23` in a narrow 500k direct row). Keep `client-engine-cache-timing.ts`'s `direct plan blog feed by author / nested rows` row for isolating this path.
  - Do not replace `filterRawNestedRelationRows()`'s relation-op `Map` counter with a small-parent array counter as a standalone cleanup. A 2026-06-18 by-author 300k reverse A/B regressed direct / local / raw compact rows from control `7.43 / 7.66 / 7.45 us/op` to patched `7.56 / 7.87 / 7.49`. A useful relation-op follow-up needs a larger compiled filter/schedule change, not a local counter shape.
  - Do not coalesce non-unique final-owner root-key scope extraction for wrapper-list and child-list first-wave queries as a standalone cleanup. A 2026-06-18 300k reverse A/B had the reverted control beat or match patched on by-author direct / local / raw compact rows (`7.10 / 7.37 / 7.14` control vs `7.43 / 7.34 / 7.25` patched). Scope extraction reuse is too small by itself.
  - Do not compile `RawNestedFinalOwnerChildListRelation.filterRows` from relation ops as a standalone cleanup. A 2026-06-18 300k reverse A/B had control beat patched on direct/local by-author rows (`7.18 / 7.09` control vs `7.24 / 7.63` patched), with raw compact tied (`7.16` control vs `7.13` patched). Removing per-call op checks is too small; relation-op work needs a larger phase-owner design.
  - Do not add `QueryInterpreter.prepare()` plus a LocalExecutor prepared-plan/last-plan runner cache as a standalone cleanup. A 2026-06-18 by-author 300k A/B was noise-level: patched local/direct/interpreter-unit first measured `7.21 / 7.12 / 0.23 us/op`, but reapply was `7.45 / 7.30 / 0.26`, and the final reverted control was `7.48 / 7.23`; the direct row softened and local did not robustly move. Prepared interpreter execution needs a larger no-comment/no-transaction phase-owner proof.
  - Do not use the generic raw-result-set prototype as a non-unique feed-by-author lower bound. A 2026-06-18 temporary harness patch made `executeRawResultSetBlogPagePrototype()` return arrays for `rawNestedUnique: false`, but `raw result-set prototype blog feed by author` / exact prototype measured `8.35 / 8.23 us/op`, slower than same-session direct / compact-node rows at `7.50 / 7.45`. A useful non-unique writer/static-wave experiment needs a more specific phase-owner design.
  - For Cloudflare/workerd probes, instantiate query compiler Wasm through a static module import / Miniflare `CompiledWasm` module. Dynamic `new WebAssembly.Module(bytes)` inside the worker fails with `Wasm code generation disallowed by embedder`. When importing `packages/client/runtime/wasm-compiler-edge.mjs` in Miniflare, explicitly register the built `@prisma/client-runtime-utils` ESM under that bare module specifier. Keep manual compiler and generated-client rows in separate Miniflare instances because both instantiate the wasm-bindgen query compiler runtime. Workerd's worker-internal timer can be too coarse for warmed pure-JS request loops; use host `dispatchFetch` timing as an upper-bound signal. The Workerd blog-page generated-client adapter must project fixture result sets to the SQL-selected column order, mirroring the Node harness, otherwise alternate selected-column shapes can fail during data mapping even when the client/runtime path is correct.
  - `workerd-query-compiler-memory.ts` focused `blog-feed-by-author` runs include benchmark-only `hoisted action` generated-client rows. A 2026-06-11 20k focused run moved exact-helper host/worker-loop from 7.84/6.55 to 7.32/6.05 us/op; treat this as target-runtime lower-bound evidence for generated call-surface work, not product behavior. The first default-generated host row can be init/order skewed, so compare worker-loop and adjacent precomputed rows.
  - Edge `wasm-compiler-edge` extension-free model delegates intentionally use lazy plain-object properties instead of an inner model composite proxy. This removes one model-delegate proxy layer for Worker generated calls while leaving the Node `client` build and all extension-bearing delegates on the existing proxy path. A 2026-06-12 rebuilt 50k Workerd feed-by-author A/B moved worker-loop descriptor-bound / exact-helper / hoisted exact from `7.02 / 6.40 / 6.22 us/op` to `6.80 / 6.10 / 5.98`; the universal plain-delegate variant was not kept because Node `client` simple `findUnique` showed a small same-session regression.
  - Do not expect generated-code hoisting to transparently recover the benchmark-only `const findMany = client.post.findMany` lower bound for existing `client.model.action(args)` calls. Generated output only exports a class from `runtime.getPrismaClient(config)` and cannot rewrite arbitrary user call sites; recovering that gap would require a new generated-owned call surface such as static prepared operation helpers, not another applied-client proxy variant.
  - Do not add a queryless `ClientEngine.requestPreparedCachedResult(cacheKey, parameterizedQuery, placeholders)` method as a standalone prepared-operation cleanup. A 2026-06-18 300k Node A/B for blog-feed-by-author measured existing direct prepared / queryless prepared / request-surface prepared at `7.02 / 7.24 / 7.63 us/op`; removing only the static protocol-query argument and hit-object wrapper is neutral-to-negative. Generated-owned prepared work still needs to remove larger phases together.
  - The internal generated prepared operation for `template:Post.findMany:authorId:blogFeedByAuthorPostListV1` is intentionally shape-specific and installed through `config.preparedOperationRegistry` as `_preparedOperations.blogFeedByAuthorPostListV1_<index>`. It learns the precomputed hit once from a sentinel protocol query and then uses the request-handler cached-result surface with dynamic `authorId` placeholders. A 2026-06-18 close gate measured Node generated prepared / exact-helper / prepared request-surface at `7.81 / 8.61 / 7.66 us/op`; Workerd worker-loop default / exact-helper / generated prepared / prepared request-surface measured `10.40 / 8.20 / 7.20 / 6.55 us/op`. Keep guards for adapter, empty extensions, no global omit, no SQL commenters, no tracing/debug, and cached-result engine support; fallback to normal `findMany` otherwise.
  - The generated prepared by-author helper now uses the narrow `RequestHandler.requestPreparedReadPrecomputedCachedResult()` surface for guarded read-only hits instead of allocating a full generic `RequestParams` object. A 2026-06-18 Node reverse/reapply was small/noisy (`8.03` reverted control vs `7.85` and `7.82 us/op` patched), but Workerd generated prepared moved from the prior kept `7.20` row to `6.70 us/op` with adjacent exact-helper / request-surface at `8.10 / 6.50`. Keep this method narrow: read-only, no transaction/dataPath/unpacker, generated prepared helpers only.
  - Do not factor generated prepared-helper client property reads into local closure variables plus a precomputed static support guard as a standalone cleanup. A 2026-06-18 300k Node patch for the by-author helper regressed the generated prepared row from `7.81` to `8.37 us/op`. Future gap-closing work should remove object allocation or change the generated operation surface, not just reshape property reads.
  - Do not reuse/mutate a learned generated prepared hit's one-key `placeholderValues` object as a standalone cleanup. A 2026-06-18 benchmark-only row measured mutable-hit generated prepared at `8.33 us/op` versus the unchanged generated prepared row at `8.23 us/op` over 300k Node iterations, and the shape would be risky for concurrent prepared calls.
  - Do not omit the PrismaPromise operation spec from the generated prepared helper as a standalone cleanup. A 2026-06-18 benchmark-only row measured no-spec generated prepared at `9.33 us/op` versus the unchanged generated prepared row at `8.59 us/op`; the metadata is also part of the existing PrismaPromise transaction/batch semantics.
  - `client-engine-cache-timing.ts` has `raw result-set direct final-owner schedule blog feed by author / nested rows`, a benchmark-only lower-bound row for the non-unique by-author topology. A 2026-06-18 300k run measured the strict phase-owner schedule at `6.63 us/op` with `queryRaw=2100000`, versus adjacent direct plan / raw compact / raw exact compact / local executor rows at `7.07 / 6.96 / 7.06 / 7.33 us/op`. Use this as evidence for productizing a plan-specific non-unique final-owner schedule that owns wave scopes, relation-op filtering, row writing, and final attachment together; do not add old/new internal plan readers if the protocol shape changes.
  - Do not productize that schedule by only removing the non-unique final-owner prefiltered child-row array / child-target staging. A 2026-06-18 runtime patch that collected nested target IDs from raw child rows and assembled child records per root passed `query-interpreter.test.ts`, but regressed 300k Node direct / raw compact / local by-author rows to `7.49 / 7.11 / 7.53 us/op` versus accepted checkpoint controls at `7.07 / 6.96 / 7.33`. The remaining gap needs relation-specific row writing and attachment as a whole.
  - Do not precompute `paramOverrides` metadata inside `createModelAction()` as a standalone generated cache-hit cleanup. A 2026-06-12 spike cached `canIgnoreParamOverrides`, `dataPath`, and callsite-override state when the model action was created; focused tests/build passed, but the 50k Workerd feed-by-author priority rows regressed versus the edge plain-delegate baseline (`descriptor-bound` 6.80 -> 6.92 us/op, `exact-helper` 6.10 -> 6.32, `hoisted exact` 5.98 -> 6.12). Keep request-path changes tied to a larger action/request specialization.
  - Do not replace `applyModelsAndClientExtensions()` with a specialized extension-free edge applied-client proxy as a standalone generated call-surface cleanup. A 2026-06-12 spike avoided the generic composite-proxy layer map for `client.post`, passed focused tests/build, but close 50k Workerd feed-by-author control beat the patch on worker-loop default (`7.50 -> 7.30 us/op`), engine-precomputed (`7.22 -> 7.12`), descriptor-bound hoisted (`6.62 -> 6.52`), and exact-helper (`6.32 -> 6.26`). The remaining normal-vs-hoisted gap likely needs generated-code hoisting or a larger generated surface change.
  - In `workerd-query-compiler-memory.ts`, generated-client precomputed counters must wrap both `requestWithPrecomputedQueryPlanCacheHit()` and `requestPrecomputedCachedResult()`. Direct engine-precomputed rows use the latter on descriptor hits, so omitting it makes active engine fast-path runs print `precomputed fast path: hits 0`.
  - For query-compiler graph traversal micro-optimizations, require Criterion evidence in addition to allocation counts. Direct `petgraph` iteration and helper-level `SmallVec` experiments around `subgraph_contains_result()`, `is_direct_child()`, and `direct_child_pairs()` either had no allocation effect or regressed/failed to improve nested-create benches despite saving one or two allocations.
  - `query-compiler/src/translate.rs` intentionally precomputes result-node reachability once per translation and uses `QueryGraph::parent_nodes()` only for that cache build. This is different from the rejected direct `subgraph_contains_result()` traversal rewrite: the recursive helper remains unchanged, while `process_children()` gets O(1) reachability lookups. Engines commit `63ac52cb5bf` saved `translate_ir`/`full_compile` allocations on nested writes (`create-nested-connectOrCreate-mixed` 1645/2616 -> 1639/2610, `one2m` 1480/2316 -> 1473/2309, `update-set-nested` 1260/2029 -> 1241/2010) with read/filter/upsert controls allocation-neutral and narrow Criterion neutral-to-positive. Do not remove the cache without close allocation and Criterion evidence.
  - Synthetic write-graph helper read queries intentionally use `String::new()` names instead of debug-only labels. Engines commit `fd443cdf449` saved 2 `graph_build` / `full_compile` allocations on `create-nested-connectOrCreate-mixed`, `create-nested-connectOrCreate-one2m`, and `update-set-nested`; `query-m2o`, `filter-contains-param`, and `upsert` controls stayed allocation-neutral. Criterion was noisy across whole runs, but there was no localized target regression after close patched/control checks.
  - `QueryGraph::finalize()` intentionally reuses the reload candidates returned by `normalize_data_dependencies()` instead of recomputing `find_unsatisfied_dependencies()` inside `insert_reloads()`. Engines commit `202f7b09aaa` saved 3-5 `graph_build` / `full_compile` allocations on nested write rows (`create-nested-connectOrCreate-mixed` 797/2608 -> 792/2603, `one2m` 675/2307 -> 671/2303, `update-set-nested` 651/2008 -> 648/2005), with `query-m2o`, `filter-contains-param`, and `upsert` controls allocation-neutral and Criterion within noise. Do not reintroduce the second scan without close allocation and timing evidence.
  - `QueryGraph::ensure_return_nodes_have_parent_dependency()` and `find_unsatisfied_dependencies()` intentionally iterate projected dependency edges directly instead of calling sorted `outgoing_edges()` / `incoming_edges()` helpers. Engines commit `179cdcbbbf1` saved 3-7 `graph_build` / `full_compile` allocations on nested write rows (`create-nested-connectOrCreate-mixed` 792/2603 -> 785/2596, `one2m` 671/2303 -> 667/2299, `m2one` 441/1369 -> 436/1364, `update-set-nested` 648/2005 -> 645/2002), with read/filter/upsert controls allocation-neutral and close Criterion neutral-to-positive. Keep this narrow; do not generalize it to the previously rejected traversal helpers without fresh timing evidence.
  - `QueryGraph::normalize_if_nodes()` intentionally checks sibling `Then` / `Else` parentage with direct incoming petgraph iteration instead of allocating/sorting `incoming_edges(&sibling)`. Engines commit `a30ce85177f` saved 2-5 `graph_build` / `full_compile` allocations on if-heavy nested-write rows and close Criterion was neutral-to-positive (`create-nested-connectOrCreate-m2one` 174.05 -> 161.11 us, mixed 312.33 -> 308.16, `update-set-nested-prisma#27650` 206.69 -> 200.64). This supersedes the older 2026-06-07 rejection; a current create-row guard recheck no longer reproduced the old regression.
  - `FieldSelection::into_virtuals_last()` intentionally builds one final vector plus a virtual tail instead of `partition()` plus a third collected vector. Engines commit `6b97cbc7d5f` saved one `translate_ir` / `full_compile` allocation and about 2.2-2.3 KiB allocated on aggregate virtual rows (`aggregate-join`, `aggregate-join-lateral`, `aggregate-nested-m2m`), with scalar/read/nested controls allocation-neutral. Reverse Criterion improved `aggregate-join` about 1.8% and `aggregate-nested-m2m` about 2.7%; `aggregate-join-lateral` and `group-by` stayed within noise.
  - Aggregation result mapping intentionally streams the paired `selection_order` / `selectors` vectors directly instead of rebuilding an `IndexSet` and sorting flattened selector identifiers. Engines commit `37b0b015f9c` saved `translate_ir` / `full_compile` allocations on `aggregate` (`247/612 -> 242/607`), `aggregate-custom` (`367/678 -> 358/669`), and `group-by` (`212/614 -> 207/609`), with reapply Criterion neutral-to-positive (`aggregate-custom` 81.288 -> 77.501 us, `aggregate` 63.611 -> 62.646, `group-by` 61.026 -> 58.293).
  - Do not replace aggregation SQL `extract_columns()` with a linear `ScalarFieldRef` dedupe plus direct `field.as_column(ctx)` emission without a new CPU hypothesis. A 2026-06-12 spike saved allocations on `aggregate` (`translate_ir/full_compile` 242/607 -> 236/601) and `aggregate-custom` (358/669 -> 341/652), but close Criterion favored the reverted control on adjacent aggregate join/lateral/nested rows and matched or beat the patch on simple/group rows.
  - `query_graph_builder/extractors/utils.rs` intentionally separates boolean compound-selector checks (`is_compound_field()`) from field-vector materialization (`resolve_compound_field()`). Engines commit `f84cdb2c3c8` added a `query-compound-id` fixture and saved 3 `graph_build` / `compile_ir` / `full_compile` allocations on that row (`154/317/365 -> 151/314/362`) with focused Criterion neutral-to-positive. Keep boolean-only call sites on `is_compound_field()` so they do not allocate `Vec<ScalarFieldRef>` just to check compound-selector membership.
  - For query-compiler translation allocation work, removing expression scaffolding that `Expression::simplify()` would discard can be worthwhile only with timing proof: the accepted single-result-subgraph fast path in `fold_result_scopes()` saved four nested-create allocations and improved the focused rerun. In contrast, rewriting `process_children()` result-position splitting saved one to six allocations on nested writes but repeatedly regressed Criterion rows; both a reverse-index removal patch and a `None` / `One` / `Many` result-subgraph enum variant were rejected. Returning binding-only child `Let` nodes directly from `process_child_with_dependencies()` saved two allocations but also regressed nested-create Criterion.
  - Do not retry single-link `RelationLinkage` storage as a standalone query-compiler allocation cleanup without a new CPU hypothesis. A 2026-06-11 spike removed the hot-path `ConditionalLink` / one-element condition vector / `BTreeMap` shape and saved 3-6 allocations on focused read rows, but close Criterion was mixed and softened `query-many-m2m`, `query-m2o`, and `query-m2o-lateral`.
  - Do not replace `ColumnIterator`'s boxed iterator with inline `SmallVec` storage, a selected-column flag, or one-column relation constructors as a standalone SQL-query-builder cleanup. A 2026-06-12 spike saved `translate_ir` allocations on M2M rows (`query-m2m` 447 -> 429, `query-many-m2m` 421 -> 403, `create-m2m` 918 -> 892, `update-m2m-disconnect` 772 -> 751), but close Criterion regressed the focused rows (`query-many-m2m` 94.38 -> 100.99 us, `query-m2o` 84.43 -> 90.81 us, `update-m2m-disconnect` 152.49 -> 160.10 us) and was reverted.
  - Do not retry the 2026-06-08 expression-container sidecar shapes without a new CPU hypothesis: replacing `Cow<'static, str>` expression binding names with a typed `BindingName` enum saved nested-write allocation counts but regressed Criterion (`update-set-nested` +7.25%), and skipping discarded result `Let` nodes in `wrap_children_with_expr()` had tiny allocation wins with broad timing regressions.
  - Do not replace the nested `extract_filter()` empty-strip collect with in-place `Vec::retain()` without a new fixture that actually moves allocation counts. A 2026-06-12 patch passed `cargo check -p query-compiler` and `cargo test -p query-core filter --lib`, but allocation counts were unchanged for `filter-contains-param`, `filter-contains-param-insensitive`, `filter-startswith-param`, `filter-in-param-insensitive`, `filter-not-contains-param`, `filter-not-in-param-insensitive`, `group-by`, `query-m2o`, `create-nested-connectOrCreate-mixed`, and `update-set-nested`, so it was reverted before Criterion.
  - Do not add a no-relation early scan to `FieldSelection::into_without_relations()` without a new fixture. A 2026-06-12 patch passed `cargo check -p query-compiler` and `cargo test -p query-compiler --test queries`, but allocation counts were unchanged on sampled scalar/read/nested/aggregate rows, likely because Rust's consuming `Vec` filter path already reuses the source buffer in the no-relation case.
  - Do not replace `ScalarFilterParser`'s one-filter scratch `Vec<Filter>` returns with `SmallVec<[Filter; 1]>` as a standalone graph-build allocation cleanup without a new CPU profile. A 2026-06-11 spike saved two `graph_build` / `full_compile` allocations and about 0.6-0.7 KiB/op on filter-heavy rows, but had no effect on nested read/write allocation rows and softened focused Criterion rows such as `aggregate-join`, `filter-contains-param-insensitive`, and `filter-startswith-param`.
  - For nested cached-plan runtime work, compact `p` / legacy `process` nodes may reuse their `InMemoryOps` object only after a compile-time scan proves there is no pagination cursor anywhere in that ops tree. A 2026-06-11 compact-join feed A/B moved generated client 18.92 -> 18.66 us/op and direct plan 13.98 -> 13.60 over 300k iterations. Broader scan-only / WeakMap static-dynamic guards were previously rejected, so keep this narrow and do not skip cloning for cursor-bearing ops.
  - Raw nested direct relation tuples are now eight-slot internal values: `['r', fieldName, child, parentColumn, childColumn, scopeName, isRelationUnique, operations]`. Do not add seven-slot compatibility readers. The query compiler may emit non-empty `operations` only for supported no-cursor per-parent relation pagination; unsupported cursor/reverse/distinct/nested/linking-field ops must fall back before raw-nested emission. The TS runtime applies these ops per parent key. Final-owner child-list relations may carry this supported ops subset; unique direct relations, wrapper-list source/child relations, and unique nested child relations still require empty ops.
  - For `QueryInterpreter` query leaves, guarded one-rendered-query fast paths are viable: legacy `query` / `execute` and compact `q` / `x` can bypass the generic rendered-query loop when SQL comments and query instrumentation are inactive. This improved/kept healthy the nested-row direct/local/inner timing rows and the standalone interpreter bench.
  - For compact `QueryInterpreter` nodes, the per-interpreter compiled-node WeakMap is an accepted cached-plan win. It compiles hot compact tags (`v`, `s`, `g`, `l`, `e`, `q`, `x`, `u`, `m`, `j`, `d`, `p`) into closures, preserves the direct `d(q)` / `d(u(q))` data-map fast paths, and falls back to the old compact interpreter for unsupported tags.
  - `QueryPlanCache` keeps a last-hit entry for single and batch lookups to avoid repeated `Map.get()` calls on warmed same-shape cache hits. Preserve the eviction invalidation tests if touching this path, because stale last-hit entries can otherwise bypass the LRU maps after eviction.
- **Testing & databases**: `TESTING.md` covers Jest/Vitest usage. Most suites run as `pnpm --filter @prisma/<pkg> test <pattern>`. DB-backed tests expect `.db.env` and Docker services from `docker/docker-compose.yml` (`docker compose up -d`). Client functional tests sit in `packages/client/tests/functional`—run them via `pnpm --filter @prisma/client test:functional` (with typechecking) or `pnpm --filter @prisma/client test:functional:code` (code only); `helpers/functional-test/run-tests.ts` documents CLI flags to target providers, drivers, etc. Client e2e suites require a fresh `pnpm build` at repo root, then `pnpm --filter @prisma/client test:e2e --verbose --runInBand`. The legacy `pnpm --filter @prisma/client test` command primarily runs the older Jest unit tests plus `tsd` type checks. Migrate CLI suites live in `packages/migrate/src/__tests__`, the CLI runs both Jest (legacy suites) and Vitest (new subcommand coverage) via `pnpm --filter prisma test`, and end-to-end coverage lives in `packages/integration-tests`.

- **Client functional tests structure**:
  - Each test lives in its own folder under `packages/client/tests/functional/` (or `issues/` for regression tests).
  - Required files: `_matrix.ts` (test configurations), `test.ts` or `tests.ts` (test code), `prisma/_schema.ts` (schema template).
  - `_matrix.ts` defines provider/adapter combinations using `defineMatrix(() => [[{provider: Providers.POSTGRESQL}, ...]])`.
  - `prisma/_schema.ts` exports `testMatrix.setupSchema(({ provider }) => ...)` returning a Prisma schema string.
  - Test file uses `testMatrix.setupTestSuite(() => { test(...) }, { optOut: { from: [...], reason: '...' } })`.
  - Run specific adapter: `pnpm --filter @prisma/client test:functional:code --adapter js_pg <pattern>` (adapters: `js_pg`, `js_neon`, `js_libsql`, `js_planetscale`, `js_d1`, `js_better_sqlite3`, `js_mssql`, `js_mariadb`, `js_pg_cockroachdb`).
  - For error assertions, use `result.name === 'PrismaClientKnownRequestError'` and `result.code` (not `instanceof`).
  - Use `idForProvider(provider)` from `_utils/idForProvider` for portable ID field definitions.

- **Client performance spike notes**:
  - A 2026-06-08 `client-engine-cache-timing.ts` spike in `/home/aqrln.guest/prisma-js-owned-hit-spike` found that generated descriptor cache-hit prototypes need direct JS property access or generated shape guarantees. Generic/exact `Object.keys()` descriptor walks are too expensive for the hot path; a sentinel/direct-access blog-page descriptor measured about 1 us/op for extraction on Node, and a direct cached-plan descriptor matched the static-shape executable lower bound. The prototype is not production-safe as written because it does not prove absence of extra enumerable keys.
  - A 2026-06-08 Workerd spike in `/home/aqrln.guest/prisma-workerd-plan-descriptor-spike` confirmed the same direction on the target runtime: sentinel/direct descriptor extraction measured about 0.40 us/op worker-loop for the nested blog-page shape versus about 0.90 static exact-shape and 1.60 lazy interpreted descriptor extraction. A product-shaped sentinel path through `requestPrecomputedCachedResult()` measured about 7.40 us/op versus about 8.70 for engine-precomputed static protocol and about 13.00 for the default generated blog-page row. Treat this as benchmark evidence only; product code still needs exact-shape safety, fallback semantics, and batching-aware routing.

- **Docs & references**: `ARCHITECTURE.md` contains dependency graphs (requires GraphViz to regenerate), `docker/README.md` explains local DB setup, `docs/benchmarking.md` covers performance benchmarking, `examples/` provides sample apps, and `sandbox/` hosts debugging helpers like the DMMF explorer.

- **Client architecture (Prisma 7)**:
  - `ClientEngine` in `packages/client/src/runtime/core/engines/client/` orchestrates query execution using Wasm query compiler.
  - `ClientEngine` query plan cache defaults are build-target specific: Node.js `client` builds keep up to 1000 entries, while `wasm-compiler-edge` builds keep up to 100 entries by default to reduce retained memory. `queryPlanCacheMaxSize: 0` disables the cache.
  - `ClientEngine.request()` and `requestBatch()` use a synchronous connected-engine fast path before falling back to async `#ensureStarted()`; keep that path when editing hot request flow because it avoids an avoidable async hop on warmed clients.
  - Keep disabled debug logging out of `ClientEngine` warmed request hot paths. `@prisma/debug` records call arguments in a global history even when the namespace is not enabled, so unguarded per-request `debug(...)` calls can retain query plans and add microseconds of overhead.
  - Do not add a lazy `RequestParams` / callsite wrapper around `RequestHandler.requestPrecomputedCachedResult()` as a standalone descriptor-hit cleanup without a new product-visible hypothesis. A 2026-06-11 patch made successful descriptor hits build callsite/error params only on catch and passed focused unit tests, but close generated rows softened (`findMany users` exact helper 2.59 vs 2.39 us/op control; blog-feed exact helper 18.44 vs 18.15).
  - `applyModel()` intentionally wraps `modelActionsLayer(client, dmmfModelName)` in `cacheProperties()`. Do not remove this without generated-client A/B evidence: a same-session 2026-06-07 run moved generated-client `findUnique` from about 22.62 to 17.85 us/op and nested blog-page from about 52.71 to 47.46 us/op while preserving fresh per-call `PrismaPromise` behavior.
  - `applyFluent()` intentionally hoists relation keys/proxy own-key metadata and reuses the root empty data path; `modelActionsLayer()` intentionally reuses fluent-supplied callsites; `getCallSite('minimal')` and edge disabled-callsite paths intentionally return a singleton. A 2026-06-07 generated-client A/B moved `findUnique` about 17.65 -> 16.85 us/op and nested blog-page about 47.85 -> 46.74 us/op, with a Workerd high-iteration smoke at 17.49/39.84 us/op host upper bounds after rebuild.
  - `applyFluent()` also intentionally precomputes reusable non-`get` proxy traps instead of calling `defaultProxyHandlers(proxyOwnKeys)` for every returned PrismaPromise. A 2026-06-07 A/B moved simple generated-client `findUnique` about 15.73 -> 14.37/14.12 us/op; nested blog-page stayed noisy/mixed, so re-benchmark both rows if touching this shape.
  - Do not add a fluent proxy `get` fast path for `then` / `catch` / `finally` / `requestTransaction` / `spec` without new evidence. A 2026-06-07 spike was mixed: patched exact generated rows were 4.83 / 17.62 us/op, while adjacent reverted control was 4.94 / 17.43 us/op, so the added branch did not clearly improve product timing.
  - `createPrismaPromiseFactory()` intentionally returns a `PrismaPromiseImpl` class with prototype methods instead of an object literal with per-instance `then`/`catch`/`finally`/`requestTransaction` closures. A 2026-06-07 A/B moved generated-client `findUnique` about 14.10 -> 12.62/12.45 us/op and nested blog-page about 39.14 -> 36.75/36.61 us/op; Workerd generated-client smoke after rebuild was 15.24/32.67 us/op host upper bounds.
  - Do not inline the no-transaction branch inside `PrismaPromiseImpl.then()` or drop its `onFulfilled ?? undefined` / `onRejected ?? undefined` normalization without new evidence. Local spikes were noise or mixed: no-transaction inline measured about 5.88/22.10 us/op vs a 5.89/22.12 baseline, and direct handler passthrough measured about 5.94/21.93 with a simple-row regression.
  - `getPrismaClient()._request()` intentionally has an empty-extension fast path that still creates the operation span and Node `AsyncResource`, then calls `_executeRequest()` directly. This skips middleware-shaped params, query-extension dispatch, result-extension context resolution, and `applyAllResultExtensions()` when `this._extensions.isEmpty()`. A 2026-06-07 generated-client A/B moved `findUnique` about 12.38 -> 10.97 us/op and nested blog-page about 37.11 -> 35.62 us/op; Workerd smoke after rebuild was 14.91/32.77 us/op host upper bounds.
  - `DataLoader` intentionally schedules batch dispatch with `queueMicrotask()` instead of `process.nextTick()`. It still batches same-turn requests and splits requests separated by `await`, with coverage in `DataLoader.test.ts`. A 2026-06-07 generated-client A/B moved `findUnique` about 10.99 -> 10.57 us/op and nested blog-page about 35.57 -> 34.13 us/op; Workerd after rebuild moved generated-client host upper bounds to 12.31/32.34 us/op and worker-internal timings to 10.41/29.75 us/op.
  - `DataLoader` supports an optional cheap `canBatch` predicate so `RequestHandler` can defer expensive structural `getBatchId()` calls for transactions and `findUnique` / `findUniqueOrThrow` requests. Single batchable candidates still wait for the microtask boundary but go to `singleLoader` without computing the recursive sorted selection key; multiple same-turn candidates compute `batchBy` at dispatch and batch normally. A 2026-06-07 A/B moved generated-client `findUnique` about 9.10 -> 6.77 us/op and nested blog-page about 32.63 -> 24.18 us/op; Workerd worker-internal timings after rebuild were 6.85/21.90 us/op.
  - Do not change `isRequestHandlerPrecomputedCachedResultAction()` from its small action-array `.includes()` check to a `switch` as a standalone generated-cache-hit cleanup. A 2026-06-11 patch passed `applyModel.test.ts` and initially looked positive on the constant blog-feed exact-helper row (9.01 -> 8.82 us/op), but close reverted control matched it at 8.83 and beat or matched default, engine-precomputed, request-precomputed, and descriptor-bound rows; by-author feed rows were neutral/mixed.
  - Do not materialize extension-free model action functions as own properties on the model delegate target as a standalone generated-cache-hit cleanup. A 2026-06-11 patch preserved model-extension override semantics and passed focused `applyModel` / composite-proxy tests, but constant feed softened versus close control (`exact-helper` 8.83 -> 9.01 us/op, descriptor-bound 9.08 -> 9.20), and by-author exact/trusted rows stayed unchanged at 9.09 / 8.61. Generated call-surface overhead is still real, though: benchmark-only hoisted action rows that capture `client.user.findUnique`, `client.user.findMany`, or `client.post.findMany` once after connection moved feed-by-author exact/trusted 9.23/8.87 -> 8.92/8.39 us/op, `findMany users` default/request-precomputed 2.47/2.50 -> 2.25/2.29, and `findUnique` default/engine-precomputed 3.06/2.48 -> 2.77/2.15. Revisit only with an outer model delegate materialization or generated-code-level hoist proof that removes both model and action lookup without breaking extensions/fluent/transactions/overwrites.
  - Do not replace the outer applied-client proxy with edge-only lazy model properties as a standalone generated call-surface optimization. A 2026-06-11 patch returned the raw client with direct lazy model getters when `TARGET_BUILD_TYPE === 'wasm-compiler-edge'` and there were no client extension properties; it passed focused proxy/model tests and builds, but the 50k Workerd feed-by-author control beat patched on every worker-loop row, including default `7.30 -> 7.38 us/op`, request-precomputed `6.94 -> 7.02`, descriptor-bound `6.82 -> 6.96`, and exact-helper `6.18 -> 6.30`.
  - `createCompositeProxy()` supports opt-in `cachePropertiesOnTarget` for stable layer values. `modelsLayer()` and `modelActionsLayer()` intentionally use it without wrapping those layers in `cacheProperties()`, so warmed generated-client calls still create model delegates/actions lazily but skip repeated layer-map and `Cache.getOrCreate()` lookups. A 2026-06-11 close A/B moved `findUnique` default/request-precomputed/exact-helper from 3.11/3.14/3.00 to 2.93/3.03/2.89 us/op, `findMany users` default/engine-precomputed/exact-helper from 2.66/2.94/2.51 to 2.59/2.59/2.40, and feed-by-author request-precomputed/exact-helper from 10.84/9.23 to 10.70/9.21; the benchmark-only trusted feed lower bound regressed 8.78 -> 8.91, so keep watching feed rows if changing this path.
  - Do not collapse `cachePropertiesOnTarget` into the explicit-overwrite set as a standalone proxy fast path. A 2026-06-11 patch removed the separate `cachedTargetKeys` check and reused `overwrittenKeys`; it passed focused proxy/model tests and helped some `findMany users` rows, but close control beat it on targeted `findUnique` request-precomputed/exact-helper (`3.19/2.92 -> 3.09/2.90 us/op`) and feed-by-author exact-helper (`9.20 -> 9.07`).
  - Do not delete target-cached proxy keys from `keysToLayerMap` as a standalone `cachePropertiesOnTarget` fast path. A 2026-06-11 patch removed the cached-target set and deleted the layer-map entry after defining the target property; it passed focused proxy/model tests and helped targeted Node `findUnique`, but the adjacent Workerd control beat the patch on feed-by-author descriptor-bound/exact-helper worker-loop rows (`7.20/6.60 -> 7.05/6.55 us/op`), while simple Node rows were mixed.
  - `getPrismaClient()._executeRequest()` intentionally checks disabled debug logging through `isClientDebugEnabled()`, mirroring `ClientEngine`'s cheap global-debug guard before calling `Debug.enabled('prisma:client')`. A 2026-06-07 generated-client A/B moved `findUnique` about 10.36 -> 9.30 us/op and nested blog-page about 34.47 -> 33.55 us/op; Workerd after rebuild moved host upper bounds to 11.26/29.53 us/op and worker-internal timings to 9.39/26.85 us/op.
  - Do not inline `DynamicTracingHelper`'s disabled fallback with optional chaining against `getGlobalTracingHelper()` without new evidence. A local spike replacing `this.getTracingHelper().runInChildSpan(...)` with `getGlobalTracingHelper()?.runInChildSpan(...) ?? callback()` regressed generated-client rows to about 6.46/24.51 us/op.
  - Do not replace `DataLoader.dispatchBatches()` `.then(...).catch(...)` continuations with two-argument `.then(success, failure)` without new evidence. A post-build source run regressed generated-client rows to about 9.61/33.57 us/op, and same-session A/B was mixed (`findUnique` 9.42 -> 9.27 us/op, nested blog-page 32.68 -> 32.78 us/op).
  - `getBatchId()` intentionally uses a loop-based sorted-key builder instead of `sort().map().join()` to keep findUnique batching keys cheaper on generated-client hot paths. A DataLoader continuation/lookup cleanup tried at the same time was reverted because it weakened the product-path timing.
  - Do not replace `getBatchId()` recursive template literals with `+` concatenation without new evidence. A local A/B regressed generated-client rows from about 9.27/32.58 us/op to 9.39/33.15 us/op.
  - Do not add a `keysArray.length > 1` guard before `getBatchId()` sorting without new evidence. A local spike initially looked weakly positive, but post-build same-session source A/B regressed generated-client rows from about 9.45/33.47 us/op to 9.63/34.38 us/op.
  - `serializeJsonQuery()` intentionally creates nested `SerializeContext`s lazily for explicit `select` / `include` traversal. Scalar `true`/`false` selections should not allocate nested contexts or path arrays unless strict-undefined validation or relation/object recursion needs them. A 2026-06-07 A/B moved generated-client `findUnique` about 16.65 -> 15.58 us/op and nested blog-page about 45.79 -> 40.60 us/op, with Workerd smoke at 16.96/35.24 us/op host upper bounds after rebuild.
  - `serializeJsonQuery()` also fast-paths successful primitive argument values in objects and arrays (`null`, string, number, boolean, bigint) before creating nested argument contexts. Keep strict-undefined and invalid-value paths context-backed. A 2026-06-07 A/B was noisy on simple `findUnique` but moved nested blog-page about 41.26 -> 40.84 us/op, with Workerd smoke at 16.43/34.96 us/op host upper bounds after rebuild.
  - `serializeJsonQuery()` should not re-check `isSkip(value)` in `createExplicitSelection()` after the first `isSkip()` branch has already continued. A 2026-06-07 generated-client A/B moved `findUnique` about 9.54 -> 9.44 us/op and nested blog-page about 33.30 -> 32.73 us/op; Workerd stayed in the same band at 11.47/29.93 us/op host upper bounds.
  - `serializeJsonQuery()` selection traversal intentionally uses `Object.keys()` indexed loops instead of `Object.entries()` in include/omit/explicit-select paths. This preserves own enumerable string-key semantics while avoiding per-entry pair arrays. A post-lazy-batch-key 2026-06-07 Node A/B moved generated-client rows from about 7.01/24.75 us/op to 6.72/23.39 us/op; Workerd was neutral rather than clearly positive.
  - `isSkip(value)` intentionally compares by `value === skip` rather than `value instanceof Skip`. `Skip` construction is guarded by a module-private symbol and supported values come from the exported singleton / `skip.ifUndefined()`, so identity is the intended fast path. A 2026-06-07 generated-client A/B was modest but net-positive after reapply (`findUnique` about 6.47 -> 6.38 us/op, nested blog-page about 23.48 -> 23.15 us/op), and Workerd worker-internal timings were 6.57/19.85 us/op after rebuild.
  - `SerializeContext.isRawAction()` intentionally uses a switch instead of constructing an action array and calling `.includes()` on every implicit selection. A 2026-06-07 same-session A/B moved generated-client rows from about 5.94/22.31 us/op to 5.84/22.16 us/op; Workerd stayed in band at 6.69/19.80 us/op worker-internal.
  - Do not guard `computeEngineSideSelection(select, computedFields)` with `computedFields ? ... : select` in `createExplicitSelection()` without new evidence. A local A/B was neutral-to-negative on nested generated-client rows (baseline about 9.36/32.31 us/op, patched about 9.30/32.48 us/op).
  - `SerializeContext.findField()` intentionally uses a WeakMap-cached field-name table per runtime model and stores the resolved table on each context, instead of scanning `modelOrType.fields` or hitting the WeakMap for every lookup. Earlier field-map attempts were rejected before the nested serializer row existed, but 2026-06-07 retries with isolated serializer evidence moved nested serialization from 4.24 to 3.54-3.55 us/op with the map, then to 3.38 us/op with context-local field tables; Workerd stayed in band, with the context-local follow-up at 6.16 / 17.85 us/op on the confirming run.
  - Do not thread a `hasExtensions` boolean through `SerializeContext` just to skip `extensions.getAllComputedFields()` on empty-extension clients without new evidence. A 2026-06-07 same-session A/B was neutral-to-negative: patched generated-client rows were 6.13/22.33 us/op versus reverted 6.05/22.28 us/op.
  - Do not pass already-looked-up `RuntimeField` values into `SerializeContext.nestSelection()` without new evidence. A local split spike improved nested generated-client rows but made the simple generated `findUnique` row noisy/worse (`6.12` us/op field-reuse-only vs `5.94` baseline), while a narrower `isRawAction()` switch kept the broad signal cleaner.
  - `QueryPlanCache` interns repeated string values only for join/raw-nested-shaped plans and normally only for strings with length >= 128; compact raw-nested `@parent$...` scope strings are the one short-string exception. The interner is refcounted per cache entry so updates, evictions, and `clear()` release strings from old plans; retained entries store compact flat refcount arrays instead of `Map`s. A 2026-06-09 narrow scope-string patch moved blog-page node warm 4.54 -> 4.47 MiB and parameterized node warm 4.76 -> 4.69 MiB, with a close compile-current-miss blog-page row effectively neutral at 2421.43 vs 2417.24 us/op. Avoid broad all-plan string interning because scalar-selection plans mostly have unique SQL strings and can regress from bookkeeping overhead.
  - Do not split cached `templateSql.fragments` strings inside `QueryPlanCache` just to expose common SQL suffixes or selected-column chunks for interning. A local spike splitting long strings at `FROM` and then at comma boundaries passed cache tests but did not reduce retained V8 heap for blog-page plans and worsened larger 1,000-shape rows.
  - `client-engine-cache-timing.ts` has cached request wrapper rows using real `QueryPlanCache` + `LocalExecutor`, plus a scripted nested blog-page adapter that returns one post, author/category, two tags, and two comments/comment authors (seven SQL calls per request). After the disabled-debug guard, generic `ClientEngine` wrapper glue is a small part of warmed cache-hit cost; bigger remaining targets are cache-key/parameterization and cached-plan execution/rendering.
  - `client-engine-cache-timing.ts` repeats the full `ClientEngine` blog-page nested warmed-cache row after the phase-split rows. Prefer this late row when judging full-engine overhead: local runs showed the early warmed row was order/JIT sensitive (about 68-76 us/op early vs about 52-57 us/op late), while the late row stayed about 8 us/op above cached request wrapper rows.
  - `client-engine-cache-timing.ts` has `generated client serialize ...` rows that construct generated-style args and call `serializeJsonQuery()` without DataLoader or engine execution. A 2026-06-07 run measured about 0.32 us/op for simple `findUnique` and 4.17 us/op for the nested blog-page shape, versus full generated rows at 4.94 / 18.24 us/op. Nested serializer work is still a meaningful target; simple scalar serialization is not.
  - `client-engine-cache-timing.ts` also has `generated client serialize cache key ...` rows that time generated-style args through `serializeJsonQuery()`, `parameterizeQuery()`, native `JSON.stringify()`, and single-query cache-key construction. A 2026-06-07 run measured 1.71 us/op for `findUnique` and 7.19 us/op for the nested blog-page shape, while the adjacent full generated nested row was 18.58 us/op. This is the measured ceiling for replacing the generated-client front half with a static descriptor or fused serializer/parameterizer.
  - `client-engine-cache-timing.ts` has benchmark-only `generated client static descriptor extract ...` and `cached request wrapper static descriptor ...` rows for the nested blog-page generated args shape. A guarded exact-shape descriptor that validates `where/select`, static nested selections, `comments.take`, and `orderBy` measured about 1.16 us/op to extract `%1`; executing the cached wrapper with that descriptor measured about 8.02-8.06 us/op versus the static-shape lower bound around 7.05 us/op. Static descriptors are a plausible cache-hit architecture lead if they remain guarded and fall back to `serializeJsonQuery()` on any semantic uncertainty.
  - A 2026-06-08 side-worktree spike in `/home/aqrln.guest/prisma-generated-shape-probe-spike` (commit `6dbdc62fe`) added an optional generated-query-shape probe hook and manually injected a safe exact blog-page probe in `client-engine-cache-timing.ts`. It improved the generated engine-precomputed blog-page row from 11.52 to 11.24 us/op and the request-precomputed blog-page row from 12.94 to 12.01 us/op over 100k Node iterations, while preserving batched `findUnique` behavior (`queryRaw=100000`, `precomputedBatchHits=200000`). Treat this as a positive lead, not landed product design: the unresolved work is generator emission / shape-exactness for arbitrary user args, not the request/engine execution path.
  - Do not land the generated-query-shape probe hook as a per-model/action config map. A 2026-06-08 generator/runtime review found it unsafe for productization: generators do not know runtime callsite object shapes, and a per-action probe can be applied to the wrong learned descriptor. A viable version needs descriptor-bound exact matchers tied to the specific learned protocol query and placeholder mapping, with recursive own-key exactness checks and Worker-safe static code only; avoid `eval` / `new Function` and combinatorial generated probe emission.
  - Descriptor-bound exact matchers remain a positive generated-cache-hit lead, but only with descriptor identity as the safety boundary. A 2026-06-08 side-worktree spike in `/home/aqrln.guest/prisma-descriptor-bound-matcher-spike` (commit `90d81c46c`) stored a manually-specialized matcher on the learned lazy descriptor itself and invoked it only through `descriptor.exactMatcher`, improving the nested blog-page engine-precomputed row from about 11.54 to 10.71 us/op while preserving batched `findUnique` counters. Do not land the benchmark-only global matcher factory; the product problem is how to produce CSP-safe descriptor-bound exact matchers without `eval` / `new Function` and without large generated-client bundle growth.
  - Productizing descriptor-bound matchers should keep descriptors runtime-derived. Generation-time can at most provide optional CSP-safe helper factories that bind to a specific learned descriptor and self-test against the first slow-path args plus `hasSamePlaceholders`; generation-time cannot know ordinary callsite key sets, `skip`/`undefined` behavior, extensions, placeholder reuse, or nested selection shapes. A shippable proof needs oracle tests comparing descriptor extraction to `serializeJsonQuery()` + `parameterizeQuery()` + cache-key construction, including strict undefined, `Prisma.skip`, arrays, `Date`, `Decimal`, `BigInt`, bytes, Json null enums, `Param`, `FieldRef`, enum db names, duplicate placeholder equality, and batch cache-key/placeholder-renaming behavior. Keep raw queries, `createMany`, extensions, global omit, tracing/debug, SQL commenters, transactions, non-empty `dataPath`, and unpackers out of scope until the shortcut has explicit parity coverage.
  - `applyModel.ts` supports an internal descriptor-bound matcher registry on `GetPrismaClientConfig`, stored as `descriptor.exactMatcher` only after `buildLazyDescriptor()` learns a real cache hit and self-tests that the matcher returns the same placeholder values in the same key order. Keep matchers descriptor-bound, CSP-safe, and returning only `placeholderValues | undefined`; the descriptor still owns `cacheKey`, `protocolQuery`, `precomputedBatchId`, and MRU lifetime. Prisma commit `549e1a06d` measured Node request-precomputed vs descriptor-bound static matcher at 12.91 -> 11.42 us/op for the nested blog-page row over 100k iterations, and Workerd worker-loop at 13.40 -> 11.80 us/op over 5k blog-page iterations, with batched `findUnique` query counts unchanged. Remaining product work is generator emission of small helper factories plus oracle coverage for special values, duplicate placeholders, extensions/omit/raw exclusions, and batch placeholder ordering. Avoid `eval`, `new Function`, generic Wasm `Reflect` walkers, generic path/op tables, and per-action global probes that are not tied to a specific learned descriptor.
  - Do not assume descriptor-bound static matchers are automatically positive for alternating nested shapes. A 2026-06-09 current-main 50k Node refresh measured default generated rows at `findUnique` 4.23 us/op, batched `findUnique` 8.93, `findMany users` 3.03, blog-page 14.66, and alternating blog-page 14.50; descriptor-bound static matcher rows were 3.94, 9.02, 2.99, 14.24, and 21.29 respectively. Keep alternating-shape Node and Workerd gates in any generated-helper product proof.
  - Do not productize descriptor-bound matchers by installing a generic runtime-derived registry in generated clients. A 2026-06-08 generator spike added `createDescriptorBoundMatcherRegistry()` after generated config construction, first as a recursive plain-object descriptor matcher and then as a narrowed flat `where`/scalar `select` matcher. Node nested rows either regressed (recursive: blog-page 14.27 vs request-precomputed 11.92 us/op at 100k) or became noise-level (flat: `findUnique` 2.95 vs 2.99 us/op at 500k), and Workerd rejected the flat helper clearly: `findUnique` 9.98 vs 6.66 us/op and batched `findUnique` 18.28 vs 11.26 us/op. Keep this path reverted; future productization needs descriptor-specific straight-line helper factories that prove Worker wins.
  - Do not productize descriptor-bound matchers as generic/simple descriptor-derived helper factories. A 2026-06-08 benchmark spike bound flat `findUnique`/`findMany` matchers from learned descriptors, but focused Node repeats measured no win beyond the existing hand-written static matcher row: `findUnique` request-precomputed 3.34 us/op, static 3.21, simple-generated 3.22; `findMany` request-precomputed 2.61, static 2.49, simple-generated 2.65. The remaining viable path is exact generated helper code that looks like the static benchmark matchers, not a descriptor-interpreter with dynamic key arrays.
  - `client-engine-cache-timing.ts` includes benchmark-only `generated client exact descriptor helper ...` rows as of Prisma commit `dfd223e47`. They bind a tiny emitted-style helper to the learned descriptor for exact flat `User.findUnique({ where: { id }, select: { id,email,name } })` and `User.findMany({ take, select: { id,email,name } })` shapes, then use fixed key-order checks and straight-line placeholder extraction. A 2026-06-08 main run over 100k iterations measured request-precomputed vs static matcher vs exact helper at 3.78 / 3.66 / 3.54 us/op for findUnique, 9.56 / 8.45 / 8.71 for batched findUnique, and 2.95 / 2.86 / 2.86 for findMany. Treat this as a positive product lead versus request-precomputed, but the static-helper comparison is mixed/noisy; the next product step is generator-emitted allowlisted helper factories with fallback tests for key-order and shape mismatch, plus Workerd verification.
  - Do not land exact descriptor helper product code by exporting a hard-coded `User.id/email/name` helper from shared runtime. A 2026-06-08 productization spike proved generator/runtime wiring and measured positive flat `findUnique` / batched `findUnique` rows, but the implementation auto-enabled a benchmark-model-specific helper for any matching schema and did not add Workerd generated-client verification. The next product shape should emit schema-specific helper code only into qualifying generated clients, or use an explicit internal benchmark gate first.
  - `workerd-query-compiler-memory.ts` has benchmark-only exact descriptor helper rows for generated `findUnique`, `findMany users`, and batched `findUnique`. A 2026-06-08 Workerd 10k generated-user run measured host / worker-loop rows: `findUnique` request-precomputed 6.24 / 4.30 us/op, descriptor-bound static 6.07 / 4.20, exact helper 6.78 / 4.80; `findMany users` request 3.76 / 1.90, static 3.53 / 1.80, exact 3.52 / 1.80; batched `findUnique` request 10.15 / 8.30, static 10.07 / 8.20, exact 9.49 / 7.70 with `precomputedBatchHits=20000`. Treat exact helpers as mixed on Workerd: positive for batched `findUnique`, neutral for `findMany`, and not a single-call `findUnique` win in that run.
  - `client-engine-cache-timing.ts` and `workerd-query-compiler-memory.ts` include benchmark-only exact descriptor helper rows for two alternating generated blog-page shapes. The helper binds shape-specific straight-line matchers to the learned full-root and two-scalar-root descriptors rather than accepting either shape for either cache key. A 2026-06-09 Node repeat over 100k iterations measured default / request-precomputed / descriptor-bound static / exact-helper alternating blog-page at 13.16 / 12.24 / 11.69 / 11.35 us/op. A 2026-06-09 Workerd 5k run measured worker-loop request-precomputed / descriptor-bound static / exact-helper alternating blog-page at 12.60 / 13.00 / 12.20 us/op, with one expected compile miss for the second warmed shape. Keep alternating-shape Node and Workerd gates for nested exact-helper productization.
  - Do not reorder `applyModel.ts` lazy descriptor extraction to try all exact matchers before any generic lazy fallback without fresh evidence. A 2026-06-11 spike targeted alternating-shape overhead, but stable exact-helper blog-page regressed 8.71 -> 9.09 us/op and descriptor-bound static regressed 8.97 -> 9.59, while the reverted close control returned to 8.68 / 8.98 us/op for stable/alternating exact helper rows.
  - Do not special-case exact `findUnique` helpers to return a fixed `{ '%1': value }` object literal without fresh evidence. A 2026-06-08 benchmark micro-spike worsened the Node 100k generated `findUnique` row to 5.11 us/op versus same-run request-precomputed 3.78 and descriptor-bound static 3.96; it was reverted before Workerd because the Node signal was clearly negative.
  - Do not special-case nested blog-page exact descriptor helpers to bind only canonical `%1` and return `{ '%1': value }` without fresh evidence. A 2026-06-11 close A/B over 100k generated exact-helper blog-page iterations measured the patched benchmark mirror at 8.97 / 9.32 us/op for stable/alternating rows, while the dynamic-placeholder close control was 8.43 / 9.37; the generator and benchmark changes were reverted.
  - Generator-emitted exact descriptor helpers are a viable internal-gated product shape, but not ready as a default feature. A 2026-06-08 side-worktree spike in `/home/aqrln.guest/prisma-generated-exact-descriptor-product-spike` (commit `d13d1f200`) emitted schema-specific matcher code into generated JS/TS clients only when `generator.config.internalExactDescriptorHelpers` supplies explicit specs such as `Account.findUnique:id:id,email`; it avoided shared runtime benchmark exports, `eval`, and `new Function`, and helpers still bind only to learned descriptors after the existing `applyModel.ts` self-test. Node 50k rows measured request-precomputed -> exact helper at 4.05 -> 3.88 us/op for `findUnique`, 9.70 -> 9.02 for batched `findUnique`, and 3.21 -> 2.95 for `findMany`; a Workerd 5k smoke measured host dispatch at 10.04 -> 9.57, 14.08 -> 13.40, and 7.15 -> 5.61 respectively. Land only as an internal benchmark/allowlist gate after oracle coverage for special values, duplicate placeholders, `Prisma.skip`/strict undefined, extensions/global omit exclusions, placeholder order, and generated-output typechecking.
  - `buildExactDescriptorMatcherRegistry.test.ts` in both generator packages now has real serializer/parameterizer/cache-key oracle coverage for the generated `template:Post.findUnique:<field>:blogPagePostV1` helper. Keep these tests when broadening nested exact helpers: they prove full/minimal blog-page shape parity, placeholder key order, stable cache keys across id/slug values, root key order rejection, extra root key rejection, wrong id/slug type rejection, `undefined` / `Prisma.skip` rejection, wrong nested `comments.take` rejection, full/minimal descriptor separation, and `String @unique` root support through `Post.slug`. The generator-output tests also emit both id and slug blog-page template binders and assert the string placeholder checks appear in generated JS/TS.
  - Do not replace the kept `createExactDescriptorMatcherRegistry(specs)` internal gate with generated generic helper code that still loops over emitted key arrays without a stronger product-path signal. A 2026-06-09 prototype emitted schema-specific functions into generated JS/TS clients and passed focused generator tests plus generator package builds, but the clean extractor microbench was mixed/noisy: emitted helper 0.127 / 0.153 / 0.130 us/op versus runtime factory 0.133 / 0.131 / 0.156 across close rounds, and the broad generated-client timing run was too contaminated to justify a large generated-code diff. Reverted; future generated helper work should either produce truly straight-line/static matchers closer to the benchmark-only hand helper or prove Worker/product wins directly.
  - `createExactDescriptorMatcherRegistry()` is the first kept internal-gated product slice for exact descriptor helpers, not a default feature. JS/TS generators emit it only when `generator.config.internalExactDescriptorHelpers` supplies explicit specs like `User.findUnique:id:id,email,name` or `User.findMany:take:id,email,name`; the parser accepts unique scalar `findUnique` fields behind explicit value types, flat numeric `findMany:take`, and scalar/enum select lists. It stays CSP-safe, descriptor-bound, and self-tested by the existing `applyModel.ts` descriptor matcher flow. A 2026-06-09 Node 100k focused run measured generated `findUnique` default / request-precomputed / hand exact / runtime exact at 3.72 / 3.61 / 3.55 / 3.49 us/op, batched `findUnique` at 8.31 / 8.59 / 8.08 / 8.11 us/op with `precomputedBatchHits=200000`, and later `findMany users` at 2.80 / 2.79 / 3.00 / 2.71 us/op. A 2026-06-09 Workerd 20k repeat importing the real runtime helper from `wasm-compiler-edge.mjs` measured worker-loop request-precomputed / runtime exact at 5.35 / 4.60 us/op for `findUnique` and 9.90 / 8.85 for batched `findUnique`, with `precomputedBatchHits=40000`; two focused `findMany` repeats measured 1.80 / 1.75 and 1.95 / 1.85 us/op. Keep Workerd repeats in the gate. Oracle coverage in `applyModel.test.ts` / `createExactDescriptorMatcherRegistry.test.ts` proves exact matcher storage after slow-path self-test, exact-miss lazy fallback, exact+lazy miss slow-path fallback, string scalar support, `findMany:take` placeholder/constant binding, `undefined` / `Prisma.skip` shape rejection, duplicate equal-placeholder non-storage, and ambiguous BigInt placeholder ownership rejection. Keep it internal until broader parity covers extensions/global omit exclusions, generated-output typechecking, and more special scalar values.
  - Do not add a generic `preparedQueryPlanMatcherRegistry` hook to `applyModel()` for normal `client.model.action(args)` delegates without a new product-shape argument. A 2026-06-12 prototype installed a prepared matcher before lazy descriptor extraction for the `Post.findMany` blog-feed-by-author shape, passed `applyModel.test.ts` and `@prisma/client` build, but the focused 300k Node row stayed noise-level: prepared matcher `9.35 / 9.21 us/op` normal/hoisted versus exact helper `9.45 / 9.17` and trusted helper `9.23 / 8.73`. The cleaner prepared-operation direction is a generated-owned helper/surface that owns the static protocol query, parameterized query, cache key, and exact validator; normal delegate interception adds runtime surface without recovering the prepared lower bound.
  - `createExactDescriptorMatcherRegistry()` now supports multiple same-model/action/clientMethod specs and must keep scanning when one candidate spec does not bind the learned descriptor. It also accepts unique scalar `BigInt` and `Boolean` `findUnique` fields behind the same internal gate. `BigInt` is special: generated args contain raw JS `bigint`, while the precomputed placeholder value is the serialized string, so generic lazy descriptor parity can fail; only store the descriptor when the exact matcher self-test reproduces the string placeholder map.
  - `createExactDescriptorMatcherRegistry()` also accepts `DateTime @unique` fields for Date-object args only. It binds through the descriptor self-test by matching the learned `Date` arg to the serialized ISO placeholder value and rejects string DateTime args / invalid Dates so normal serializer validation still runs. Keep this narrow unless adding full oracle parity for string DateTime values.
  - `createExactDescriptorMatcherRegistry()` accepts `Decimal @unique` fields for `DecimalJsLike` args only. It binds by matching the learned decimal's `toFixed()` value to the serialized placeholder and rejects plain number/string decimal inputs, so normal serializer validation and Decimal normalization still run for those shapes. Keep this narrow unless adding full oracle parity for alternate Decimal input forms.
  - `createExactDescriptorMatcherRegistry()` accepts `Bytes @unique` fields for `Uint8Array`/Buffer args only. It binds by matching the learned `ArrayBuffer` view to the serializer's base64 placeholder value and rejects base64 strings/arrays, so normal validation still owns those shapes. The generated blog-page template helper remains restricted to `Int`/`String` roots because it validates primitive placeholder descriptors.
  - `createExactDescriptorMatcherRegistry()` accepts `Json @unique` fields for primitive string/number/boolean args and plain structural object/array args that produce one reusable Json placeholder. `client-engine-runtime` parameterizes primitive values on exact Json scalar-mask edges as `{ type: "Json" }` placeholders with `JSON.stringify(value)`, while Json string operators such as `string_contains` stay on `String` placeholders. The helper still rejects `null`, `$type` tagged objects, `toJSON`, sparse arrays, inherited enumerable keys, non-finite numbers, `ArrayBuffer` views, null-enum objects, `undefined`, and `Prisma.skip` so the normal serializer/parameterizer keeps owning validation-sensitive cases.
  - `createExactDescriptorMatcherRegistry()` accepts enum `findUnique` fields with generated `enumValues` maps from schema names to database names. It must return mapped database values in placeholder order, reject unknown enum strings and object/tagged enum-like values, and keep enum handling explicit rather than treating enum fields as plain strings.
  - `applyModel.ts` learned lazy descriptors store numeric placeholder value types as `int32` or `float`, not raw `typeof value === "number"`. `createExactDescriptorMatcherRegistry()` treats `number` specs as signed 32-bit `Int` values, including `findMany:take` and generated blog-page `Int` roots. It must reject fractional, non-finite, and out-of-range values in both exact matchers and lazy fallback extraction so helpers do not reuse an `Int` query plan for values the parameterizer would encode differently. Do not map `Float @unique` to this path without a distinct semantic value type and cache-key/placeholder-type oracle coverage.
  - `createExactDescriptorMatcherRegistry()` accepts `Float @unique` fields through a distinct `float` value type for finite non-integer numbers only. Integer Float inputs can parameterize as `Int` placeholders, so they must not hit the `float` exact helper; they rely on the generic `int32` lazy descriptor or normal slow path.
  - Do not replace `createExactDescriptorMatcherRegistry()`'s generic `hasOwnEnumerableKeysInOrder(value, expectedKeys)` calls with fixed-arity key helpers just to avoid tiny expected-key array allocations without new evidence. A 2026-06-09 close A/B on runtime exact descriptor helper rows regressed all focused Node rows: patched `findUnique` / batched `findUnique` / `findMany users` measured 4.13 / 7.60 / 2.66 us/op, while the reverted control measured 3.45 / 7.14 / 2.61.
  - Do not broaden `createExactDescriptorMatcherRegistry()` to a generic recursive nested exact-args tree without a new CPU hypothesis. A 2026-06-09 prototype for a descriptor-bound `findUniqueExactArgs` spec covering the blog-page nested shape passed runtime tests but regressed the generated stable blog-page row badly: runtime exact nested measured 23.93 us/op and alternating nested 13.99, versus same-session generated/request-precomputed/static-matcher blog-page rows around 12.65 / 12.22 / 11.30 us/op. Nested exact helpers need straight-line generated matcher code or a different execution architecture, not a recursive spec interpreter.
  - Do not productize nested exact descriptor helpers by teaching `createExactDescriptorMatcherRegistry()` a nested-selection DSL. Keep that runtime factory flat-only. The viable internal-gated nested shape is a narrow generated-template path such as `template:Post.findUnique:id:blogPagePostV1`, where generated client code binds a hand-authored straight-line matcher to a learned descriptor after the existing `applyModel.ts` self-test, validates descriptor constants/placeholders, and falls back to the flat registry or slow path on any uncertainty.
  - `packages/client-generator-{js,ts}/src/utils/buildExactDescriptorMatcherRegistry.ts` now emits that narrow internal generated-template path for `template:Post.findUnique:id:blogPagePostV1`, while flat specs still fall back through `createExactDescriptorMatcherRegistry()`. The 2026-06-09 gate passed generated-code execution tests plus JS/TS generator builds, and benchmark-only rows stayed in the expected band: Node 50k stable blog-page default / request-precomputed / descriptor-bound static / exact-helper at 12.54 / 12.32 / 11.33 / 11.30 us/op, alternating at 13.51 / 12.90 / 12.41 / 11.99, and Workerd 5k worker-loop stable request/static/exact at 12.20 / 12.20 / 11.80 with alternating 12.60 / 12.60 / 12.00. This remains an explicit internal helper template, not a default feature or a generic nested matcher.
  - `packages/client-generator-{js,ts}/src/utils/buildExactDescriptorMatcherRegistry.ts` also emits `template:Post.findMany:take:blogFeedPostListV1` for the constant `take: 10` blog-feed shape. `take` is not parameterized today, so the helper must reject different `take` values instead of pretending they share a cache key. Corrected 2026-06-11 Node rows measured default / request-precomputed / descriptor-bound static / exact-helper at 24.07 / 22.93 / 21.66 / 19.90 us/op over 100k and 22.24 / 24.02 / 19.09 / 19.26 over 300k. Workerd 10k coverage for the same shape measured host-dispatch default / engine-precomputed / request-precomputed / descriptor-bound static / exact-helper at 22.18 / 17.79 / 17.76 / 17.77 / 17.95 us/op, with all precomputed rows hitting 10000/10000. Productize further only with descriptor-bound self-test and oracle coverage; do not route it through a generic nested matcher.
  - `packages/client-generator-{js,ts}/src/utils/buildExactDescriptorMatcherRegistry.ts` also emits `template:Post.findMany:authorId:blogFeedByAuthorPostListV1` for the strict blog-feed-by-author shape. It binds only learned descriptors with exact root keys `where,take,orderBy,select`, exact `where: { authorId }`, an `int32` `authorId` placeholder, constant `take: 10`, fixed `createdAt desc` order, and the full blog-page nested select. Node 300k rows measured generated default / request-precomputed / descriptor-bound static / exact-helper at 21.09 / 20.78 / 19.48 / 19.28 us/op. A focused 20k Workerd repeat measured worker-loop default / request-precomputed / descriptor-bound static / exact-helper at 18.75 / 15.85 / 16.55 / 14.85 us/op. Avoid first-call benchmark values where `authorId` equals constant `take`, because the value-keyed lazy descriptor intentionally collapses duplicate placeholder values and will not store the exact matcher on that warmup call.
  - `client-engine-cache-timing.ts` and `workerd-query-compiler-memory.ts` include benchmark-only prepared exact operation rows for the strict blog-feed-by-author shape. They model a generated-owned call surface where structural identity is known by construction: no public nested args object construction, no descriptor walk, one `int32` `authorId` validation, then a static protocol query/cache key and dynamic placeholder map. The direct row calls `requestPrecomputedCachedResult()` on the engine; the request-surface row creates a fresh `PrismaPromise` and routes through `RequestHandler.requestPrecomputedCachedResult()`. Node 300k measured direct prepared / request-surface prepared / exact-helper / trusted hoisted / cached-wrapper trusted at 7.53 / 7.87 / 9.22 / 8.34 / 7.83 us/op, with direct/local lower bounds around 7.19-7.20. Workerd 20k measured worker-loop direct prepared / request-surface prepared / exact-helper / hoisted exact at 5.05 / 5.40 / 6.35 / 5.95 us/op, with host dispatch 6.19 / 6.56 / 7.56 / 7.16. Treat this as a strong architecture lead for generated-owned prepared surfaces, not product code; productization still needs generated helper factories, descriptor/self-test safety or a separate exact API contract, batching-aware routing, and no generic Wasm `Reflect` walker.
  - `PrecomputedQueryPlanCacheHit` intentionally always carries `parameterizedQuery`; do not make it optional or add old-hit-format compatibility. `ClientEngine.requestPrecomputedCachedResult()` cache misses compile from that stored parameterized query and execute with the current placeholder values, avoiding stale warmup-query values for future generated/internal prepared operations. A 2026-06-12 safety gate kept Node prepared direct / request-surface rows in band at about `7.62-7.68 / 7.79-7.81 us/op`, proved the no-cache miss path over 100 iterations, and measured Workerd request-surface prepared at `5.68 us/op` over 50k worker-loop iterations.
  - Do not special-case exact descriptor hit extraction in `applyModel.ts` to return `{ cacheKey, placeholderValues }` when `parameterizedQuery` / `queryInfoQuery` are absent. A 2026-06-12 patch initially moved the by-author exact-helper row from 9.22 to 9.01 us/op, but the close reverted control matched or beat it on the broader exact-helper surface: blog page 8.03 patched vs 7.73 control, blog feed 9.14 vs 8.74, by-author 9.06 vs 9.02, hoisted by-author 9.02 vs 8.93, with simple rows mixed. Keep the spread shape unless a new monomorphic-object hypothesis has stronger evidence.
  - Do not make `take` / `skip` placeholders only in the JS parameterizer. DMMF currently marks root/relation `take` and `skip` as non-parameterizable, and the Wasm query compiler rejects manual `Param<Int>` values for root `take`, nested `comments.take`, shared root+nested placeholders, and separate root/nested placeholders with `Placeholder<Int> is not a valid Int`. Arbitrary pagination placeholders need an engines-side design covering `Take`, SQL limit/offset lowering, in-memory pagination, `take == 0`, one-row uniqueness, and duplicate placeholder ownership.
  - `applyModel.ts` keeps a tiny MRU cache of learned lazy descriptors per model action, capped at two descriptors. This fixes alternating hot generated shapes without making descriptor retention unbounded. A 2026-06-08 side-worktree run in `/home/aqrln.guest/prisma-descriptor-cache-spike` moved `generated client blog page / 2 alternating nested row shapes warmed cache` from 31.77 to 12.36 us/op over 100k Node iterations, while the stable one-shape row stayed roughly flat (11.97 -> 12.22 us/op) and batched `findUnique` preserved one query per pair (`queryRaw=100000`, `precomputedBatchHits=200000`). If changing descriptor storage, keep the cap and rerun stable, alternating, and batched controls.
  - `client-engine-cache-timing.ts` has benchmark-only `generated client lazy descriptor extract ...` and `cached request wrapper lazy descriptor ...` rows that build a generic interpreted descriptor from the first slow-path generated args + placeholder map. A 2026-06-07 run measured lazy extraction at 0.32 us/op for simple `findUnique` and 2.40 us/op for nested blog-page; lazy wrapper nested was 9.42-9.46 us/op versus specialized static descriptor around 8.14 us/op. This supports lazy descriptors as a product-shape lead, but the hot path likely needs specialized/compiled matchers rather than a generic descriptor interpreter for nested shapes.
  - For raw-nested execution, strict final-owner writer paths are viable when they target the current compiler-emitted direct relation shape rather than adding old/new internal formats. A 2026-06-10 change added a unique-root direct raw-nested final-owner fast path for the blog-page-shaped plan (two unique children, one wrapper-list child, one list child with a unique nested child), supporting scalar metadata and `_count` paths. Serial A/B moved `direct plan after phase warmup blog page / nested rows` 7.37 -> 5.73 us/op, generated exact descriptor helper 11.21 -> 10.89, and full generated blog-page 12.96 -> 11.87; main confirmation measured 5.65 / 10.52 / 11.87. The static-wave writer lower bound is still around 3.8-4.0 us/op, so continue removing concrete per-query overhead from this current shape. The pre-existing TS-only raw-nested `m` relation reader was removed on 2026-06-10 after verifying engines produce direct raw-nested relation tuples only; do not reintroduce alternate raw-nested relation formats for compatibility.
  - Do not retry final-owner row-writer branch/path micro-specializations without a new CPU profile. A 2026-06-11 direct-prefix/two-level metadata path specialization covered root-shaped `_count` mappings and passed focused interpreter tests, but the 300k direct blog-page row stayed flat at 4.76 us/op versus same-session controls at 4.76-4.78. The gap to the benchmark-only 4.08/3.81 writer lower bounds needs a larger static ownership/schedule change, not just shaving the generic row-writer branch.
  - Raw-nested scope-value dedupe intentionally stays array-first for small fanouts and promotes to `Set` only after `RAW_NESTED_INDEX_THRESHOLD` unique values. A 2026-06-11 cleanup moved scope-value extraction plus final-owner child-target IDs away from unconditional `Set` allocation and measured direct compiled blog-page 5.78 us/op, exact compact-node 5.35 us/op, generated blog-page 11.77 us/op, and a properly warmed Workerd 20k generated blog-page smoke at 11.76 us/op host / 10.60 us/op worker loop. Do not reintroduce unconditional `Set` allocation on small raw-nested scopes without fanout-specific timing evidence.
  - Do not unroll direct-field final-owner row writers as a standalone raw-nested optimization. A 2026-06-11 patch returned straight-line closures for direct string-field mapping lengths 0-4 and passed focused interpreter tests, but close timing was neutral-to-negative: patched direct / exact compact / generated nested rows were 5.85 / 5.68 / 12.94 us/op, while reverted controls were 5.77 / 5.77 / 12.18. The next final-owner proof needs to remove a larger phase such as generic rendering/scope construction or relation wave work.
  - Do not add a standalone `renderQueryWithSingleBinding()` style helper for final-owner relation queries. A 2026-06-11 patch avoided per-relation scope-object construction but routed local-scope queries through a separate single-binding evaluator; focused tests passed, but patched direct / exact compact / generated nested rows were 5.89 / 5.88 / 12.64 us/op versus close controls at 5.77 / 5.77 / 12.18. A useful render/scope proof needs a larger compiled query-leaf shape that removes scope objects and generic render/evaluate machinery together.
  - Do not generalize the final-owner compiled query-leaf renderer into a standalone renderer for ordinary compact `q` nodes without a larger ownership change. A 2026-06-11 patch compiled strict compact template SQL renderers for no-comments/no-instrumentation `q` leaves and passed interpreter tests, but close blog-feed product rows softened: direct plan 16.36 us/op patched vs 15.57 control, generated feed 21.76 vs 21.37.
  - Final-owner raw-nested relation queries intentionally use strict compiled compact-template renderers for supported local-scope query leaves. They are not generic render helpers: they compile only compact template SQL with direct placeholders/constants and `T` tuple fragments, reject unsupported shapes, and fall back to `#executeRawNestedReadDbQuery()` when needed. A 2026-06-11 patch moved Node direct / exact compact / generated nested rows from close controls around 5.77 / 5.77 / 12.18 us/op to 4.45 / 4.39 / 9.96, with a higher-iteration repeat at 4.34 direct and 9.53 generated. A properly warmed Workerd 20k smoke measured default generated blog-page at 11.50 us/op host / 10.40 worker loop, request-precomputed at 10.15 / 9.10, and engine-precomputed at 7.63 / 6.45. Preserve the strict guards and tuple coverage if touching this path.
  - Do not return adapter-owned `Date` objects directly from the final-owner row writer as a standalone allocation cleanup. A 2026-06-11 patch bypassed `new Date(value)` only inside `tryCompileRawNestedFinalOwnerRowWriter()` and passed focused interpreter tests, but close timing was neutral/noisy: patched/control generated blog-page 10.22/10.18 us/op, exact helper 8.96/8.93, compact node 5.23/5.22, exact compact 5.04/5.09, and direct phase-warm 5.14/5.21. The mutability/isolation risk is not worth it without a larger final-owner ownership contract.
  - Do not change final-owner compiled query renderers to return a single `SqlQuery` instead of a one-element array as a standalone cleanup. A 2026-06-11 patch removed that short array allocation and passed focused interpreter tests/build, but close timing was not product-positive: patched direct / exact compact / generated nested rows were 4.31 / 4.25 / 9.80 us/op, close reverted controls were 4.36 / 4.32 / 9.54, and a reapplied generated sample was 9.93. The direct/exact effect was noise-level while generated softened.
  - Do not productize a serializable raw-nested writer-program compact node that still executes generic phase closures and slot arrays. A 2026-06-09 `N` node spike wrote final owner objects for a strict unique-root numeric blog-page payload and passed focused interpreter tests/build, but measured only 5.92-6.03 us/op versus compact `n` at 6.49-6.97 and static-wave writer at 3.93-4.06, with higher heap churn. The next writer proof needs generated/static final-owner wave code, not a generic phase interpreter wrapped in a new plan tag.
  - Do not add a raw-nested direct leaf relation shortcut that maps child rows directly into parent records while leaving the rest of compact `n` execution intact. A 2026-06-09 prototype for direct leaf `r` relations with numeric refs and direct no-conversion mappings passed interpreter tests, but close A/B moved compact node only 6.57 -> 6.11 us/op and exact compact only 6.68 -> 6.30, below the 15% gate for more raw-nested local shortcuts; generated blog-page smoke did not show a stronger product-path signal.
  - `ClientEngine.request()` has an internal `precomputedQueryPlanCacheHit` single-request hook carried by `RequestHandler`; it only uses the payload on an existing single-plan cache hit and otherwise falls back to the normal `parameterizeQuery()` / stringify path. Generated precomputed descriptors can also carry the structural `precomputedBatchId` from `getBatchId()` so repeated descriptor hits avoid walking the protocol query again during DataLoader dispatch. `RequestHandler` forwards precomputed payload arrays to `requestBatch()` only when every batched request has one; `ClientEngine.requestBatch()` then tries to build a batch from the stored parameterized single-query trees and otherwise falls back to current `parameterizeBatch()`. This batch-aware path preserves DataLoader batching but mechanically renames placeholders per query and does not preserve cross-query equal-value placeholder reuse, so do not productize beyond guarded internal measurements without cache-key/placeholder-shape review. `getPrismaClient._executeRequest()` also has an internal `protocolQuery` override that is only honored when there is no `argsMapper`, no extensions, and no global omit. A 2026-06-07 Node run of `client-engine-cache-timing.ts` measured `internal request precomputed lazy descriptor ...` at 4.21 us/op for `findUnique` and 16.10 us/op for nested blog-page; adding dynamic prebuilt protocol query measured 3.15 us/op and 12.23 us/op, while reusing a static protocol query measured about 3.1-3.3 us/op and 11.8-11.9 us/op. Adjacent generated-client rows were 5.47 and 18.59 us/op. Direct surface splits measured `request handler precomputed static protocol lazy descriptor ...` at 2.04/10.59 us/op, `client engine precomputed static protocol lazy descriptor ...` at 1.47/10.05 us/op, and `prisma promise engine precomputed static protocol lazy descriptor ...` at 1.70/10.34 us/op. This proves `ClientEngine.request()` and PrismaPromise are not the remaining nested bottleneck once precomputed data is available. The internal generated `requestPrecomputedFastPath` now calls `RequestHandler.request()` directly on descriptor hits to skip `_request()` wrapper overhead; this is a benchmark/productization lead only, because a real product path must preserve or explicitly replace `_request()` tracing, AsyncResource, debug logging, and middleware/extension guards.
  - Query-compiler Wasm request compilation is still string-based: the TS `QueryCompiler` interface exposes `compile(request: string)` / `compileBatch(batchRequest: string)`, implemented by Rust `QueryCompiler::compile(request: String)` / `compile_batch(request: String)` in `query-compiler/query-compiler-wasm/src/compiler.rs`. The request path is `ClientEngine` parameterization on the JS heap -> `JSON.stringify` parameterized query/request -> `RequestBody::try_from_str()` -> `JsonBody` -> `JsonProtocolAdapter::convert_single()` -> `Operation` -> `query_compiler::compile()`. `tsify::serde_wasm_bindgen` is used for constructor params and result/error serialization, not for request parsing today. A future `compileFromValue(JsValue)` entrypoint would remove JS-side stringify but would still materialize `JsonBody` / `serde_json::Value` unless it also replaces `JsonProtocolAdapter` with a JS-value-backed adapter. For cache-hit reads, the current direct cached-result path is already the right first principle: avoid entering Wasm at all.
  - `JsonProtocolAdapter::convert_selection()` intentionally collects `<field>: false` exclusion keys during the main selection walk instead of pre-scanning the `SelectionSet`. Removal of selected fields still happens after `$scalars` / `$composites` and nested selections are processed, preserving wildcard-plus-exclusion semantics. Engines commit `e761557fa45` had zero allocation-count effect but close Criterion was neutral-to-positive, with the clearest wins on `create-nested-connectOrCreate-mixed`, `nested-pagination-query`, and `nested-upsert-nested-only`.
  - Do not treat handle-only cached execution for exact generated `findMany` descriptor hits as a sufficient JS-owned query/cache-hit proof. A 2026-06-09 side-worktree spike added a `PrecomputedCachedResultHandle` path so hot hits passed `{ cacheKey, modelName, action, placeholderValues }` instead of `protocolQuery` into `ClientEngine`, with fallback to the current query-taking path on cache miss. It passed `RequestHandler.test.ts` and `pnpm --filter @prisma/client... build`, but close 100k Node A/B was noise-level: generated request-precomputed `findMany` 2.64 -> 2.62 us/op and default generated `findMany` 2.69 -> 2.64. The current direct cached-result path already avoids Wasm and uses the cache's last-hit slot; the next JS-owned proof must remove larger phases such as descriptor/protocol construction, structural identity, or plan/result transfer together.
  - Generated-client `requestPrecomputedFastPath` defaults on as of Prisma commit `14aad0a37`, but only for plain non-transactional adapter-backed calls with ignorable param overrides, no extensions/global omit, disabled tracing, and disabled Prisma client debug logging. The path still enters `RequestHandler.request()`, so DataLoader batching is preserved; do not default batchable `findUnique` calls onto the direct engine-precomputed path unless batching semantics are explicitly replaced. A 2026-06-08 A/B over 50k local iterations moved normal generated rows from 7.02 -> 4.25 us/op (`findUnique`), 13.41 -> 8.55 (`batched findUnique`), 4.64 -> 4.14 (`findMany users`), and 19.86 -> 14.33 (nested blog-page), while keeping batched `queryRaw` at 50000.
  - `RequestHandler.requestPrecomputedCachedResult()` is the productized direct cached-result surface for non-batchable generated read descriptor hits. As of Prisma commit `c30ba026b`, `applyModel.ts` uses it only for learned `findMany` / `findFirst` / `findFirstOrThrow` descriptors under the same plain-request observability guards, and it still maps errors through RequestHandler before returning. A 2026-06-08 A/B moved normal generated `findMany users` from 4.20 -> 2.99 us/op over 50k Node iterations; Workerd generated `findMany users` measured around 2.10 us/op worker loop, close to explicit request-precomputed at 2.00. Keep `findUnique` on the request-precomputed/DataLoader path.
  - `RequestHandler.singleLoader` can use direct precomputed cached results for singleton batchable requests only after DataLoader has already waited for the microtask boundary and decided the request is not part of a batch. Keep the guard narrow: require `requestPrecomputedCachedResult`, an existing `precomputedQueryPlanCacheHit`, no transaction, empty `dataPath`, and no `unpacker`; multiple same-turn `findUnique` calls must still go through `requestBatch()`. Do not route the already-productized `requestPrecomputedCachedResult()` method through a shared helper with extra guards in the `findMany` hot path: a 2026-06-08 spike regressed generated `findMany users` by about 5%, while the refined singleton-only path moved adjacent Node rows from 4.51/4.40 -> 4.15 us/op for `findUnique` and 13.96/13.89 -> 13.38 for nested blog-page, with batched `queryRaw` unchanged at 50000.
  - `workerd-query-compiler-memory.ts` has `client-static-descriptor-extract ...` and `client-lazy-descriptor-extract ...` rows that run descriptor validation/extraction inside the worker isolate over generated-style args. 2026-06-07 reduced-count local runs measured worker-internal extraction around 0.14-0.16 us/op static findUnique, 0.86 us/op static nested blog-page, 0.22 us/op lazy findUnique, and 1.48-1.56 us/op lazy nested blog-page. The same file also has generated-client-Miniflare-only precomputed request rows; local 20k runs measured worker-internal `client-internal-precomputed-static-protocol ...` around 4.35 us/op findUnique and 12.35-12.85 us/op nested blog-page, `client-request-handler-precomputed-static-protocol ...` around 3.40-4.20 and 10.90-11.50, `client-engine-precomputed-static-protocol ...` around 1.55-1.65 and 8.25-8.50, and `client-prisma-promise-engine-precomputed-static-protocol ...` at 1.65 and 8.60. This supports the Node conclusion on the target runtime: descriptor extraction and precomputed engine hits are promising, while `_request`/RequestHandler plumbing and batching semantics are the hard product surface.
  - Do not compile lazy descriptors into nested matcher closures as a standalone optimization without new evidence. A benchmark-only closure-compiled descriptor was slightly worse than the interpreted lazy descriptor (`2.42` vs `2.39` us/op extraction; `9.79` vs `9.42-9.46` us/op nested wrapper), so ordinary closure nesting does not recover the specialized matcher gap.
  - `client-engine-cache-timing.ts` has `generated client promise construction ...` rows that call generated model methods without awaiting the returned PrismaPromise. A 2026-06-07 run measured only about 0.27 us/op for `findUnique` and 0.49 us/op for the nested blog-page shape, so proxy/PrismaPromise construction is not the main generated-client gap; look after promise construction unless fresh profiles say otherwise.
  - `client-engine-cache-timing.ts` has `cached request wrapper precomputed key ...` rows that precompute parameterized queries, placeholder values, and cache keys, then time only cache lookup, `LocalExecutor.execute()`, and response wrapping. A 2026-06-07 nested blog-page run measured 7.27 us/op versus direct/local executor around 7.0 us/op and full cached wrapper around 11.3 us/op, so the warmed-wrapper gap is almost entirely parameterization/stringify/cache-key construction rather than cache lookup or response wrapping.
  - `client-engine-cache-timing.ts` has `cached request wrapper static shape ...` rows that compile/cache one structural query shape, then time cache lookup, dynamic `%1` placeholder-scope creation, `LocalExecutor.execute()`, and response wrapping without `parameterizeQuery()` or `JSON.stringify()` in the loop. A 2026-06-07 nested blog-page run measured 7.16-7.24 us/op versus current cached wrapper around 11.12 us/op, showing dynamic placeholder scope construction is cheap once structural identity is known.
  - Do not add a narrow connected/non-debug/no-sql-commenter single-query cache-hit fast path to `ClientEngine.request()` without new evidence. A local spike bypassed the generic request path on cache hits, but late full-engine nested rows stayed around 52 us/op and did not shrink the gap to cached request wrapper rows.
  - Do not add a standalone local cached-result shortcut by threading `LocalExecutor.executeCachedResult()` / `QueryInterpreter.runWithoutSqlCommenter()` through `ClientEngine.requestPrecomputedCachedResult()` without a stronger product-path design. A 2026-06-08 side-worktree spike only moved the direct cached-result blog-page row from 10.39 to 10.19 us/op and the generated engine-precomputed blog-page row from 11.79 to 11.78 us/op. Extending the same spike to store a cached plan handle on `PrecomputedQueryPlanCacheHit` measured 10.33 us/op direct and 11.74 us/op generated, still noise-level for added API/cache-semantics complexity.
  - Do not cache `ClientEngine`'s `hasSqlCommenters` check as a private constructor field without stronger evidence. A local spike built cleanly, but same-command late warmed nested rows were slightly faster with the legacy per-request config check (17.46 us/op legacy vs 17.61 us/op cached field over 100k iterations).
  - `client-engine-cache-timing.ts` also has render-only, data-map-only, and nested root-data-map phase rows for cached plans. Current rows show `renderQuery()` for the first DB query and scalar data mapping are small relative to nested cached-plan execution. The nested root `dataMap` split is sensitive to JIT/order effects: repeated "direct plan after phase warmup" rows dropped from roughly 57-58 us/op to roughly 45 us/op, close to manual inner+outer, so do not treat root `dataMap` pushdown as proven without warmup-controlled measurements.
  - Exact-shape cache-key phase rows in `client-engine-cache-timing.ts` show `parameterizeQuery()` dominates cache-key cost; `JSON.stringify()` plus key concatenation is comparatively small on current Node/V8 measurements.
  - Do not switch single-query cache keys to the full compile request string without new evidence. A local `request as cache key` benchmark row was slower on simple shapes, retained larger keys, and was only noise-level on the nested blog-page shape.
  - Non-compacted `multi` batch query plans can also populate the single-query plan cache when the individual plans fit within the configured cache size. This lets later matching single queries reuse plans compiled as part of a mixed batch. Compacted batches are intentionally not decomposed.
  - `QueryPlanCache.getSingle()` / `getBatch()` have a last-hit fast path for repeated same-shape cache hits; they still call the LRU touch helper, and evictions clear matching last-hit pointers. `QueryPlanCache` also performs cache-local, refcounted sharing for retained compact join plans: repeated strings are interned, repeated child `q`/`x` database-query templates under joins are canonicalized across cached entries, and repeated nested result structures under `dataMap` nodes are shared while root result structures remain distinct. Root query templates are intentionally not canonicalized so distinct root SQL strings do not bloat the interner with one-off large keys.
  - `QueryPlanCache` only interns strings with length >= 128 and stores retained per-entry interner counts in compact flat arrays. This keeps large repeated SQL fragments shared while avoiding bookkeeping for medium/short strings. A 2026-06-08 cache-memory A/B showed disabling all interning regressed 1,000 retained blog-page plans from about 4.88 MiB to 15.76 MiB, lowering the threshold to 8 regressed to about 5.23 MiB, and raising it to 1024 regressed to about 5.36 MiB. The accepted flat-count + 128-byte cutoff variant measured about 4.55 MiB for 1,000 retained blog-page plans and about 4.75 MiB for parameterized blog-page plans.
  - Do not add an unconditional second-sighting admission policy for large join/raw-nested single plans without a better hot-repeat design. A 2026-06-08 spike retained only probation keys for one-off blog-page churn (node warm heap about 800 KiB instead of 4.54 MiB), but it changed warm-cache semantics: one warmup request no longer cached the nested plan, timed warmed rows still compiled once, and `blog page nested rows / warmed cache` regressed from about 12.93 to 14.31 us/op in the filtered timing run.
  - `packages/client/src/__tests__/benchmarks/query-performance/workerd-query-compiler-memory.ts` has raw compile-retention scenarios, `mode=client-cache` scenarios that manually parameterize benchmark value churn, and `mode=client-execute` scenarios that instantiate a generated-like edge Prisma Client with a fake SQLite adapter to execute warmed cache-hit requests in Miniflare/workerd.
  - In `client-engine-cache-timing.ts`, the fake blog-page adapter's `getBlogPageResultSet()` caches resolved `SqlResultSet`s by rendered SQL string and falls back to parsing the table name from the SQLite `FROM main.<table>` fragment; keep that shape when editing the benchmark. The older repeated `sql.includes(...)` classifier added noticeable benchmark-only overhead (about 1.86 us/op adapter-only vs about 0.79 us/op with the SQL-result cache), so treat adapter classifier samples as harness overhead rather than product runtime.
  - Two executor implementations: `LocalExecutor` (driver adapters, direct DB) and `RemoteExecutor` (Accelerate/Data Proxy).
  - `LocalExecutor.execute()` keeps the warmed non-transaction path free of its own `async` frame and reuses stable interpreter transaction-manager option objects. If editing this path, preserve the split transaction helper unless new benchmarks show it is no longer useful.
  - `QueryInterpreter` class in `packages/client-engine-runtime/src/interpreter/query-interpreter.ts` executes query plans against `SqlQueryable` (driver adapter interface).
  - `QueryInterpreter` compiles hot compact tuple nodes once per plan object into closures cached in a per-instance `WeakMap`. This avoids redispatching through `#interpretCompactNode` on cached-plan hot paths while keeping fallback behavior for less common tags (`c`, `+`, `R`, `r`, `t`, `V`, `?`, `0`, `-`, `i`, `M`).
  - `QueryInterpreter.run()` precomputes per-run SQL-commenter and query-instrumentation booleans in the runtime context; avoid rechecking those flags inside each query node on cached-plan hot paths.
  - `QueryInterpreter.run()` uses outer non-raw driver-error mapping, while raw SQL query/execute paths keep local `rethrowAsUserFacingRawError` handling so `$queryRaw`/`$executeRaw` still surface P2010. The common non-raw single-query `dataMap(query)` path skips the result-set merge loop when SQL commenters and query instrumentation are disabled.
  - `RequestHandler.request()` intentionally avoids an `async` success-path frame and `singleLoader` maps engine responses with a promise continuation instead of `await`. Keep synchronous-throw conversion to rejected promises if editing this path. A 2026-06-07 same-session generated-client A/B moved `findUnique` about 11.88 -> 11.13 us/op and nested blog-page about 36.51 -> 35.73 us/op; Workerd generated-client smoke after rebuild stayed essentially flat at 15.30/32.99 us/op host upper bounds.
  - Do not add a root `dataPath.length === 0` / already-deserialized fast path in `RequestHandler.mapQueryEngineResult()` or `unpack()` without new evidence. A local spike removed `Object.values()`, skipped `deepGet()` for empty paths, and then bypassed `unpack()` for local JS results, but generated-client rows regressed to roughly 12.20/38.75 us/op versus a same-session 12.06/37.19 baseline.
  - `QueryInterpreter` join nodes avoid `Promise.all(...map(async ...))` when there is exactly one child join expression. The blog-page cached plan has join child counts `[4, 1, 1]`, so this mainly helps nested joins with actual child rows; empty-row ClientEngine timing rows can stay noisy.
  - Do not collapse `QueryInterpreter` `let`/`l` scope handling to a single reused nested context just to avoid repeated object spreads; that benchmarked neutral-to-soft on the current cached-plan probes.
  - Query flow: `PrismaClient` → `ClientEngine.request()` → query compiler → `executor.execute()` → `QueryInterpreter.run()` → driver adapter.
  - Local `ClientEngine` execution opts `QueryInterpreter` into native JS result values and marks non-raw responses so `RequestHandler` skips JSON-protocol deserialization; `RemoteExecutor` and `packages/query-plan-executor` keep JSON-protocol-shaped values and must not skip normalization/deserialization.
  - `packages/query-plan-executor/src/server/schemas.ts` intentionally validates `QueryRequestBody.plan` as `z.unknown()`, not `z.record(...)`. Compact query plans are internal tuple/array values, and the executor does not maintain a Zod schema for the runtime plan shape. Do not reintroduce object-record validation or old object-plan compatibility at this boundary.
  - `QueryInterpreter` precomputes its max SQL bind/chunk size once from `ConnectionInfo.maxBindValues` or provider defaults and passes the field to `renderQuery()` from query leaves. Keep the adapter override precedence if editing this path.
  - Query-plan result field nodes inside object `fields` may omit `type: 'field'` and may omit `dbName` when it is identical to the result field key; `packages/client-engine-runtime/src/interpreter/data-mapper.ts` recognizes field nodes by `fieldType` and defaults missing `dbName` to that key. Explicit `dbName` is still required when SQL column names differ from the result field key.
  - `data-mapper.ts` caches result-set field mappings by result fields and column shape, including scalar type name and list arity, so row mapping does not rediscover that metadata per cell. Keep the cached mapping shape guarded by interpreter and product-path timing benchmarks.
  - Do not add a separate field-only result-set mapper path or return existing `Date` objects directly from JS-mode datetime mapping without new benchmark evidence; both were mixed/noisy on nested blog-page rows and did not clearly improve local executor timings.
  - Do not cache `attachChildrenToParents()` join key metadata in a WeakMap without new evidence; it regressed the dedicated join/deep-nested interpreter microbench and did not clearly improve nested blog-page product rows.
  - Do not replace multi-child join `Promise.all(joinExpressions.map(async ...))` with a helper that first builds child promises and then result objects via explicit loops. A local spike passed tests/build and kept interpreter benches in range, but product-shaped nested rows were not positive.
  - Do not split compact `j` execution into direct `attachChildToParents()` calls just to avoid child wrapper objects without new evidence. A local spike slightly improved some inner/local rows but stayed neutral-to-negative on warmed `ClientEngine` and direct nested rows after rebuild, so it was reverted.
  - Do not cache compact mapped-join / nested-single-child-join matcher results in WeakMaps without new evidence. A local spike improved some product-shaped cached-plan rows, but regressed interpreter `join (1:N)`, `sequence`, and `deep nested join` throughput badly enough to revert.
  - Do not rewrite compact mapped-join / nested-single-child-join matchers into direct private execution helpers just to avoid matcher result allocations. A local spike passed focused tests/build, but regressed cached request wrapper nested blog rows to roughly 49-54 us/op versus the current roughly 42 us/op band.
  - Do not skip `cloneObject()` / `evaluateProcessingParameters()` for cursor-bearing or WeakMap-classified `InMemoryOps` without fresh product-row evidence. The accepted shape is a per-compiled-node no-cursor scan; older broad static-looking guards regressed full nested blog rows and the deep nested interpreter bench.
  - `processManyRecords()` intentionally skips linked pagination only when it is provably a no-op for the entire result set: no cursor, no positive skip, `take` present, and `take >= records.length`. A 2026-06-11 close A/B on the current compact-join blog-feed shape moved direct plan 15.55 -> 13.67 us/op and generated client 20.84 -> 18.81 us/op over 300k iterations. Do not widen this to missing `take`, cursors, or positive skips without fresh product-row evidence.
  - Current `client-engine-cache-timing.ts` nested blog-page `findUnique` inner plans have no compact `p` process nodes. A benchmark-only attempt to strip process nodes failed with zero nodes found, so do not chase `processRecords()` for that benchmark shape without first revalidating the compiled plan.
  - Do not route generic `query` / compact `q` interpreter nodes through `#executeQuery()` just to reuse the single-query fast path. A local source run was neutral, but after rebuilding `@prisma/client-engine-runtime` it regressed product-shaped nested blog rows to roughly 65 us/op cached wrapper and 61 us/op local executor.
  - Do not inline a no-comment/no-instrumentation single-query fast path inside compact `q` nodes without new evidence. A local spike avoided the generic result-set merge loop for nested child queries, but warmed `ClientEngine` nested rows did not improve and cached request wrapper nested rows regressed to roughly 61-62 us/op.
  - Do not add a compact `u(q)` fast path that calls `#executeQuery()` directly without new evidence. A local spike improved direct nested-plan rows to roughly 37 us/op, but regressed cached request wrapper nested rows to roughly 46.5-46.7 us/op versus the current roughly 42-43 us/op band.
  - Do not specialize `serializeSql()` with computed-key object literals for 0-5 column result rows without new evidence. A local spike worsened direct warmup nested rows to roughly 50 us/op and did not clearly improve cached wrapper/local executor product rows.
  - `serializeSql()` does have accepted unrolled assignment paths for 0-5 columns and exactly 11 columns. This is intentionally not the rejected computed-key object-literal shape; it hoists column names once per result set and assigns into plain row objects. The current `client-engine-cache-timing.ts` / `workerd-query-compiler-memory.ts` fixtures use result-set widths 0, 2, 3, 5, and 11, so verify a new hot width exists before adding more exact serializer cases. Keep `serialize-sql.test.ts`, `data-mapper.test.ts`, `interpreter.bench.ts`, and `client-engine-cache-timing.ts` coverage if editing it.
  - Do not add a separate `renderSingleQuery()` helper just to avoid `renderQuery()`'s one-element `SqlQuery[]` allocation on the no-comments/no-instrumentation fast path. A local A/B over cached request wrapper rows was neutral on cheap rows and slower on nested blog rows; the existing flat template rendering cache plus single-query interpreter fast path is better on current V8.
  - The benchmark blog-page query currently compiles to one outer `dataMap` over a `let`/`join` tree. Nested `q` nodes serialize SQL result sets to row objects first, and the outer data map then remaps the joined object graph into the final response shape. Future nested-row wins likely need data-map pushdown / plan-shape changes rather than tiny `attachChildrenToParents()` key tweaks.
  - `client-engine-cache-timing.ts` includes nested blog-page phase rows for adapter-only `queryRaw` over the seven benchmark result sets, `serializeSql()` over those same result sets, `renderQuery()` over all seven query leaves with representative scopes, an inner-plan variant where compact `q` leaves are replaced with precomputed fresh row objects, per-root-child-branch rows, a variant where the root compact `j` join's immediate children are precomputed, and a variant where the root compact `j` join is replaced with a precomputed fresh joined object. After the compact nested-branch/root mapped-join fast paths and exact-width `serializeSql()` paths, current local rows put all-leaf `renderQuery()` around 1.1 us/op, adapter-only seven result sets around 2.1 us/op, `serializeSql()` over those result sets around 1.75 us/op, precomputed query leaves around 7.6 us/op, precomputed root join children around 4.7 us/op, and precomputed join leaves around 4.6 us/op. Per-child branch rows put simple author/category branches around 4.5 us/op and deeper tags/comments branches around 5.9-6.0 us/op.
  - After the latest public-API cleanup, a 2026-06-07 direct-plan refresh put local executor/direct nested blog-page rows around 7.2-7.5 us/op, raw compact node around 6.8 us/op, exact prototype around 5.9 us/op, and raw prototype around 5.3 us/op. A 2026-06-08 raw-nested fanout specialization compiles relation arrays into closures and moved focused compact raw rows from roughly 6.40 / 6.75 us/op to roughly 6.0-6.2 / 6.0-6.1 us/op. The remaining direct/local gap is spread across `mapRawNestedRows`, rendering/evaluation, relation scope/attachment, and plan shape. The next meaningful runtime slice should remove multiple generic phases through an exact-shape/raw-result-set executor or compiler plan-shape change.
  - Do not compile raw nested DB leaves into executor closures just to hoist `isRawSqlQuery()` or the shared `#executeRawNestedReadDbQuery()` dispatch without new evidence. A 2026-06-08 spike passed focused tests but measured compact/exact compact/direct blog rows at 6.04 / 6.00 / 6.53 us/op, neutral versus the accepted fanout patch.
  - Do not add a generic descriptor-interpreted flat edge-slot raw plan without compiling the slot schedule or emitting more direct assembler code. A 2026-06-08 benchmark-only spike modeled the blog-page raw plan as seven slots plus attachment descriptors, but measured about 6.7 us/op versus about 6.0 us/op for the current compact raw node and about 5.2 us/op for the hand-written raw result-set prototype.
  - Do not add a generic raw nested flat schedule that compiles nodes/levels/edges but still allocates per-run result arrays, scopes, records, and loops over generic edge descriptors. A 2026-06-08 side-worktree spike measured about 7.0 us/op for blog-page raw rows versus about 6.0 us/op for the compact raw node and about 5.2 us/op for the hand-written raw result-set prototype.
  - Do not add a typed direct raw nested row mapper that still calls `mapRawNestedFieldValue()` for every field without new evidence. A 2026-06-08 spike targeted production compiler-emitted field-type mappings but measured `direct plan blog page / nested rows` at 6.76 us/op versus an adjacent 6.64 us/op baseline.
  - `data-mapper.ts::mapObjectWithMappings()` avoids `Object.hasOwn()` on the common present, non-`undefined` field/object mapping path and only falls back to ownership checks for `undefined`. This benchmarked as a meaningful nested blog-page outer-data-map win; preserve missing-field behavior for explicit `undefined` properties if touching this path.
  - `QueryInterpreter` has a compact mapped-join fast path for the root compiler shape `l(@parent = q) -> l(@parent$field... = m(field, g @parent)) -> j(g @parent, children...)`. This targets root relation joins such as blog-page author/category/tags/comments and benchmarked as a major nested-row and value-scope win; keep focused coverage in `query-interpreter.test.ts`.
  - `QueryInterpreter` has a compact nested-branch fast path for the compiler shape `l(@parent = q) -> l(@parent$field = m(field, g @parent)) -> j(g @parent, one child)`. This targets nested relation branches such as blog-page `tags` / `comments` and benchmarked as a meaningful nested-row win; keep focused coverage in `query-interpreter.test.ts`.
  - `QueryInterpreter` intentionally reuses one scoped runtime context per legacy/compact `let` scope and compact join fast-path scope instead of cloning `{ ...context, scope }` for every binding or join child. This benchmarked as a small neutral-to-positive cached nested-plan cleanup; preserve the one-context-per-scope shape when touching scope semantics.
  - `QueryInterpreter.#executeRawNestedReadDbQuery()` intentionally returns the driver `queryRaw()` promise directly on the no-raw-SQL/no-comments/no-instrumentation/single-query fast path. The callers already await the helper and `QueryInterpreter.run()` still maps rejected promises through `rethrowAsUserFacing()`, so reintroducing `return await` just adds an async continuation on a hot raw-nested DB-leaf path.
  - Do not replace `QueryInterpreter.run()`'s `{ ...options, ...runtimeFields }` context construction with an explicit object copy without stronger evidence. A local spike slightly improved the full cached-wrapper nested blog row but regressed direct/local nested-plan rows materially; the spread shape appears better for the inner interpreter path on current V8.
  - `QueryInterpreter`'s local `mapField()` helper handles array-of-records inline and only recurses for nested arrays. This targets hidden join-key extraction for nested branch arrays and benchmarked as a small neutral-to-positive cleanup; keep the behavior for primitives (`mapField([1], 'id')` returns `[1]`) unless changing the interpreter contract deliberately.
  - Do not add a separate strict single-parent join attachment helper in `attachChildrenToParents()` without new evidence. A local spike that avoided one-element parent array wrapping and repeated parent `asRecord()` calls passed tests but worsened cached wrapper/direct/local nested blog rows, so the generic tiny strict-key helper remains better on current V8.
  - Do not split the common one-child join path into a separate `attachChildToParents()` helper just to avoid the temporary one-element `children` array/object. A same-session A/B after V8 warmup showed patched and unpatched nested blog rows were effectively identical (cached wrapper about 17.7-17.8 us/op, direct/local about 11.9-12.1 us/op), so the extra branch/helper is not worth keeping.
  - Do not change DateTime JS result cloning in `data-mapper.ts` from `new Date(value)` to `new Date(value.getTime())` without new evidence. The clone-preserving patch passed mapper tests and lint, but nested blog rows stayed in the same warm band (cached wrapper roughly 17.6-17.9 us/op, direct/local roughly 11.8-12.1 us/op) and the isolated outer data map did not move.
  - The compiler creates the root data-map wrapper in `/home/aqrln.guest/prisma-engines/query-compiler/query-compiler/src/translate.rs`: `map_result_structure(&graph, ...)` runs before node translation and wraps the translated root sequence in `Expression::DataMap`. Query-mode nested relation plans are built in `translate/query/read.rs` (`add_inmemory_join()` / `build_read_related_records()`), while `core/src/query_graph_builder/read/utils.rs::merge_relation_selections()` injects hidden linking fields needed for `attachChildrenToParents()`. Any child-side data-map/projection pushdown must preserve those join keys until attachment is done.
  - For root read queries that structurally guarantee `Expression::RawNestedRead`, the compiler intentionally skips building the root result mapper that would be discarded. Engines commit `491ce36d76c` keeps this guarded by a conservative preflight plus a hard invariant error if translation does not produce raw-nested output; sampled query-mode nested reads saved 11-26 full-compile allocations while join/write/aggregate controls stayed allocation-neutral.
  - Do not remove the owned `Filter` clones inside `query-compiler/core/src/query_graph_builder/extractors/filters/filter_fold.rs` just for allocation savings without new evidence. A local ownership cleanup passed `cargo test -p query-core filter_fold --lib` and `cargo check -p query-compiler`, but the focused allocation profile for `query-m2o`, `query-many-m2m`, `nested-pagination-query`, `create-nested-create`, and `update-set-nested` was unchanged.
  - Do not rewrite `QueryGraph::child_pairs()` / `direct_child_pairs()` to bypass the intermediate sorted `outgoing_edges()` vector without new evidence. A one-pass pair collection plus `EdgeRef: Copy` saved 11 full-compile allocations on `update-set-nested`, but reversed Criterion showed the unpatched code was faster on `update-set-nested` and otherwise neutral/mixed.
  - Query compiler raw nested direct read translation has a kept allocation cleanup in engines commit `7b0f3323f37`: `RelationField::single_left_scalar()` and `Model::single_primary_identifier_scalar()` avoid allocating relation-scalar vectors when the direct raw nested path only accepts a single-column relation. Focused allocation profile improved `translate_ir` / `full_compile` by 2 allocs on direct one-to-many/many-to-one reads, 6 on m2m reads, and 8 on `create-nested-connectOrCreate-mixed`, with focused Criterion neutral-to-positive. Keep this helper path scoped to single-scalar/direct raw nested reads; do not route the known-regressed SQL-builder join paths through it.
  - Query builder model projection column collection has a kept allocation cleanup in engines commit `a05eab3fec2`: `ModelProjection::as_columns()` builds a single `Vec<Column<'static>>` directly and pushes relation scalar columns inline, avoiding boxed `ColumnIterator`s and per-relation temporary scalar-column vectors. Focused allocation profile improved sampled read/nested-write full-compile rows by roughly 13-37 allocations/op, and adjacent Criterion was neutral-to-positive including join rows. Do not conflate this with the rejected relation-helper SQL-builder detour; this cleanup preserves the existing SQL-builder field/relation paths and only removes projection-column collection scaffolding.
  - Query-plan `templateSql.fragments` are compact-only: raw string chunks, `null` scalar parameter placeholders, and compact tuple/tuple-list fragments. Do not reintroduce legacy tagged string chunks or other old fragment objects on this internal boundary.
  - Wasm query compilation currently crosses the JS/Wasm boundary with a JSON request string (`QueryCompiler.compile(request: string)` / `compileBatch(request: string)` in `packages/client-common/src/QueryCompiler.ts`). On the Rust side, `query-compiler-wasm/src/compiler.rs` calls `RequestBody::try_from_str`, `request-handlers/src/protocols/mod.rs` deserializes with `serde_json::from_str::<JsonBody>()`, `protocols/json/body.rs` materializes owned `JsonBody` maps/`serde_json::Value`, and `protocols/json/protocol_adapter.rs` walks that tree again into owned `Operation`/`Selection`/`ArgumentValue`. Replacing string deserialization with `serde_wasm_bindgen::from_value` would only remove JS-side request parsing unless the request parser itself is reworked to validate directly over `js_sys` values or another typed/lazy representation.
  - A temporary release-Wasm phase probe over query compiler fixture reads showed JSON request decoding around 4-7 microseconds (roughly 4-6% of internal compile+serialize time), `into_doc` around 5-7 microseconds for common reads, compiler work around 50-60%, and plan serialization often around 25-35% (up to ~39% for nested create). Treat `serde_wasm_bindgen::from_value` as a low-ceiling optimization unless it also removes the request stringify/cache-key path or the owned `JsonBody`/`serde_json::Value` tree; plan serialization and compiler-owned allocations are larger targets. A 2026-06-08 sidecar `compileSerdeWasmBindgen(JsValue)` spike for simple parameterized `findUnique(User.id)` was worse than the current string path in release Wasm: about 39-44 us/op vs 30-33 us/op including per-op stringify.
  - Treat the JS-owned input query / Rust-owned internal IR idea as a larger architecture track, not a narrow boundary-conversion optimization. The useful cache-hit wedge would pass the original JS query object by reference, fuse parameterization plus structural cache lookup before building `JsonBody`/`ArgumentValue`, and return cached JS plan objects on hits. Avoiding Rust-owned SQL strings is probably a separate SQL-template/interner design because compile misses still construct SQL fragments in Rust.
  - Do not implement JS-owned query cache hits as a naive Wasm `js_sys` / `Reflect` structural walker over already-parameterized JS objects. A 2026-06-07 proof-only `lookupParameterizedPlanRef` / `cacheParameterizedPlanRef` spike compiled, but isolated lookup rows were far slower than the current cache-key path: about 39.9 us/op for `findUnique`, 24.2 us/op for stable `findMany`, and 163.6 us/op for the nested blog-page shape. A 2026-06-08 follow-up hard-coded only the generated blog-page `Post.findUnique` args shape in Wasm and still measured 27.85 us/op in optimized Wasm versus 1.23 us/op for the JS static descriptor and 2.48 us/op for the JS lazy descriptor. Another 2026-06-08 sidecar over simple `findUnique(User.id)` measured a naive `compileFindUniqueJs(JsValue)` Reflect walker at about 32-36 us/op and walker-only at about 8.3 us/op, while `JSON.stringify` alone was about 0.43 us/op. A practical JS-owned-query design needs a lower-overhead representation/access strategy, not repeated dynamic property walking from Wasm.
  - Do not replace the current `parameterizeQuery()` + native `JSON.stringify(parameterizedQuery.query)` cache-key path with a handwritten JS structural serializer without new evidence. A benchmark-only fused prototype that walked `JsonQuery` with `ParamGraph` raw edge data and directly serialized exact matching cache keys was slower across sampled rows (`blog post page` 363,567 ops/sec current vs 163,134 ops/sec fused). If structural cache keys are revisited, the likely useful version needs to avoid Wasm-owned input maps/plan serialization across the boundary, not merely hand-roll JS string generation.
  - Do not pre-size query compiler result-node `IndexMap` field maps with `selection_order.len()` / aggregation ordered-set length without new evidence. A local allocation profile saved only about 0-2 allocations and up to about 1 KiB per compile, while Criterion regressed `create-nested-create`, `filter-contains-param`, `nested-pagination-query`, and `aggregate-nested-m2m`.
  - `query_graph_builder::read::utils::merge_relation_selections()` returns early when there are no nested queries after applying an optional parent relation. Keep that early return: local allocation profiling saved 2 allocations/op and about 2.5 KiB/op in `graph_build`/`compile_ir` for common read fixtures, and Criterion improved `query-m2o`, `filter-contains-param`, and `nested-pagination-query` by roughly 1.5-2.3%.
  - `query_graph_builder::extractors::filters::extract_unique_filter()` fast-paths all-unique selectors and `internal_extract_unique_filter()` avoids the temporary filter vector for one-field unique selectors. This intentionally removes redundant `AND 1=1` tautologies from query snapshots and benchmarked as a roughly 5-9% compile-time win on affected unique-selector write/read fixtures (`connectOrCreate`, `update-set-nested`, `delete-one`, `upsert`). Do not reintroduce the unique/rest partition or one-field vector path without stronger evidence.
  - `query_graph_builder::write::nested::connect_or_create_nested` should reuse an already-built `child_model.shard_aware_primary_identifier()` when a branch needs the same child identifier for both the read node and projected dependency. This saved two `graph_build` allocations/op on `create-nested-connectOrCreate-mixed` and `create-nested-connectOrCreate-one2m` with neutral focused Criterion results.
  - Do not add a `read_id(s)_infallible_with_selected_fields()` helper just to bypass automatic primary-id merging when a caller already has `model.shard_aware_primary_identifier()`. A local spike saved only 3 allocations/op on `create-nested-connectOrCreate-mixed` and `update-set-nested`, moved no other sampled write fixtures, and close Criterion was mixed/regressed on `delete-many-limit` and `update-set-nested-prisma#27650`.
  - Do not replace nested-create temporary `Vec`s with `SmallVec<[...; 1]>` without new evidence. A local spike saved 4 full-compile allocations and about 1.3 KiB/op on `create-m2m`, but close Criterion was slower on `create-m2m` and unchanged controls.
  - `query_graph_builder::write::utils::coerce_values()` returns a `CoercedParsedInputValues` wrapper, not a `Vec`; singleton nested write inputs intentionally avoid allocating a one-element `Vec`. If a caller truly needs a `Vec`, collect explicitly and re-benchmark the affected nested write fixtures.
  - Do not replace nested `connect` filter dedupe in `query_graph_builder::write::nested::connect_nested` with manual linear `Vec` dedupe as a standalone allocation cleanup. A 2026-06-11 spike saved two `graph_build` / `full_compile` allocations and about 1.2-1.5 KiB/op on affected connect rows, but close Criterion regressed `create-nested-connect`, `update-connect-child-one2m`, and `update-connect`.
  - Do not special-case `Filter::Empty` selector folding in `translate/query/write.rs::build_query_from_record_filter()` without new evidence. A local spike saved 7 allocations/op only on `update-set-nested-prisma#27650`, had no direct Criterion win on that fixture, and regressed `create-nested-create` in the same focused run.
  - `query_structure::filter::IntoFilter` intentionally returns a direct scalar filter for singleton `SelectionResult`s and singleton placeholder selector vectors instead of wrapping them in `Filter::And(Vec)`. This source-level cleanup saved one `translate_ir`/`full_compile` allocation on many nested write rows and five on `update-set-nested-prisma#27650`, with close Criterion A/B staying within noise. Do not restore the old `exactly_one().and_then(SelectionResult::as_placeholders)` path without new evidence.
  - `query-compiler/src/data_mapper.rs::get_result_node()` should consume borrowed `SelectedField` slices rather than rebuilding nested relation `FieldSelection`s with `f.selections.to_vec()`. The borrowed-slice shape saved 1-2 allocations/op and up to about 1.6 KiB/op in `translate_ir` / `compile_ir` for nested read/write fixtures, with focused Criterion wins on `connectOrCreate`, `query-m2o`, and `update-set-nested`.
  - `query-structure/src/field_selection.rs::FieldSelection::merge()` has fast paths for empty RHS, empty LHS, and RHS already contained in LHS before falling back to `itertools::unique()`. This avoids the hash/set allocation for common ID/linking-field merges and saved about 10-22 full-compile allocations/op plus roughly 9-20 KiB/op on sampled nested writes and reads, with focused Criterion neutral-to-positive. Do not remove these fast paths without allocation-profile and Criterion evidence.
  - Do not add a no-relation guard to `FieldSelection::into_without_relations()` without new evidence. A 2026-06-08 scout-led spike returned `self` when no top-level relation selections existed, but allocation profiles were unchanged on sampled nested read and scalar/filter fixtures, so it was reverted.
  - Do not rewrite `FieldSelection::as_scalar_fields()` to fail fast without new timing evidence. A 2026-06-08 spike first used eager `Vec::with_capacity()` and saved only small allocation bytes while Criterion regressed `query-m2o-lateral` and `query-many-one2m`; the refined `Vec::new()` fail-fast version lost the allocation win and was reverted.
  - Do not change `query_document::parser::Path` from owned `String` segments to a lifetime-generic `Cow<'a, str>` cons-list as a standalone allocation optimization. A 2026-06-08 spike reduced full-compile allocation counts by roughly 11-33/op on sampled reads/nested writes, but saved only about 0.1-0.2 KiB/op and Criterion regressed common compile rows by about 1.6-3.3%. If parser path allocation is revisited, use the larger borrowed mutable push/pop stack idea that removes the per-hop `Rc` node allocation and materializes segments only for validation errors.
  - `query-compiler/core/src/query_document/parser.rs::Path` intentionally uses a mutable `Vec<String>` stack as of engines commit `4352448ee00`, with `segments_with()` / `parent_segments_with()` materializing validation paths only on error. This replaced the immutable `Rc<Option<(String, Path)>>` cons-list and saved 14-60 full-compile allocations/op plus roughly 1.8-5.6 KiB/op on sampled read/write fixtures; focused Criterion improved the main rows by about 1.3-3.7%.
  - `query-compiler/core/src/query_document/parser.rs::Path` now stores that mutable validation stack in `SmallVec<[String; 8]>` rather than `Vec<String>`. Engines commit `a62504a80bd` saved only 2-3 full-compile allocations/op and about 0.1-0.4 KiB/op on sampled reads/nested writes, so it was kept only after adjacent Criterion stayed neutral-to-positive: reapply medians were `query-m2o` 28.921 us, `query-many-m2m` 35.399 us, `update-set-nested` 100.65 us, and `create-nested-connectOrCreate-mixed` 127.00 us versus reverted-control medians 29.952, 35.588, 102.42, and 130.18 us.
  - Do not replace SQL query-builder `linking_fields() -> FieldSelection -> ModelProjection` join-column paths with direct `left_scalars().as_columns()` without new CPU evidence. A 2026-06-08 sidecar/main spike saved 12 allocations and about 3.6 KiB on `nested-pagination-join`, but Criterion regressed `nested-pagination-join` by about 15%, `aggregate-join` by about 5.5%, and `nested-distinct-join` by about 2.9%, so it was reverted. Retesting the same detour on engines commit `e98dd7b4193` still did not justify it: allocation profile saved 6 translate/full-compile allocations and about 0.9 KiB on join/lateral rows, but reverse Criterion was mixed and regressed `nested-pagination-join`, `query-m2o-lateral`, and `query-o2m-lateral`.
  - Do not rewrite `FieldSelection::union_iter()` to fold through `FieldSelection::merge()` without new evidence. A local spike saved only two allocations on `nested-pagination-join` and improved that row, but regressed `update-set-nested-prisma#27650` by roughly 6-12%.
  - Do not replace `query_document::Selection::dedup()`'s `unique_by(|s| s.name.clone())` with a small no-name-clone scan without new evidence. A local spike produced zero allocation movement on sampled nested write/read fixtures.
  - Do not rewrite `Model::shard_aware_primary_identifier()` to build only one `Vec<ScalarFieldRef>` instead of the current scalar-id vector plus field-ref vector without new evidence. The rewrite saved 2-17 allocations/op across sampled graph-build rows but broadly regressed Criterion, including roughly 4.5% on `create-nested-connectOrCreate-mixed`, 6-8% on read controls, and 6.7% on `update-set-nested`.
  - Do not hoist `child_model.shard_aware_primary_identifier()` out of `connect_or_create_nested::handle_many_to_many()`'s value loop without new evidence. A local spike added one allocation/op to the mixed and one-to-many connect-or-create fixtures because preserving the outer identifier forced an extra clone.
  - In `write/nested/update_nested.rs`, replacing `parent_relation_field.related_model().shard_aware_primary_identifier()` with the already passed `child_model.shard_aware_primary_identifier()` was allocation-neutral on update-set and one-to-many update fixtures; do not churn this call site without a broader reason.
  - In `write/nested/set_nested.rs`, passing the already available `child_model` into the many-to-many / one-to-many set handlers instead of recomputing `parent_relation_field.related_model()` was allocation-neutral on `update-set-nested`, `update-set-nested-prisma#27650`, connect-or-create, create-m2m, and query-m2o samples; do not churn these handler signatures without a broader reason.
  - `query_graph_builder::write::WriteArgsParser::nested` uses `SmallVec<[(RelationFieldRef, ParsedInputMap); 1]>` for nested-write parser handoffs. This avoids a heap `Vec` allocation for common zero/singleton nested write inputs and saved small graph-build bytes/counts on update-set fixtures while staying within Criterion noise. Keep `NestedWriteOperations<'a>` as the local alias if extending create/update handoff signatures.
  - `query-compiler/src/translate.rs::process_child_with_dependencies()` returns a direct `Expression::Let` when a child has projected bindings but no validations, instead of creating `Expression::Seq(vec![Let])` and relying on `simplify()` to unwrap it. This saved `translate_ir` allocations/bytes on nested writes (`create-nested-create` 1299 -> 1297 full allocations; `update-set-nested` 2165 -> 2160 and 268.2 -> 264.9 KiB), with focused Criterion neutral-to-positive after reversed A/B.
  - `query-compiler/src/translate/query/read.rs::add_inmemory_join()` collects parent linking fields into one small `Vec`, sorts by field name, and `dedup_by`s before building `mapField` bindings. This replaced `itertools::unique().sorted_by()` and saves about one allocation in `translate_ir` / `compile_ir` for nested read/write samples without changing binding order. Keep Criterion coverage on `query-many-m2m`, `nested-pagination-query`, and `create-nested-create` if editing this path.
  - `query-compiler/src/translate/query/read.rs::build_raw_read_related_records()` borrows `RelatedRecordsQuery` and clones only the mutable pieces (`args`, `selected_fields`, optional `parent_results`) instead of cloning the whole related read tree before compiling raw nested child reads. This saved 5-11 full-compile allocations on sampled raw-nested read/connected write rows and kept focused Criterion neutral-to-positive after a narrow rerun (`query-many-m2m` within noise, `query-many-one2m` improved).
  - `query-compiler/src/translate.rs::fold_result_scopes()` checks whether there is a single result node with `result_nodes().take(2).count()` and only builds the binding-name `Vec` for the multi-result fallback. This saves two allocations in `translate_ir` / `compile_ir` for `create-nested-create`; common read samples do not hit this shape.
  - `query-compiler/src/translate.rs::process_child_with_dependencies()` reuses one `incoming_edges(&node)` vector for validation extraction, projected dependency bindings, and the child translator. Do not reintroduce separate incoming-edge lookups there: the reuse saves about four allocations in `translate_ir` / `compile_ir` for `create-nested-create`, while common read samples are unchanged.
  - `FieldSelection::union_iter()` has empty/single fast paths and is used by query-graph dependency normalization to avoid building temporary `Vec<FieldSelection>` unions. This saved two `graph_build` allocations/op and about 2.5 KiB/op on several nested/read fixtures, with noisy but ultimately neutral-to-positive focused Criterion runs. Keep coverage on `query-many-m2m`, `update-set-nested`, and `connectOrCreate` if editing it.
  - `query_graph_builder::read::utils::pairs_to_selections()` intentionally pre-sizes its output vector with `pairs.len()`. This does not change allocation counts, but it trimmed about 0.3-1.9 KiB/op from targeted `graph_build` / `full_compile` read and nested fixtures and benchmarked neutral-to-positive in focused Criterion rows.
  - Do not replace write translation's one-element `Expression::Sum` / `Expression::Concat` vectors with a generic helper that returns the single expression directly without stronger evidence. A local spike improved `delete-many` and `create-many-and-return`, but regressed `create-nested-create` and `update-set-nested`; the simplifier already removes the wrapper later, and the construction-path change perturbed hotter nested writes.
  - Do not move `query_document/parser.rs::parse_arguments()` path construction behind the optional-argument presence check without new evidence. A local spike saved 12-24 `graph_build` allocations/op across common read/nested write fixtures, but Criterion regressed several important compile rows (`query-m2o`, `nested-pagination-query`, `filter-contains-param`, `delete-one`, `update-set-nested`), so the eager path construction remains faster.
  - Do not rewrite `query_document/parser.rs::parse_arguments()` to pre-index given arguments into a `SmallVec` by schema argument as a standalone cleanup. A 2026-06-12 spike had no allocation-count movement on sampled filter/read/nested rows and regressed focused Criterion rows (`nested-pagination-query` +2.72%, `update-set-nested` +6.99%), so the simple repeated scans remain faster for current fixtures.
  - Do not rewrite `query_graph_builder::extractors::filters::filter_fold` to match `Filter` by move instead of `f.clone()` just to remove clone-looking work. A local spike saved only one allocation on `filter-not-contains-param` and regressed focused filter Criterion rows by roughly 2.7-8.8%.
  - `query_graph_builder::extractors::filters::fold_search_filters()` pre-sizes its output vector with `filters.len()`. This is only a tiny byte-allocation cleanup for grouped filters (`filter-not-contains-param` full compile stayed at 667 allocs/op but dropped about 0.4 KiB) and should not be treated as a general filter speed win.
  - `query_graph_builder::extractors::filters::merge_search_filters()` intentionally skips `fold_filter()` / `fold_search_filters()` only for `AND` / `OR` / `NOT` trees that contain no `Search` / `NotSearch` scalar condition. Engines commit `69faaa96948` saved four `graph_build` / `full_compile` allocations on `filter-not-contains-param` (`373 -> 369`, `616 -> 612`) while scalar controls stayed allocation-neutral, and close Criterion plus a reversed-control repeat was timing-neutral. Do not broaden the pre-check to every scalar filter without new evidence; the kept shape preserves the old scalar fast path.
  - Do not broadly rewrite `query-compiler/src/translate.rs::NodeTranslator::process_child_with_dependencies()` projected-dependency binding collection from the existing `Either`/`flat_map` shape into an imperative loop just to skip no-op parent bindings. A refreshed local spike saved `update-set-nested` allocations (2160 -> 2150 full compile) and improved its close timing, but added allocations to `create-nested-create`, `create-nested-connectOrCreate-one2m`, and `update-set-nested-prisma#27650`; direct Criterion regressed `create-nested-connectOrCreate-mixed` 128.36 -> 132.55 us and `create-nested-connectOrCreate-one2m` 116.40 -> 121.22 us.
  - Do not preallocate `WriteArgsParser`'s inner `WriteArgs` map from raw `data_map.len()` without stronger evidence. A full-capacity spike added allocations to hot nested writes, and a reserve-on-first-write variant worsened cheap many-write bytes while leaving nested rows effectively unchanged.
  - Do not rewrite `pairs_to_selections()` to avoid cloning `ParsedField` for scalar selections without a stronger speed win. A local spike saved 3-9 `graph_build` allocations/op across the default allocation-profile fixtures, but a direct Criterion A/B made `create-nested-create` slower while the no-patch baseline stayed faster.
  - Do not replace `ScalarFilterParser::parse()`'s `Vec<Vec<Filter>>` flattening with a single extend loop without new evidence. A local spike saved one allocation and about 0.5 KiB/op for `filter-contains-param`, but Criterion regressed the focused compile rows broadly, including roughly 5-10% on filter/nested-pagination rows in that run.
  - Do not cache each nested relation's `left_scalars()` result inside `query-compiler/src/translate/query/read.rs::add_inmemory_join()` without new evidence. A local spike saved 1-3 `translate_ir` allocations/op and about 1.5-2 KiB/op on nested fixtures, but Criterion regressed `query-m2o` and `create-nested-create`, so recomputing the small scalar vectors remains faster.
  - Do not add a single-scalar parent-result fast path in `query-compiler/src/translate/query/read.rs::build_read_related_records()` / `build_raw_read_related_records()` as a standalone cleanup. A 2026-06-12 spike bypassed `split_into()` / `FieldSelection::assimilate()` when parent and child link scalars had matching type/arity, but allocation counts were unchanged and close Criterion was mostly neutral-to-slightly-worse (`query-m2o 76.792 -> 77.138 us`, `query-many-m2m 86.374 -> 86.601`, `query-unique-one2m-pagination 84.440 -> 84.874`).
  - Do not replace `itertools::unique()` in nested `connect` / `disconnect` filter extraction with a manual small-`Vec` linear dedup scan without new evidence. The spike saved two `graph_build` / `full_compile` allocations and about 1.3-1.5 KiB/op on affected connect/disconnect fixtures, but a close Criterion A/B made `create-nested-connect`, one-to-many parent connect, and `update-m2m-disconnect` slower.
  - Do not replace `query-compiler/src/expression.rs::Pagination.cursor`'s `HashMap` with a serialized vector-of-pairs just for tiny cursor allocation savings. The spike reduced allocated bytes by up to about 2.7 KiB/op on `update-set-nested`, but final focused Criterion regressed `query-m2o` and `update-set-nested-prisma#27650`.
  - Nested connect-or-create existence checks should reuse the child-link selection already returned by the read node when feeding `IfInput`; `Flow::If` only needs a row-list placeholder for `RowCountNeq(0)`, not specifically the child primary identifier. The accepted engines change `dd196d58292` saved two full-compile allocations on `create-nested-connectOrCreate-m2one` and `create-nested-connectOrCreate-mixed` with neutral close Criterion and clean Wasm/product builds.
  - Many-to-many nested connect-or-create intentionally returns the existing-or-created child id from the `if` and runs one shared `ConnectRecords` node after the branch. Engines commit `9b870cf327e` replaced duplicate branch-local connect nodes, saved about 135-138 full-compile allocations on `create-nested-connectOrCreate-mixed` / `one2m`, and improved close Criterion by roughly 4.5% on those rows. Do not split the shared connect back into both branches without new close allocation and timing evidence.
  - Many-to-many nested connect-or-create no longer needs a create-side `Flow::Return`: `CreateRecord` already returns the selected child id that the shared post-`if` connect consumes. Engines commit `8b2a45cfe7b` saved 16-17 full-compile allocations on `create-nested-connectOrCreate-mixed` / `one2m` with close Criterion neutral-to-positive and clean Wasm/product builds. Keep this scoped to the m2m create branch; broader scalar-only/create-return removal still needs fresh close allocation and timing evidence.
  - Nested-only to-one updates intentionally validate the found child row and pass it through `Flow::Return` instead of creating an empty `UpdateRecord` plus reload. Engines commit `6f256b26dfd` saved 125 full-compile allocations and about 7% Criterion time on `update-set-nested-prisma#27650` while preserving missing-child validation. Do not reintroduce the redundant empty update node without new close allocation and timing evidence.
  - Nested-upsert `Then` branches with nested-only update data intentionally validate the found child row and pass it through `Flow::Return`; nested operations attach to that returned child instead of running an empty `UpdateRecord`. Engines commit `34bde27cfb5` added `nested-upsert-nested-only` coverage, saved 137 full-compile allocations, and improved that fixture by about 4% in close Criterion while keeping scalar update branches on the existing update/emulation path.
  - Do not try to share duplicate many-to-many nested-upsert `connect` target reads with a small parser/graph rewrite. A 2026-06-12 exact-match spike on `nested-upsert-nested-only` shared the `Category` read across create/update branches while keeping branch-local `ConnectRecords`, but allocation moved the wrong way (`full_compile 3011 -> 3032`, `translate_ir 1723 -> 1749`). A true hoist also risks changing missing-connect-target error order unless the graph has a branch-aware shared-node primitive.
  - One-to-many nested `set: []` intentionally skips the new-child read, both diff nodes, and the impossible connect branch. Engines commit `d3d45546416` added `update-set-nested-empty` coverage and feeds the old-child read directly into the required-child-side relation violation check plus disconnect update, saving 344 full-compile allocations and about 18-20% Criterion time on the focused row. Do not add an old/new graph compatibility branch for this internal shape.
  - One-to-many nested `set` on required child-side relations intentionally validates old-only rows and returns without building a nulling update, because any required-side disconnect must fail with a relation violation. Engines commit `a87f6ccff37` changed both empty and non-empty required `set` paths to validation-only nodes, saving 165/162 full-compile allocations and improving close Criterion by about 30%/5% on `update-set-nested-empty` / `update-set-nested`. Optional child-side relations still use the real disconnect update path.
  - Top-level upsert update branches with no scalar writes intentionally pass through the initial parent-id read instead of building a no-op update/read path. Engines commit `a3ee45d7d44` added empty and nested-only update coverage, attaches nested writes to the `Flow::Return` pass-through when present, and saved 203/206 full-compile allocations with about 16%/10% Criterion wins on `upsert-empty-update` / `upsert-nested-only-update`. Scalar update branches still use the normal update and on-update emulation path.
  - Do not skip the nested-create parent `DataExpectation::non_empty_rows` edge validation just because the parent node is a `CreateRecord`. A 2026-06-12 spike saved 27-29 full-compile allocations on create-parent nested create rows, but close Criterion softened `create-nested-create` and unrelated controls, so the validation stays until a broader graph-shape change gives new CPU evidence.
  - Do not add a direct `SelectionResult::from_selected_pairs()` constructor as a standalone translation cleanup. The 2026-06-12 spike saved zero allocations on sampled rows and regressed quick Criterion for `nested-upsert-nested-only` by about 5.3%, despite some mixed improvements on smaller rows.
  - Do not pre-size JSON protocol `Selection` nested-selection vectors as a standalone request-adapter cleanup. A 2026-06-12 spike added schema/selection-set capacity hints, then narrowed to only root `Vec::with_capacity(query_selection.len())`; even the narrowed patch added allocations on sampled `into_doc` / `full_compile` rows (`create-nested-create 1226 -> 1228`, `query-many-one2m 632 -> 634`, `nested-upsert-nested-only 3011 -> 3013`), so the current empty-vector growth pattern stays.
  - Many driver adapters construct fresh `SqlResultSet.columnNames` / `columnTypes` arrays for each query result (e.g. via driver field metadata maps). Runtime caches that need to survive repeated real DB calls should not rely only on result metadata array identity; cache by stable column shape when appropriate.
  - `ExecutePlanParams` interface in `packages/client/src/runtime/core/engines/client/Executor.ts` defines what's passed through the execution chain.
  - `TransactionManager` in `packages/client-engine-runtime/src/transaction-manager/transaction-manager.ts` owns interactive transaction IDs and implements nested transactions using savepoints. Savepoint SQL is provider-specific (e.g. PostgreSQL uses `ROLLBACK TO SAVEPOINT <name>`, MySQL/SQLite use `ROLLBACK TO <name>`, SQL Server uses `SAVE TRANSACTION <name>` / `ROLLBACK TRANSACTION <name>` and has no release statement).
  - `Transaction` in `packages/driver-adapter-utils` models savepoint behavior as async methods (`createSavepoint`, `rollbackToSavepoint`, optional `releaseSavepoint`) instead of returning SQL via `savepoint(action, name)`. `TransactionManager` expects adapter methods for savepoints and does not synthesize provider fallback SQL.
  - Fluent API `dataPath` is built in `packages/client/src/runtime/core/model/applyFluent.ts` by appending `['select', <relationName>]` on each hop; runtime unpacking in `packages/client/src/runtime/RequestHandler.ts` currently strips `'select'`/`'include'` segments before `deepGet`.
  - In extension context resolution, `dataPath` should be interpreted as selector/field pairs (`select|include`, relation field). Do not strip by raw string value or relation fields literally named `select`/`include` get dropped.
  - Generated `findUnique` / fluent-capable actions always pass through `applyFluent()`, even for the root non-chained call. Root calls supply an empty `dataPath` override (and normally a `callsite`) before invoking the model action; nested fluent calls use a non-empty `dataPath`. Any generated-call fast path that rejects all `paramOverrides` will silently miss root `findUnique` unless it explicitly allows the empty-root fluent override shape, while still falling back for non-empty `dataPath` to preserve nested result unpacking.
  - Do not skip the root `applyFluent()` proxy as a quick generated-call optimization: a 2026-06-07 internal fast-path spike improved generated precomputed rows by about 0.5-0.6 us/op but was reverted because returning the raw PrismaPromise breaks relation chaining. A product-safe version needs to preserve lazy relation access while avoiding per-call proxy allocation for the common await-only path.
  - Do not replace `applyFluent()`'s per-call proxy handler with a stable handler plus `WeakMap` state without new evidence. A 2026-06-07 spike preserved semantics but regressed promise construction from roughly 0.29 / 0.53 us/op to 0.61 / 1.17 us/op and made generated precomputed rows slower.
  - `applyModel.ts` intentionally hoists the stable `clientMethod` label once per cached model-action property instead of rebuilding `${jsModelName}.${key}` inside each generated-call path. A 2026-06-08 focused A/B over 200k iterations moved generated `findUnique` from 3.02 -> 2.91 us/op and generated `findMany users` from 2.60 -> 2.51 us/op; broad generated rows were mixed but neutral-to-positive.
  - For generated precomputed query-plan work, the safer productization shape is request-layer first: pass `protocolQuery` + `precomputedQueryPlanCacheHit` into the request path and let `RequestHandler`/`DataLoader` decide whether the call is truly single or batched. `RequestHandler.singleLoader` forwards precomputed data to `ClientEngine.request()`; `RequestHandler.batchLoader` forwards `precomputedQueryPlanCacheHits` only when every request in the batch has one, and `ClientEngine.requestBatch()` falls back to current batch parameterization if any payload is missing or unsuitable. A 2026-06-07 Node run measured this internal request-layer path at 5.63 -> 3.87 us/op for generated `findUnique` and 18.29 -> 13.11 us/op for the nested blog-page shape. A later batched `findUnique` run measured 16.22 -> 14.76 us/op on Node and 17.80 -> 12.90 us/op in Workerd while preserving one `queryRaw` per pair; adding `precomputedBatchId` on descriptor hits improved that to 11.76 us/op Node and 11.50 us/op Workerd. Calling `RequestHandler.request()` directly on descriptor hits then measured 3.49 us/op single findUnique, 10.59 us/op batched findUnique, and 12.39 us/op nested blog-page on Node; Workerd measured about 4.70, 11.30, and 14.00 us/op. Generated `findMany users` is a safer non-batchable shape: Node measured 4.23 us/op baseline, 3.00 direct-engine, and 3.20 request precompute; Workerd measured 3.50 / 2.10 / 2.50 us/op worker-loop, all with unchanged `queryRaw`. Direct-engine precompute was much faster for some shapes but bypassed DataLoader and doubled `queryRaw` for batched `findUnique`.
  - Precomputed request batches intentionally use a precomputed-specific batch cache key derived from the per-query precomputed `cacheKey`s. This lets warmed generated batches hit the plan cache before building the full renamed `JsonBatchQuery`; compile misses still build the normal parameterized batch and store individual plan entries. A 2026-06-07 Node run moved generated request-precomputed batched `findUnique` to 9.15 us/op, then 8.37 us/op in a focused run, while preserving one `queryRaw` per pair; Workerd measured 10.50 us/op worker-loop / 12.46 us/op host dispatch.
  - `RequestHandler.batchLoader` intentionally has a two-request branch that manually builds `queries`, optional `precomputedQueryPlanCacheHits`, `containsWrite`, and mapped results instead of using `map`/`every`/`some` helpers. This targets the same common two-`findUnique` batch shape. A 2026-06-07 Node run moved generated request-precomputed batched `findUnique` from 8.08 to 7.66 us/op; Workerd moved from 9.70 to 9.20 us/op.
  - Batch precomputed placeholder renaming intentionally skips recursive query walking when a hit's placeholder names already match the desired batch sequence (`%1`, `%2`, ...). This avoids cloning the first query in common generated `findUnique` batches. A 2026-06-07 same-session Node A/B moved generated request-precomputed batched `findUnique` from 11.18 us/op reverted to 10.28-10.62 us/op patched, and Workerd measured 10.60 us/op versus the recent 10.80 us/op journal baseline.
  - `DataLoader` intentionally has a two-deferred-job same-batch-key fast path that dispatches the pair directly instead of routing through the `batches` object. This targets common `Promise.all([findUnique, findUnique])` generated-client batching. A 2026-06-07 Node run moved generated request-precomputed batched `findUnique` from 8.37 to 8.08 us/op after the precomputed batch cache-key patch; Workerd moved from 10.50 to 9.70 us/op.
  - `DataLoader` also intentionally routes exactly two-job batches through `dispatchTwoJobBatch()`, preserving `batchOrder` with one comparator and resolving the two results directly instead of using `sort`, `map`, and result loops. A 2026-06-08 Node run moved generated request-precomputed batched `findUnique` from 7.66 to 7.19 us/op; Workerd moved from 9.20 to 9.10 us/op.
  - `packages/client-engine-runtime/src/batch.ts::convertCompactedRows()` intentionally has a single-key fast path for compacted batches. It pre-resolves the one key per requested argument, pre-deserializes the one key per row, compares values via `doKeyValuesMatch()` from `utils.ts` to preserve Decimal/Date/Bytes/BigInt semantics, and falls back to the generic path if an argument lacks the key. A 2026-06-08 Node run moved generated request-precomputed batched `findUnique` from 7.19 to 6.98 us/op; Workerd moved from 9.10 to 8.80 us/op.
  - Do not replace `canIgnorePrecomputedFastPathParamOverrides()`'s `Object.keys()` shape check with a `for...in` / `Object.hasOwn()` loop without new evidence. A 2026-06-08 spike measured generated request-precomputed batched `findUnique` at 7.02 us/op versus the accepted 6.98 us/op, so it was reverted.
  - Do not add a row-shape check to the single-key compacted batch fast path to return rows directly when `Object.keys(row)` matches the selection without new evidence. A 2026-06-08 spike passed focused tests but regressed generated request-precomputed batched `findUnique` to 7.73 us/op from the accepted 6.98 us/op.
  - Do not replace the `expectedValues.map()` inside the single-key compacted batch fast path with a manual result-array loop without new evidence. A 2026-06-08 spike passed focused tests but regressed generated request-precomputed batched `findUnique` to 7.40 us/op from the accepted 6.98 us/op.
  - Do not add optional `placeholderNames` to `PrecomputedQueryPlanCacheHit` just to avoid `Object.keys(hit.placeholderValues)` in `tryBuildPrecomputedBatchCacheHit()` without new evidence. A 2026-06-08 spike regressed generated request-precomputed batched `findUnique` to 7.16 us/op from the accepted 6.98 us/op.
  - Do not add a specialized `true` lazy-descriptor constant node without stronger evidence. A 2026-06-08 profile showed descriptor matching as visible in the generated nested row, but the `true` node specialization was mixed: request-precomputed blog-page moved 11.84 -> 11.77/11.86 in focused runs, while the adjacent broad A/B regressed the target blog-page row from 11.22 to 11.59 us/op.
  - Do not add a separate `ClientEngine.requestCachedResultByKey()` hook that takes `{ model, action, cacheKey, placeholderValues }` unless a new benchmark shows a larger win. A 2026-06-08 sidecar spike found it essentially tied with the existing `requestPrecomputedCachedResult()` direct surface on the target nested row (9.58 vs 9.59 us/op in the broad comparison), so the retained benchmark row calls `requestPrecomputedCachedResult()` directly instead.
  - Do not delay `ClientEngine.requestBatch()` `JsonBatchQuery` payload allocation by making `tryBuildPrecomputedBatchCacheHit()` read `queries`/`transaction` directly without new evidence. A 2026-06-08 spike regressed generated request-precomputed batched `findUnique` to 7.36 us/op from the accepted 6.98 us/op.
  - Do not skip `RequestHandler` traceparent computation just because `client._engine.name === 'ClientEngine'` without new evidence. A 2026-06-08 spike added that local-engine gate and a unit test, but generated request-precomputed batched `findUnique` regressed from the committed 7.19 us/op to 7.28 us/op, so it was reverted.
  - Do not add a one-placeholder/two-query special branch to `tryBuildPrecomputedBatchCacheHit()` without new evidence. A 2026-06-08 spike avoided the generic `cacheKeys`/`JSON.stringify([cacheKeys, transaction])` path for no-commenter/no-transaction two-hit batches, but generated request-precomputed batched `findUnique` regressed to 7.55 us/op from the committed 7.19 us/op.
  - Do not further micro-optimize `renamePrecomputedPlaceholders()` by direct-filling the combined placeholder map or by adding a separate one-placeholder no-`Map` recursive helper without new evidence. 2026-06-07 follow-up spikes built successfully but measured generated request-precomputed batched `findUnique` at 10.84 and 10.80 us/op, worse than the accepted identity-skip implementation's 10.28-10.62 us/op band.
  - Lazy descriptor `hasExactKeys()` intentionally accepts the common generated-object key order with an indexed equality fast path before falling back to order-independent `Object.hasOwn()` checks. A 2026-06-07 same-session A/B moved generated request-precomputed rows from 3.38 / 10.55 / 3.36 / 12.66 us/op reverted to roughly 3.36 / 10.27 / 3.35 / 12.37 us/op patched for single findUnique / batched findUnique / findMany users / nested blog-page, and Workerd stayed directionally positive.
  - Do not replace lazy descriptor `hasExactKeys()` with a `for..in` no-allocation exactness check without new evidence. A 2026-06-08 spike preserved own-enumerable semantics by calling `Object.hasOwn()` for each key, but generated engine-precomputed blog-page regressed from 10.87 to 11.64 us/op over 100k Node iterations and request-precomputed regressed from 12.16 to 12.57. The current `Object.keys()` plus ordered fast path is better on the real hot row.
  - Benchmark-only straight-line exact descriptor helpers for nested generated shapes remain a positive lead, but the win is modest and must stay descriptor-bound. A 2026-06-09 hard-coded `Post.findUnique` blog-page helper in `client-engine-cache-timing.ts` moved same-session Node generated blog-page request-precomputed / descriptor-bound static / exact-helper rows to 11.55 / 10.53 / 10.47 us/op over 100k iterations, and Workerd worker-loop request-precomputed / descriptor-bound static / exact-helper rows to 12.40 / 13.00 / 12.00 us/op over 5k iterations. This supports generator-emitted straight-line nested matchers with oracle coverage; it does not justify generic recursive matchers or enabling the helper by default.
  - Do not add a `RequestHandler.requestPrecomputedSingle()` direct-engine helper for non-batchable generated precomputed hits unless a new benchmark shows a larger win and the result mapping semantics are made explicit. A 2026-06-07 spike kept existing error mapping but skipped DataLoader/unpack for non-`findUnique` precomputed hits; it only moved generated request-precomputed `findMany users` from 3.35 to 3.31 us/op and regressed nested blog-page from 12.30 to 12.37 us/op, so it was reverted.
  - Do not add a separate `_requestPrecomputed()` helper that preserves `_request()`'s operation span and Node `AsyncResource` while bypassing serialization as a standalone middle path. A 2026-06-07 spike regressed generated request-precomputed rows to 4.09 / 12.06 / 13.40 us/op for single findUnique / batched findUnique / nested blog-page, worse than both direct `RequestHandler.request()` and the earlier `_request()`-based path.
  - Do not drop callsite capture as a standalone optimization for the request-layer precomputed path. A 2026-06-07 spike that skipped fluent/request helper callsite creation measured effectively unchanged CPU at 3.85 / 13.11 us/op, while weakening error context. Retrying after the direct-RequestHandler descriptor path was also not worth keeping: single `findUnique` improved only 3.49 -> 3.38 us/op, while batched `findUnique` and nested blog-page regressed to 11.28 and 12.74 us/op.
  - Do not cache the stable client-level generated precomputed fast-path guard booleans as a standalone cleanup. A 2026-06-07 spike moved request-precomputed rows to 3.45 / 10.90 / 12.85 us/op for single findUnique / batched findUnique / nested blog-page, versus the accepted direct-RequestHandler descriptor path at 3.49 / 10.59 / 12.39 us/op; the heavier rows regressed.
  - `ClientEngine.requestPrecomputedCachedResult()` intentionally returns the action result directly for generated engine-precomputed descriptor hits after looking up the cached plan in `QueryPlanCache`. It falls back to normal `request()` for raw queries, sqlcommenters, interactive transactions, or cache eviction. Prisma commit `f38529e4b` moved generated engine-precomputed rows over 50k iterations from 3.11 -> 2.83 us/op (`findUnique`), 5.70 -> 5.14 (`batched findUnique`), 3.34 -> 2.96 (`findMany users`), and 11.76 -> 11.11 (`blog page`). Keep this as a ClientEngine-owned path rather than a RequestHandler shortcut so RequestHandler batching/unpack/error semantics stay unchanged.
  - Do not add an opaque `QueryPlanCache` entry handle to `PrecomputedQueryPlanCacheHit` just to bypass `QueryPlanCache.getSingle(cacheKey)` without stronger evidence. A 2026-06-08 spike with eviction/clear invalidation was neutral-to-negative on sequential confirmation (engine blog-page 11.00 -> 11.06 us/op, engine `findMany` 2.82 -> 2.83) and only noise-level positive for request batched `findUnique` (7.17 -> 7.08). `QueryPlanCache` already has a repeated single-key fast path; a useful plan-handle design must skip more request routing/extraction work than the final key lookup.
  - Do not replace `tryExtractLazyDescriptor()`'s `{ ...precomputedQueryPlanCacheHit, placeholderValues }` return with direct stable fields as a standalone optimization. A 2026-06-08 spike improved one engine-precomputed nested row but regressed generated request-precomputed rows (`batched findUnique` 7.63 -> 8.01 us/op, `findMany` 3.23 -> 3.40, blog-page 12.01 -> 12.07), so the spread copy is not the current limiting cost.
  - Do not cache `action` / `isWrite` metadata on `applyModel.ts` lazy descriptors as a standalone optimization. A 2026-06-08 spike measured generated engine-precomputed blog-page at 11.66 us/op patched versus 11.64 us/op reversed, so it was neutral noise.
  - Do not compile `applyModel.ts` learned lazy descriptors into per-node closure matchers without new evidence. A 2026-06-08 spike replaced the generic recursive `matchesLazyDescriptorNode()` switch with shape-specific closures, but generated engine-precomputed rows were worse/noisy (`findUnique` 3.04, batched 5.40, `findMany` 3.03, blog-page 11.77 us/op) than the accepted direct-result path.
  - Do not compile learned lazy descriptors into a generic path/op-table matcher with independent root-to-leaf checks. A 2026-06-08 benchmark-only spike preserved exactness semantics but regressed nested blog-page descriptor extraction from 2.54 to 2.91 us/op because repeated path lookup cost more than recursive descriptor traversal.
  - In `packages/client-engine-runtime/src/parameterization/parameterize.ts`, `Object.keys()` based copy-on-write traversal benchmarked faster than guarded `for...in` traversal despite the extra key-array allocation. Do not replace those loops with `for...in` for allocation reasons unless a new benchmark proves it.
  - Do not reorder `parameterize.ts` selection traversal from `key !== '$scalars' && key !== '$composites' && typeof value !== 'boolean'` to `typeof value !== 'boolean' && ...` without new evidence. A local spike slightly helped some ad hoc timing rows but lost the close `caching.bench.ts` A/B across parameterization and cache-hit-key rows.
  - `parameterize.ts` defers `ParamGraph.inputNode()` / `outputNode()` lookups until the current field selection actually has arguments/selection. Preserve this when touching parameterization: it benchmarked as a small cache-key hot-path win, especially for nested selections without arguments.
  - `parameterize.ts` handles primitive scalar placeholders with a direct type/mask branch instead of routing through `getPrimitivePlaceholderType()` / `matchesPrimitiveMask()`. Preserve the rare `ScalarMask.Json` primitive stringification behavior if touching this path.
  - Lazy caching of `ParamGraph.inputNode()` / `outputNode()` wrapper objects produced mixed/noisy `query-performance/caching.bench.ts` results and did not clearly improve nested parameterization. The accepted hot path is different: `parameterize.ts` uses `ParamGraph.rootData()` / `inputEdgeData()` / `outputEdgeData()` / `enumValuesByIndex()` to traverse by node id and raw readonly edge data without allocating wrapper objects during each request.
  - `ParamGraph.rootDataFor(modelName, action)` keeps a constant-memory last-root lookup cache so repeated parameterization of the same model/action avoids rebuilding the `"Model.action"` root key. Do not replace this with a full model/action root index without new retained-memory evidence; that variant added per-root maps/strings and only gave noisy product-path wins.
  - Do not compact query-protocol input placeholder values from `{ name, type }` objects into tuple payloads without stronger evidence. A local spike emitting scalar/list placeholders as `[name, type]` / `[name, ["List", inner]]` and adding Rust parser support saved only about 14 cache-key bytes per scalar placeholder and did not materially improve focused cache-key timing (`blog page` 3.37 us/op legacy vs 3.35 us/op compact over 50k iterations), while adding protocol/test churn.
  - `packages/client/src/__tests__/benchmarks/query-performance/client-engine-cache-timing.ts` has focused `compile prebuilt request ...` and `compile current miss ...` rows for Client-to-Wasm compile-boundary work. A 2026-06-07 run showed JS request construction/cache-key work is only single-digit microseconds on cache-hit shapes, while full Wasm compile misses are hundreds-to-thousands of microseconds; a JS-owned query rewrite is mainly a cache-hit/memory-boundary lead unless it also removes Rust-owned protocol parsing/adaptation work.
  - `packages/client/src/__tests__/benchmarks/query-performance/client-engine-cache-timing.ts` has `raw result-set blog page assembly / nested rows`, a fixture-specific lower-bound row for mapping/joining nested query-mode results directly from raw `SqlResultSet`s. A local 2026-06-07 run measured about 0.5 us/op versus about 9.2 us/op for the current inner nested plan and about 11.8 us/op for local executor. Treat this as evidence for a larger raw-result-set mapping/joining plan shape that removes serialized intermediate row objects, generic join attachment, and outer data-map work; do not use it to justify more tiny helper rewrites around the current serialized-row plan.
  - `client-engine-cache-timing.ts` also has `raw result-set prototype blog page / nested rows`, a generic benchmark-only executor that still uses compiled DB query leaves, `renderQuery()`, and seven adapter `queryRaw()` calls. It measured about 5.5 us/op versus about 12 us/op for local executor on the same fixture, so the next production slice should be a compact raw-nested-read plan node/protocol plus Rust emission from query-mode nested `DataMap`/`add_inmemory_join()`, not another current-interpreter helper tweak.
  - `@prisma/client-engine-runtime` supports compact raw nested read nodes shaped as `['n', rawNestedReadQuery, unique, enums?]`. Raw mappings use numeric column indexes plus required field-type metadata on this internal boundary, with nested field paths such as `_count.comments`; named column refs and missing field-type fallback were removed after the 2026-06-10 producer scan showed 89/89 full-corpus raw-nested mappings were numeric with 0 missing field types. The interpreter caches resolved mapping descriptors plus scalar conversion kinds by mapping array and returns `[]` before resolving mappings when a result set has zero rows. `query-compiler` can emit these nodes for direct query-mode nested reads, falling back for in-memory processing, validation wrappers, and composite relation keys; safe implicit many-to-many shapes now use `build_read_m2m_query()` plus the hidden linking alias as the child column. A local 2026-06-07 Wasm run on the blog-page fixture measured modest single-digit wins (cached request wrapper about 15.8-16.8 us/op vs prior about 17.7; local executor about 10.2-10.9 vs prior about 11.8), while the flattened raw prototype still measured about 5.4-5.5 us/op. Treat Rust-emitted `n` as correctness-preserving groundwork; the next win requires reducing exact-shape object allocation/relation attachment overhead inside `n`, not merely emitting it.
  - In query-compiler raw nested read translation, column indexes are compile-time-only lookup state. Engines commit `e98dd7b4193` replaced escaping `HashMap<String, usize>` maps with a borrowed local `RawColumnIndexes<'_>` vector and returns the child join column index directly from `build_raw_read_related_records()`. Do not reintroduce owned column-index maps unless a new profile shows the linear borrowed lookup is wrong for larger selected-column shapes.
  - `packages/client/src/__tests__/benchmarks/query-performance/query-plan-cache-memory.ts` supports `QUERY_PLAN_CACHE_MEMORY_RENDER=1` to render every retained plan's DB query leaves once before measuring heap and understands compact raw nested read (`n`) plans. A corrected 2026-06-07 local-Wasm run showed a blog-page raw nested plan is about 4.1 KiB serialized with 7 DB query leaves, 1 raw nested node, and SQL strings still dominating. Rendering 1,000 retained blog-page plans used about 17.96 MiB before the two-hit flat SQL render cache and about 16.62 MiB after it. `render-query.ts` now retains full flat rendered SQL only after a query object is rendered twice; this lowers cold-shape memory while preserving hot repeated-plan rendering speed after the second render.
  - `QueryPlanCache` interns repeated child raw-nested subtrees and long strings inside compact raw nested read (`n`) plans. This recovered cache-memory sharing that raw nested emission had bypassed: a local 2026-06-07 probe dropped 1,000 retained blog-page plans from about 16.8 MiB to 5.3 MiB without render, and from about 16.6 MiB to 5.3 MiB with `QUERY_PLAN_CACHE_MEMORY_RENDER=1`. Keep `query-plan-cache.test.ts` coverage for raw nested child subtree sharing if editing cache interning.
  - Do not split cached template SQL string fragments at `FROM` boundaries as a post-processing memory optimization. A local spike in `QueryPlanCache` worsened retained blog-page plans after raw nested subtree interning (node-default warm 5.27 -> 5.45 MiB without render; 5.29 -> 5.46 MiB with render), likely because extra fragment arrays/strings outweighed any shared suffix. If root SQL ownership remains a target, work at the compiler/protocol level with reusable SQL template/string handles instead.
  - In `prisma-engines/query-compiler/query-compiler/src/expression.rs`, do not change `Expression::Validate.rules` from `Vec<DataRule>` to `SmallVec<[DataRule; 1]>` without new CPU evidence. A local allocation spike saved only 2–5 allocations on nested write translation/full compile and regressed `compile/update-set-nested-prisma#27650` by about 2.7% in Criterion.
  - Do not change `Expression::Let.bindings` from `Vec<Binding>` to inline `SmallVec<[Binding; 1]>`: `Binding` owns an `Expression`, so inline storage makes `Expression` recursively contain itself without indirection and fails to compile. Boxing the binding expression or binding list would add allocations back, so this is not a useful expression-container cleanup.
  - In `packages/client-engine-runtime/src/interpreter/render-query.ts`, a primitive fast path inside `evaluateArgs()` benchmarked slower across interpreter scenarios; keep the straight `evaluateArg(args[i], ...)` loop unless new evidence changes.
  - In `packages/client-engine-runtime/src/interpreter/query-interpreter.ts`, single-column strict join attachment has two specializations. Large joins use scalar string keys once parent+child rows are above the threshold; keep that threshold because applying the scalar-key map path to tiny joins regressed the `interpreter: join (1:N)` benchmark. Tiny strict single-key joins use a direct nested equality loop instead of the generic `JSON.stringify([key])` path; preserve the focused collision coverage for both paths.
  - Do not split the tiny strict join direct loop into wrapper-array-free helper calls without stronger evidence. A follow-up avoided `[parentRecords]` / `[childRecords]` wrapper arrays and looked fine in interpreter microbench, but the rebuilt product probe was flat on cached wrapper rows and worse on direct nested-plan rows.
  - `packages/client-engine-runtime/src/interpreter/data-mapper.ts` caches object-data field descriptors in `objectFieldMappingsCache`, separate from result-set column mappings. This avoids recomputing db names, scalar type names, list flags, and nested object descriptors while applying the outer `dataMap` over already-materialized nested results. Keep `data-mapper.test.ts`, `query-interpreter.test.ts`, `interpreter.bench.ts`, and `client-engine-cache-timing.ts` coverage if editing this path.
  - Do not add pre-switch primitive scalar fast paths in `mapMappedField()` / `mapResultSetField()` for already-typed `string` / `int` / `float` / `boolean` values unless new evidence overturns it. A local A/B preserved fallback behavior but slowed direct data-map rows (`outer data map` 1.62 -> 1.70 us/op, precomputed data map 2.26 -> 2.30 us/op) and regressed the nested cached wrapper on rerun.
  - Do not reuse a mutable no-commenter `LocalExecutor` runtime-options object just to avoid per-request object allocation. A local A/B was mixed (`findUnique` 2.38 -> 2.30 us/op, stable `findMany` 2.48 -> 2.62, blog value churn 4.95 -> 4.80, nested rows 18.75 -> 18.71) and the subtle async-copy assumption is not worth the complexity.
  - `client-engine-cache-timing.ts` has an `interpreter unit` row that runs a compact `unit` node through `QueryInterpreter.run()` with stable runtime options. A local Node/V8 probe measured this floor under 1 us/op, so do not chase generic interpreter run setup before larger cached-plan execution/data-shape and wrapper/cache-key surfaces.

- **Adding PrismaClient constructor options**:
  - Runtime types: `PrismaClientOptions` in `packages/client/src/runtime/getPrismaClient.ts`.
  - Validation: `packages/client/src/runtime/utils/validatePrismaClientOptions.ts` (add to `knownProperties` array and `validators` object).
  - Engine config: `EngineConfig` interface in `packages/client/src/runtime/core/engines/common/Engine.ts`.
  - Generated types: Update both `packages/client-generator-js/src/TSClient/PrismaClient.ts` (`buildClientOptions` method) and `packages/client-generator-ts/src/TSClient/file-generators/PrismaNamespaceFile.ts` (`buildClientOptions` function).
  - Use `@prisma/ts-builders` for generating TypeScript type declarations.
  - Current known options: `errorFormat`, `adapter`, `accelerateUrl`, `log`, `transactionOptions`, `omit`, `comments`, `__internal`.

- **Creating new packages**:
  - Create directory under `packages/`, add `package.json`, `tsconfig.json`, `tsconfig.build.json`, and `helpers/build.ts`.
  - Package is auto-discovered via `pnpm-workspace.yaml` glob `packages/*`.
  - For type-only packages, use `bundledConfig` from `helpers/compile/configs.ts`.
  - Add as dependency to consuming packages using `"workspace:*"` version.
  - **Important**: Add resolution path to `tsconfig.build.bundle.json` under `compilerOptions.paths` for go-to-definition to work in editors.
  - **Important**: If the package has tests, add it to `.github/workflows/test-template.yml`. Utility packages typically go in:
    - `others` job (Linux, no Docker): includes `debug`, `generator-helper`, `get-platform`, `fetch-engine`, `engines`, `instrumentation`, `instrumentation-contract`, `schema-files-loader`, `config`, `dmmf`, `generator`, `credentials-store`, `sqlcommenter-*` packages.
    - `client-packages` job: includes `client-common`, `client-engine-runtime`, `ts-builders`, `client-generator-js`, `client-generator-ts`, `client-generator-registry`.
    - `driver-adapter-unit-tests` job: includes `adapter-libsql`, `adapter-mariadb`, `adapter-d1`, `adapter-pg`, `adapter-planetscale`, `adapter-mssql`, `adapter-neon`.
    - `no-docker` job (Windows/macOS): mirrors the `others` and `client-packages` jobs for cross-platform testing.

- **Prisma 7 direction**: Migration from `schema.prisma` datasource URLs / `env()` to `prisma.config.ts`. Commands, tests, and fixtures should read connection settings from `PrismaConfigInternal.datasource` (or driver adapters) rather than CLI flags or environment loading. SQLite datasource URLs now resolve relative to the config file and not to the schema.
- **Test helpers**: `ctx.setConfigFile('<name>')` (from `__helpers__/prismaConfig.ts`) overrides the config used for the next CLI invocation and is automatically reset after each test, so no explicit cleanup is needed. Many migrate fixtures now provide one config per schema variant (e.g. `invalid-url.config.ts` next to `prisma/invalid-url.prisma`) and tests swap them via `ctx.setConfigFile(...)`. `ctx.setDatasource`/`ctx.resetDatasource` continue to override connection URLs when needed.

- **CLI commands**: Most commands already accept `--config` for custom config paths. Upcoming work removes `--schema` / `--url` in favour of config-based resolution. When editing CLI help text, keep examples aligned with new config-first workflow.
  - For isolated Studio verification, you can run `packages/cli/src/Studio.ts` directly via `pnpm exec tsx` and pass a config object that preserves `loadedFromFile`; this keeps SQLite URLs resolving relative to the config file while avoiding unrelated `packages/cli/src/bin.ts` imports.
  - Studio is now pre-bundled into `packages/cli/build/studio.js` and `packages/cli/build/studio.css`, served only through explicit routes in `packages/cli/src/Studio.ts` via the runtime-specific `packages/cli/src/studio-server.ts` bindings, and should keep listener-level coverage in `packages/cli/src/__tests__/studio-server.vitest.ts` because a past Node regression dropped `GET` bodies by treating them like `HEAD`.
  - If Enter or click does not open a cell editor in Studio, verify that the current table and column are writable before assuming a keyboard regression; views/system tables and read-only columns legitimately stay non-editable.

- **Driver adapters datasource**:
  - Helper `ctx.setDatasource()` in tests overrides config.datasource for connection-specific scenarios.

- **Testing patterns**:
  - Tests rely on fixtures under `packages/**/src/__tests__/fixtures`; many now contain `prisma.config.ts`.
  - Default Jest/Vitest runner is invoked via `pnpm --filter @prisma/<pkg> test <pattern>`; it wraps `dotenv` and expects `.db.env`.
    - Some packages already use Vitest, `packages/cli` uses both for different tests as it's in the process of transition, older packages still use Jest.
  - Functional generated clients in `packages/client/tests/functional/**/.generated` import `packages/client/runtime/client.js` directly; runtime changes in `src/runtime` may need corresponding runtime bundle updates to be exercised by functional tests.
  - Client e2e `_steps.ts` files run inside `packages/client/tests/e2e/_utils/standard.dockerfile`; the startup script exports `NODE_PATH` for CommonJS and symlinks globally installed `zx` into `/test/node_modules` because ESM package resolution ignores `NODE_PATH`. Linux Docker runs may need SELinux-compatible bind mounts (`:z`) for mounted e2e files to be readable in the container.
  - Inline snapshots can be sensitive to formatting; prefer concise expectations unless the exact message matters.

- **Environment loading**: Prisma 7 removes automatic `.env` loading.

- **Driver adapter error handling**:
  - Database errors are mapped in each adapter's `errors.ts` (e.g., `packages/adapter-pg/src/errors.ts`).
  - `MappedError` type in `packages/driver-adapter-utils/src/types.ts` defines all known error kinds.
  - Known error kinds include: `GenericJs`, `UnsupportedNativeDataType`, `InvalidIsolationLevel`, `LengthMismatch`, `UniqueConstraintViolation`, `NullConstraintViolation`, `ForeignKeyConstraintViolation`, `DatabaseNotReachable`, `DatabaseDoesNotExist`, `DatabaseAlreadyExists`, `DatabaseAccessDenied`, `ConnectionClosed`, `TlsConnectionError`, `AuthenticationFailed`, `TransactionWriteConflict`, `TableDoesNotExist`, `ColumnNotFound`, `TooManyConnections`, `ValueOutOfRange`, `InvalidInputValue`, `MissingFullTextSearchIndex`, `SocketTimeout`, `InconsistentColumnData`, `TransactionAlreadyClosed`, and database-specific kinds (`postgres`, `mysql`, `sqlite`, `mssql`).
  - `convertDriverError()` in each adapter maps database-specific error codes to `MappedError` kinds.
  - `rethrowAsUserFacing()` in `packages/client-engine-runtime/src/user-facing-error.ts` maps `MappedError` kinds to Prisma error codes (P2xxx).
  - To add a new error mapping: (1) add kind to `MappedError` in driver-adapter-utils, (2) map database error code in relevant adapter(s), (3) add Prisma code mapping in `getErrorCode()` and message in `renderErrorMessage()`.
  - Raw queries (`$executeRaw`, `$queryRaw`) use `rethrowAsUserFacingRawError()` which always returns P2010; regular Prisma operations use `rethrowAsUserFacing()`.
  - When no specific mapping exists for a database-specific kind (`postgres`, `mysql`, `sqlite`, `mssql`) — i.e. the adapter didn't recognize the underlying DB error code — `rethrowAsUserFacing()` falls back to a P2039 `UserFacingError` with the message `Database error. Code: <originalCode>. Message: <originalMessage>` carrying the raw `originalCode` / `originalMessage`. This keeps the DB error surface as `PrismaClientKnownRequestError` locally and as an HTTP 400 from the query plan executor (instead of HTTP 500, which Accelerate strips), so that schema-drift-style failures (stale migrations, stale generated client, etc.) remain debuggable. Raw queries keep the historical P2010 (`Raw query failed. Code: <originalCode>. Message: <originalMessage>`) via `rethrowAsUserFacingRawError()`; P2039 is used for non-raw queries so the message doesn't claim a regular Prisma operation was a raw query. Truly unknown `kind` values still fall through to `assertNever` so new driver-adapter variants surface clearly during development.
  - Prisma error codes currently assigned outside the documented public Error Reference (P2000–P2037) are: **P2038** — used for the `PrismaClientInitializationError` raised by `ClientEngine` when no driver adapter is configured (see `CLIENT_ENGINE_ERROR` in `packages/client/src/runtime/core/engines/client/ClientEngine.ts`); **P2039** — used for unmapped database-specific driver-adapter errors as described above. Pick the next available code (P2040, …) for any new additions and document it here.

- **SQL Commenter packages**:
  - `@prisma/sqlcommenter`: Core types (`SqlCommenterPlugin`, `SqlCommenterContext`, `SqlCommenterQueryInfo`, `SqlCommenterTags`) for building sqlcommenter plugins.
  - `@prisma/sqlcommenter-query-tags`: AsyncLocalStorage-based plugin for adding ad-hoc tags via `withQueryTags()` and `withMergedQueryTags()`.
  - `@prisma/sqlcommenter-trace-context`: Plugin for adding W3C Trace Context `traceparent` header to queries.
  - `@prisma/sqlcommenter-query-insights`: Plugin for adding parameterized query shapes to comments (format: `Model.action:base64Payload`).
  - Plugins are registered via `PrismaClient({ comments: [plugin1(), plugin2()] })`.
  - E2E tests for sqlcommenter plugins live in `packages/client/tests/e2e/sqlcommenter*` directories.
  - `SqlCommenterQueryInfo` distinguishes `type: 'single'` (single query) vs `type: 'compacted'` (batched queries merged into one SQL statement). For non-raw client-engine queries, the SQL commenter context should receive parameterized query payloads so plugins such as query-insights never see user data values.

- **Codebase helpers to know**:
  - `@prisma/internals` exports CLI utilities: `arg`, `loadSchemaContext` (less used now).
  - `packages/migrate/src/__tests__/__helpers__/context.ts` sets up Jest helpers including config contributors.
  - `packages/config` defines `PrismaConfigInternal`; inspect when validating config assumptions.
  - `@prisma/ts-builders` provides a fluent API for generating TypeScript code (interfaces, types, properties with doc comments).
  - `@prisma/driver-adapter-utils` defines core interfaces: `SqlQuery`, `SqlQueryable`, `SqlDriverAdapter`, `SqlDriverAdapterFactory`, `SqlMigrationAwareDriverAdapterFactory`, `MappedError` for error handling, `ConnectionInfo`, `Provider` type (`'mysql' | 'postgres' | 'sqlite' | 'sqlserver'`).
  - `@prisma/client-engine-runtime` exports query interpreter, transaction manager, and related utilities.
  - `@prisma/client-common` provides shared client utilities used by both generators and runtime.
  - `@prisma/client-runtime-utils` provides utility types and singletons for Prisma Client.
  - Query-plan SQL `argTypes` are forwarded to driver adapters; `dbType` is optional in `@prisma/driver-adapter-utils`, so absent native database types should be omitted from Wasm plan serialization rather than emitted as `dbType: null`.
  - Query-plan compact template SQL `argTypes` serialize scalar/no-native-DB parameter types as one-letter scalar strings (for example `"i"` or `"I"`) and scalar/native-DB parameter types as `[compactScalarType, dbType]` tuples (for example `["i", "INTEGER"]`). Full canonical `ArgType` objects are still valid for raw SQL and list/tuple/object arg-type cases. The client-engine-runtime renderer expands compact arg types back to adapter-facing `{ arity: 'scalar', scalarType, dbType? }` objects before calling driver adapters; tuple arg-type elements need the same normalization.
  - Compact query-plan scalar type aliases are: `s` string, `i` int, `I` bigint, `f` float, `d` decimal, `b` boolean, `e` enum, `u` uuid, `j` json, `D` datetime, `B` bytes, `?` unknown, `o` object, and `x` unsupported. Do not reintroduce canonical scalar-string support in compact template SQL `argTypes`; adapter-facing object shapes and result field metadata should still receive/accept canonical `scalarType` strings where those object-shaped forms are in use.
  - Query-plan SQL `templateSql` queries serialize only as the compact tuple `[fragments, [prefix, hasNumbering], args, argTypes, chunkable]`. Raw SQL remains object-shaped. Do not add legacy object-shaped `templateSql` compatibility on this internal boundary.
  - Query-plan SQL `templateSql.fragments` serialize scalar parameter placeholders as `null`, tuple placeholders as `["T", itemPrefix, itemSeparator, itemSuffix]`, and tuple-list placeholders as `["L", itemPrefix, itemSeparator, itemSuffix, groupSeparator]`. SQL string chunks are raw strings; do not add legacy tagged-object fragment compatibility on this internal boundary.
  - Query-plan `PrismaValue::Placeholder` values serialize only as `{ "$p": [name, type] }`. `PrismaValue::GeneratorCall` values serialize only as `{ "$g": [name, args] }`. Raw-parameter `prisma__type` serialization is a separate external path and should not be confused with query-plan PrismaValue internals.
  - Compacted batch response arguments also use only query-plan `{ "$p": [name, type] }` placeholders. Do not reintroduce JSON-protocol `{ "$type": "Param" }` compatibility in `batch.ts`; a 2026-06-10 sqlite/postgres/mysql producer-shape scan found 0 JSON `Param` placeholders and 12 compact `$p` placeholders in sampled compacted batches, and generated batched `findUnique` stayed healthy after deletion.
  - Query-plan expression nodes serialize only as compact tagged tuples; do not add legacy `{ type, args }` compatibility on this internal boundary. Current compact tags are `v` value, `s` seq, `g` get, `l` let, `e` get-first-non-empty, `q` query, `x` execute, `R` reverse, `+` sum, `c` concat, `u` unique, `r` required, `j` join, `m` map-field, `t` transaction, `d` data-map, `n` raw-nested read, `V` validate, `?` if, `0` unit, `-` diff, `i` initialize-record, `M` map-record, and `p` process.
  - Compact `dataMap` nodes may omit the fourth `enums` tuple slot when the enum map is empty; the TS interpreter must default missing compact enums to `{}`.
  - Query-plan support structures serialize only as compact tuples: let-bindings as `[name, expr]` and join expressions as `[child, on, parentField, isRelationUnique]`; do not add legacy object-shaped binding/join compatibility on this internal boundary.
  - Query-plan result object nodes serialize only as compact tuples: `[serializedName, fields]` when `skipNulls` is false and `[serializedName, fields, true]` when `skipNulls` is true; do not add legacy `{ type: 'object', serializedName, fields, skipNulls }` compatibility on this internal boundary.
  - `QueryPlanCache` interning walkers intentionally only special-case compact tuple tags (`j`, `n`, `d`, etc.) and generic plain-object traversal for maps inside compact plans. Do not reintroduce object-shaped `query` / `execute` / `join` / `dataMap` / result-object compatibility branches; a 2026-06-10 cleanup removed the remaining stale branches with `query-plan-cache.test.ts`, `@prisma/client` build, cache-memory, and compile-current-miss evidence.
  - Query-plan validation `DataRule`s serialize only as `["=", count]` for `rowCountEq`, `["!", count]` for `rowCountNeq`, `["a", count]` for `affectedRowCountEq`, and `"n"` for `never`; do not add legacy `{ type, args }` rule compatibility on this internal boundary.
  - Query-plan validation errors serialize only as compact identifiers and contexts: `r` relation violation as `[relation, modelA, modelB]`, `m` missing related record as `[model, relation, relationType, operation, neededFor?]`, `M` missing record as the operation string, `i` incomplete connect input as `expectedRows`, `o` incomplete connect output as `[expectedRows, relation, relationType]`, and `n` records not connected as `[relation, parent, child]`. Use `getValidationError()` before calling `performValidation()`.
  - Query-plan process-node `InMemoryOps` may omit default fields. The TS interpreter treats missing `pagination` / `distinct` / `linkingFields` as `null`, missing `reverse` as `false`, missing `nested` as `{}`, and missing pagination `cursor` / `take` / `skip` as `null`.
  - Raw nested read sibling relations should be started concurrently, matching compact join behavior. `QueryInterpreter.#compileRawNestedReadQuery()` intentionally keeps a single-relation direct await fast path but uses `Promise.all` for two or more relations; an async-boundary benchmark showed raw nested compact-node blog-page execution improving from 31.38 us/op to 22.86 us/op, while the immediate fake-adapter CPU row regressed only from 7.62 us/op to 7.76 us/op. Use `CLIENT_ENGINE_CACHE_TIMING_ASYNC_BLOG_PAGE_ADAPTER=1 CLIENT_ENGINE_CACHE_TIMING_FILTER='blog page / nested rows' pnpm exec node --expose-gc --import tsx packages/client/src/__tests__/benchmarks/query-performance/client-engine-cache-timing.ts` to validate this tradeoff.
  - Raw nested direct and many-to-many relation attachment switches from linear scans to key indexes only once relation inputs reach `RAW_NESTED_INDEX_THRESHOLD` (currently 8). Keep tiny inputs on the linear path; preserve first-child-wins behavior for unique relations and fresh per-parent arrays for list relations. A synthetic 100-parent raw nested shape improved from 208.0 us/op to 79.7 us/op after indexing, while the tiny blog-page compact-node row stayed in band at ~7.7 us/op.
  - Do not inline `mapRawNestedFieldValue()`'s scalar conversion switch directly into `mapRawNestedRows()` without a broader mapper specialization. A local spike only moved raw result-set compact node blog-page timing from 7.53 to 7.50 us/op while worsening the product-shaped cached request wrapper from 14.88 to 15.00 us/op.
  - Do not add a special two-row fast path to `getRawNestedScopeValue()` without stronger evidence. A local spike avoided the `Set` path for exactly two parent rows, but broad stable/fresh metadata runs were mixed and exact compact-node A/B was noise-level (stable 7.89 vs 7.84 us/op, fresh 13.14 vs 13.37 us/op).
  - Do not replace raw nested sibling `Promise.all(relations.map(...))` with a manually filled promise array unless new product evidence appears. A local compact-node A/B was noise-level (manual 7.91 us/op vs baseline 7.96 us/op at 100k iterations) and not worth changing the hot path.
  - Do not add a zero-mapping fast path to `mapRawNestedRows()` just for exact-wrapper raw nested nodes such as `tags: [{ tag: ... }]` without new evidence. A direct-plan blog-page A/B was neutral-to-negative (patched 8.92 us/op vs reverted 8.89 us/op at 300k iterations).
  - Raw nested exact-wrapper nodes with no scalar mappings and one unique direct child relation, such as compiler-emitted `tags: [{ tag: ... }]`, use a specialized `QueryInterpreter` execution path that maps `{ tag: childOrNull }` directly. Keep the specialization narrow: broader zero-field mapper fast paths were neutral-to-negative, while the exact-wrapper specialization moved direct-plan blog-page nested rows from the recent 8.89-8.92 us/op baseline to 8.65-8.71 us/op in local 2026-06-07 probes.
  - Raw nested relation child scopes may use a local one-key object only when the relation subtree references no placeholders outside the relation-supplied placeholder. Preserve the fallback to inherited `Object.create(parentScope)` scopes for child queries that also need outer user placeholders or generator argument placeholders. A local 2026-06-07 A/B moved blog-page nested rows from cached wrapper/direct/raw-compact/local 14.16/8.56/7.42/8.64 us/op to 12.28/7.89/6.38/7.85, with regression coverage for a child query using both `@parent$id` and `%tenantId`.
  - Raw nested unique-wrapper child scopes use the same local-scope guard as regular relation scopes. This matters for compiler-emitted exact-wrapper shapes such as `tags: [{ tag: ... }]`: a same-session 2026-06-07 A/B moved direct/local blog-page nested rows from about 8.01/8.00 us/op reverted to 6.85/6.86 patched, while a wrapper-relation test preserves inherited scope for `@parent$tagId` plus outer `%tenantId`. The Workerd generated-client blog-page upper bound stayed coarse and neutral around 110 us/op.
  - Do not replace local raw-nested scope construction from `{}` plus `scope[name] = value` with a computed property literal `{ [name]: value }`. A local 2026-06-07 A/B regressed blog-page nested rows from cached wrapper/direct/raw-compact/local 10.95/6.92/6.44/6.90 us/op reverted to 11.73/7.49/7.10/7.52 patched.
  - Do not precompile numeric raw nested row-mapper closures just to move `resolveRawResultColumnMappings()` / WeakMap lookup out of the execution loop. Local direct-plan blog-page nested-row probes regressed or stayed product-neutral despite improving a synthetic compact-node lower bound: an unrolled no-conversion mapper moved raw compact 7.32 -> 7.13 us/op but worsened cached wrapper/local rows (14.12/8.63 -> 14.30/8.75), and a converted mapper variant regressed cached wrapper/local to 15.07/9.10. Revisit only as part of a broader generated/exact-shape executor that removes more per-field and relation-shape work.
  - Do not unroll the direct raw nested row mapper by fixed mapping widths as a standalone optimization. A sidecar spike for widths 0/2/3/5/7 regressed the product-relevant raw result-set compact blog-page row from 6.00 to 6.32 us/op and left the exact compact row neutral at 6.04 vs 6.02 us/op. The next useful raw nested runtime lead is a full-tree exact assembler or compiler-emitted shape executor, not lower-level row-width unrolling.
  - Do not add a `path.length === 2` fast path to `setRawNestedPath()` just for `_count.likes` / `_count.comments`. A local focused direct-plan row improved slightly (8.81 -> 8.71 us/op), but broader product-shaped rows regressed while patched (cached request wrapper 14.78 us/op, local executor 9.02) and recovered after revert (14.61 / 8.98). Treat this as too small unless part of a generated/exact-shape raw mapper.
  - Do not add a separate plain-field raw nested mapper just to avoid the `typeof fieldNameOrPath` branch for leaf rows. A local 2026-06-07 spike over the actual blog-page raw nested plan was mixed: patched broad rows were cached request wrapper 14.47 us/op, direct 8.71, raw compact 7.55, local executor 8.96; reverted rows were 14.63, 8.91, 7.65, and 8.90. The end-to-end signal is too small unless part of a generated/exact-shape mapper.
  - Do not change DateTime cloning from `new Date(value)` to `new Date(value.getTime())` as a standalone raw nested/data-mapper optimization. A local 2026-06-07 spike passed focused tests but was mixed/noisy on blog-page nested rows (cached wrapper 14.56 us/op, direct 8.76, raw compact 7.59, local executor 9.01; focused direct 8.84 at 300k).
  - Do not replace the raw nested compiled query/relation closure tree with a method-driven data-object executor as a standalone optimization. A local 2026-06-07 spike passed `query-interpreter` tests but focused direct-plan blog-page nested rows stayed at 8.85 us/op, and the broad product row was mixed (cached wrapper 14.33, direct 8.80, raw compact 7.39, local executor 8.97). The exact raw-result-set prototype's 5.2 us/op ceiling likely needs a generated/exact-shape executor, not a generic data-object interpreter.
  - Do not add a unique-root direct-relation raw nested executor as a standalone optimization. A local spike passed `query-interpreter` tests but worsened same-session product rows (patched direct/local blog-page nested rows 8.68/8.87 us/op vs reverted 8.57/8.63). The extra closure/concurrency shape outweighed skipping root `getRawNestedScopeValue()` / generic direct attachment.
  - Do not skip `.slice()` in indexed raw nested list-relation attachment just because `parentRows.length === 1` unless a real product profile shows the grouped-array copy is material. A synthetic one-parent/100-child direct relation improved only from 5.00 to 4.93 us/op, and the equivalent many-to-many shape was neutral at 11.71 vs 11.70 us/op; preserving fresh per-parent arrays is more important than this tiny synthetic signal.
  - Query compiler raw nested emission serializes only numeric `RawResultColumnRef::Index` values and required raw-mapping field types. Do not reintroduce named column-ref or missing-field-type fallback in the TS runtime: the 2026-06-10 fixture-corpus producer scan compiled 60 PostgreSQL query-compiler fixtures with 89/89 raw-nested mappings numeric, 0 string refs, and 0 missing field types. Earlier compiler-emitted indexes reduced the blog-page serialized plan from about 4.1 KiB to 3.7 KiB and node-warm retained plan heap from about 5.27 MiB to 4.88 MiB in local 2026-06-07 probes.
  - `query-compiler/src/translate.rs::translate()` builds `map_result_structure(&graph, ...)` before root translation and discards it for `Expression::RawNestedRead`. Do not simply move `map_result_structure()` after translation: `NodeTranslator::translate_query()` plucks nodes from the graph. Do not add a clone-based precheck either: it lowered raw-nested read allocation counts but increased or mixed allocated bytes because cloning the root `ReadQuery` outweighed the skipped generic data map. A safe compile-allocation optimization needs either an exact no-clone pre-translation eligibility predicate or a consume-with-rollback refactor.
  - Do not prioritize the raw-nested `map_result_structure()` skip as the next Rust allocation project unless a broader translation refactor is already underway. A 2026-06-09 refreshed measurement-only patch that forced `structure = None` bounded the possible savings at about 17-35 allocations/op and 1.3-3.5 KiB/op on `query-m2o`, `query-o2m`, `query-m2m`, `query-many-m2m`, `query-many-one2m`, and `nested-pagination-query`; useful, but low-ceiling compared with graph-build/translate ownership work. A real implementation would need consume-once raw-nested eligibility/translation or delayed result-map construction with preserved graph/query data.
  - Do not land a standalone pre-translation raw-nested result-map skip guarded by a borrowed "will translate to raw nested" predicate without new timing evidence. A 2026-06-09 implementation saved about 17-30 allocations/op and 1.8-3.1 KiB/op on eligible raw-nested reads, but close Criterion A/B regressed fallback/write guard rows (`nested-pagination-query` 31.39 -> 33.36 us, `update-set-nested` 102.79 -> 107.07 us) while only improving `query-many-one2m` (32.78 -> 31.92 us). Keep this for a larger translation redesign, not as a micro-patch.
  - Do not pre-size `pairs_to_scalar_selections()` with `Vec::with_capacity(pairs.len())` as a standalone graph-build allocation cleanup. A 2026-06-09 spike passed `cargo check` but produced no allocation-count movement in focused profiles and only about 0.6 KiB/op byte noise on one nested-update row.
  - Do not replace query-compiler raw nested column-index `HashMap<String, usize>` with a linear `Vec<(String, usize)>` lookup without new evidence. The spike saved only about 0.1-0.2 KiB/op and no allocation counts in read allocation profiles, while Criterion regressed `query-many-m2m` by ~13.8%, `nested-pagination-query` by ~8.9%, and `query-m2o` by ~4.2%.
  - `QueryInterpreter.#compileRawNestedReadQuery()` intentionally reuses the same `RawNestedReadResult` object for relation attachment and return. Do not reintroduce a second identical result wrapper for relation-bearing raw nested nodes without benchmark evidence.
  - Numeric raw nested result mappings are cached by mapping array, not by `SqlResultSet.columnNames`, because numeric refs are independent of result metadata. There is no named-ref compatibility cache on this internal boundary anymore. Use `CLIENT_ENGINE_CACHE_TIMING_FRESH_BLOG_PAGE_RESULT_METADATA=1` with `client-engine-cache-timing.ts` to simulate adapters that allocate fresh result metadata arrays; this exposed compact-node blog-page overhead dropping from 17.90 to 13.80 us/op after the numeric-only cache.
  - Query-plan result `FieldType.arity` is only semantically needed for list fields in the TS data mapper. Omitted arity means non-list; do not reintroduce required/optional scalar arity into Wasm plan serialization.
  - Query-plan result object nodes serialize only as compact tuples `[serializedName, fields]` or `[serializedName, fields, true]` when `skipNulls` is true. The data mapper does not accept legacy `{ type: 'object', serializedName, fields, skipNulls }` containers on this internal boundary.
  - Query-plan result field nodes may omit `type: "field"` and default `dbName` metadata; field nodes are identified by `fieldType`, and missing `dbName` means the surrounding result-object key. Non-list primitive scalar default-`dbName` fields may serialize directly as compact raw strings in result object `fields` (for example `"i"` or `"s"`). Object-shaped `fieldType` remains for list, enum, bytes, extension, and explicit `dbName` metadata. Structured scalar `fieldType.type` values are compact-only too; do not reintroduce canonical result scalar strings such as `"int"` / `"string"` on this internal boundary.
  - Rust result-node mapping in `query-compiler/query-compiler/src/data_mapper.rs` intentionally avoids rebuilding lookup maps for field selections, virtual groups, and nested query aliases inside `get_result_node()`. Linear lookups over the existing selections reduced allocation counts and stayed neutral-to-positive in focused compilation benches; do not reintroduce per-result `HashMap`/group-map construction without new evidence.
  - Rust read translation owns `FieldSelection`s for `ReadQuery` inputs. Prefer consuming helpers such as `FieldSelection::into_without_relations().into_virtuals_last()` when the caller owns the selection; using borrowed `without_relations()` clones all retained fields. `into_virtuals_last()` also has a no-virtual fast path, so do not replace it with manual partitioning unless new allocation profiles justify it.
  - Rust one-to-many read translation should avoid wrapping a single child-link filter in `Filter::And(Vec)`. `build_read_one2m_query()` keeps the lone filter directly and only allocates an `And` vector for zero or multiple filters; this removes one allocation on query-load one-to-many rows and has Criterion coverage.
  - Rust nested `set` graph building should deduplicate the small set of extracted unique filters with the existing `Vec` instead of `itertools::unique()`. The current in-place linear dedup avoids the extra `HashSet` path and saved two allocations on the `update-set-nested` fixture; do not reintroduce hash-set dedup without many-item set evidence.
  - Do not replace the second `parent_relation_field.related_field().linking_fields()` call in `one_to_many_inlined_child()` connect-or-create with another `child_link.clone()` unless new evidence appears. A local allocation profile over connect-or-create and update controls showed no allocation or byte movement.
  - `QueryGraph::normalize_if_nodes()` sibling `Then` / `Else` checks now use direct incoming petgraph iteration. An older 2026-06-07 local spike rejected this shape, but engines commit `a30ce85177f` rechecked it on the current compiler and kept it: close Criterion was neutral-to-positive on connect-or-create/update rows and a create-row guard no longer reproduced the old regression. Do not reintroduce the allocating/sorting `incoming_edges(&sibling)` helper without new close A/B evidence.
  - Do not replace `QueryGraph::subgraph_contains_result()` recursive `outgoing_edges(node)` checks with direct `petgraph::edges_directed(...).any(...)` iteration. A local spike saved up to 21 `translate_ir` allocations on nested writes, but reversed Criterion showed the old sorted-vector path faster or equal on `create-nested-connectOrCreate-m2one`, `create-nested-create-with-composite-id`, and update-set controls.
  - `packages/client/src/__tests__/benchmarks/query-performance/query-plan-cache-memory.ts` includes concrete and parameterized blog-page scenarios. The parameterized scenarios mirror `ClientEngine` cache-key semantics by running `parameterizeQuery()` before `JSON.stringify(parameterizedQuery.query)` and `compiler.compile()`.
  - `packages/client/src/__tests__/benchmarks/query-performance/client-engine-cache-timing.ts`'s blog-page fake adapter must return `SqlResultSet` columns in the same order as the generated SQL projection, not the full static fixture order. Raw nested plans can use numeric column refs, and selection-mask/retained-shape scenarios will otherwise read the wrong cells while real drivers would return projection-shaped rows.
  - `QUERY_PLAN_CACHE_MEMORY_BREAKDOWN=1 pnpm exec node --expose-gc --import tsx packages/client/src/__tests__/benchmarks/query-performance/query-plan-cache-memory.ts` prints serialized plan composition for representative scalar and blog-page plans. On 2026-06-07 the sqlite blog-page plan was ~4.3 KiB serialized, with ~3.5 KiB from strings and the root SELECT alone ~1.0 KiB; compacting small join/pagination metadata is therefore a low ceiling compared with avoiding or interning SQL text/template ownership.
  - `QUERY_PLAN_CACHE_KEY_BREAKDOWN=1 pnpm exec node --expose-gc --import tsx packages/client/src/__tests__/benchmarks/query-performance/query-plan-cache-memory.ts` prints retained cache-key prefix/suffix/trie lower bounds. On 2026-06-07, 1000 retained blog-page keys used 512.2 KiB unparameterized / 562.0 KiB parameterized; a prefix trie would only save 87.8 KiB / 132.2 KiB, while a theoretical shared prefix+suffix representation would save ~399.0 KiB because the nested relation tail is identical. Do not build a prefix-trie `QueryPlanCache` unless new workloads show larger prefix-shaped repetition; any key-memory design needs exact collision handling and probably belongs with a structural key or JS-reference pipeline.
  - `packages/client/src/__tests__/benchmarks/query-performance/client-engine-cache-timing.ts` has both `raw result-set prototype blog page / nested rows` and `raw result-set exact prototype blog page / nested rows`. The first is a flattened lower bound; the second preserves `tags: [{ tag: ... }]` and reads wrapper objects in its checksum. On 2026-06-07 the exact prototype measured about 5.33 us/op versus compact raw nested node 7.89 us/op, showing the remaining exact-shape executor ceiling is not mostly wrapper semantics.
  - Do not serialize flat template SQL as a pre-rendered compact tuple like `["S", sql, args, argTypes]` unless a new design avoids building a full Rust `String` during serialization. A local spike reduced retained plan heap by roughly 3-5% (`blog page / node default warm` 9.87 MiB -> 9.39 MiB for 1000 retained plans) but regressed Wasm compile-prebuilt rows by roughly 1-5% and Criterion reported significant regressions on `filter-contains-param(-insensitive)` and `nested-pagination-query`.
  - `packages/client/src/__tests__/benchmarks/query-performance/workerd-query-compiler-memory.ts` accepts `LOCAL_QC_BUILD_DIRECTORY=/path/to/prisma-engines/query-compiler/query-compiler-wasm/pkg` and mounts that package's sqlite `query_compiler_fast_bg.js` / `.wasm` into Miniflare under the bundled provider-suffixed module names. Use this to test local query-compiler Wasm changes in the Workerd probe without rebundling `packages/client/runtime`.
  - `workerd-query-compiler-memory.ts` has a `client-cache-key` mode and `WORKERD_CLIENT_CACHE_KEY_ITERATIONS` to time the hand-parameterized cache-hit key path without serializing cached plans on every hit. Local 2026-06-07 runs with 50k iterations measured worker-internal key/lookup rows at about 0.44-0.46 us/op for findUnique and 1.52-1.54 us/op for blog-page, while high-iteration generated-client warmed-cache rows measured about 6.92 / 19.90 us/op. Treat `client-cache-key` as a JS-side lower bound, not full generated-client parameterization.
  - `workerd-query-compiler-memory.ts` also prints Workerd-side retained cache-key shape stats for retained plan caches. A 2026-06-07 local-Wasm run showed 100 retained blog-page cache keys at 48.3 KiB total: prefix/suffix factoring could lower-bound them at 8.8 KiB, but a prefix trie only saved 5.0 KiB / 10.4% because most repetition is in the nested relation suffix. Treat prefix-trie cache-key compression as low-ceiling for the current Workerd product shape.
  - `workerd-query-compiler-memory.ts` generated-client rows accept `WORKERD_GENERATED_FIND_UNIQUE_ITERATIONS` and `WORKERD_GENERATED_BLOG_PAGE_ITERATIONS`. Worker-internal `performance.now()` can still report zero elapsed time in Miniflare, so use higher generated-client iterations to amortize host dispatch. A 2026-06-07 local-Wasm run with `WORKERD_GENERATED_FIND_UNIQUE_ITERATIONS=100000 WORKERD_GENERATED_BLOG_PAGE_ITERATIONS=20000` measured generated-client warmed-cache host upper bounds at 22.50 us/op for findUnique and 44.40 us/op for blog-page, with 100,000/0 and 20,000/0 cache hits/misses.
  - `workerd-query-compiler-memory.ts` also has direct-engine and request-layer generated-client rows for `__internal.enginePrecomputedFastPath` / `__internal.requestPrecomputedFastPath`. A 2026-06-07 local-Wasm run with 20k/5k generated iterations measured worker-loop generated-client rows at 7.50 us/op baseline, 2.35 us/op direct-engine, and 4.55 us/op request-layer for findUnique; blog-page nested rows were 19.20 us/op baseline, 9.60 us/op direct-engine, and 12.20 us/op request-layer. All measured fast-path calls hit precomputed data. The Workerd probe also has generated `findMany users` rows; a 10k local-Wasm run measured baseline 3.50 us/op, direct-engine precompute 2.10 us/op, and request-layer precompute 2.50 us/op, with `queryRaw` unchanged.
  - `RequestHandler.mapQueryEngineResult()` should not read `process.env.PRISMA_CLIENT_GET_TIME` on every successful request. Keep the internal timing flag as an edge-safe module-load boolean (`typeof process !== 'undefined' && Boolean(process.env.PRISMA_CLIENT_GET_TIME)`) unless runtime-toggled behavior is explicitly required. A local 2026-06-07 A/B moved generated-client warmed-cache rows from 5.66 / 22.05 us/op reverted to 4.98 / 21.60 us/op patched, and Workerd high-iteration generated-client rows stayed in band at 6.86 / 19.70 us/op.
  - `serializeJsonQuery.ts` intentionally stores serializer selection/argument paths as linked `PathNode`s and materializes arrays only for validation/error paths. Do not change this back to `selectionPath.concat(fieldName)` / `argumentPath.concat(fieldName)` in hot `SerializeContext.nestSelection()` / `nestArgument()`: a local 2026-06-07 A/B moved generated-client warmed-cache rows from 5.04 / 21.43 us/op reverted to 4.73 / 19.70 us/op patched, and Workerd high-iteration generated-client rows improved to 6.61 / 19.30 us/op.
  - Do not cache `isRawQuery(query)` in `ClientEngine.request()` as a standalone hot-path cleanup. A local 2026-06-07 spike was too small/noisy to keep: patched generated-client rows were 4.87 / 19.71 us/op, reverted control was 4.88 / 20.15 us/op.
  - `serializeFieldSelection()` intentionally reads `select` / `include` / `omit` directly and lazily builds an argument object only for non-selection keys, instead of using `{ select, include, ...args }` plus `delete args.omit`. This preserves raw `$type` argument wrapping while avoiding rest-object allocation for selection-only nodes. After the linked-path serializer cleanup, a 2026-06-07 A/B moved generated-client rows from 4.79 / 20.04 us/op reverted to 4.70 / 19.50 us/op patched, and Workerd high-iteration generated-client rows improved to 5.96 / 18.30 us/op.
  - `serializeFieldSelection()` intentionally omits `arguments` when serialized arguments are empty. `JsonFieldSelection.arguments` is optional, Rust `FieldQuery.arguments` is an `Option`, and the JSON protocol adapter treats missing arguments as an empty list. Keep non-empty `arguments` before `selection` in insertion order and keep raw-tagged payloads under `arguments`. A 2026-06-07 current-form run improved parameterization rows substantially (`findUnique` ~1.73M ops/sec, blog-page ~692k ops/sec) and Workerd nested generated-client timing moved to 17.95 us/op, while the simple generated row was mixed/noisy around 6.03 us/op.
  - `serializeFieldSelection()` also intentionally returns a selection-only node directly when there are no argument keys, instead of calling `serializeArgumentsObject(EMPTY_ARGS)` just to omit the empty result. A 2026-06-07 A/B after empty-`arguments` omission moved generated-client rows from 4.76 / 19.77 us/op reverted to 4.62 / 19.11 us/op patched; adjacent Workerd control was 6.45 / 18.25 us/op reverted versus patched runs at 6.23 / 17.95 and 6.35 / 18.20 us/op.
  - `createExplicitSelection()` intentionally avoids an eager `findField()` for object-valued selections unless computed-field skipping needs it; `nestSelection()` performs the field lookup once for those branches. Keep scalar `true` selections on the direct `findField()` path. A broader cached object-field-name set helped nested rows but regressed simple `findUnique`; the accepted narrower control-flow change moved generated-client rows from 4.75 / 18.98 us/op reverted to 4.65 / 18.31 us/op patched, with Workerd in band at 6.24 / 18.00 us/op.
  - Do not split `createExplicitSelection()` into separate no-computed-fields and computed-fields loops without new evidence. A 2026-06-07 spike removed per-key computed-field checks from the empty-extension path but regressed current generated-client rows to 4.74 / 18.55 us/op versus the committed profile at 4.71 / 18.45 us/op and the previous same-session accepted run at 4.65 / 18.31 us/op.
  - Do not change `SerializeContext` to share root params with per-context `modelName` / path overrides as a standalone allocation cleanup. A 2026-06-07 spike removed nested `{ ...this.params, ... }` spreads but regressed the isolated nested serializer row from 4.17 to 4.39 us/op.

- **Coding conventions**:
  - Use **kebab-case** for new file names (e.g., `query-utils.ts`, `filter-operators.test.ts`).
  - **Avoid creating barrel files** (`index.ts` that re-export from other modules). Import directly from the source file (e.g., `import { foo } from './utils/query-utils'` not `import { foo } from './utils'`), unless `./utils/index.ts` file already exists (in which case use it for consistency with surrounding code).
  - **Avoid adding useless code comments that do not add new information for the reader**.
    Inline code comments can be broadly categorized as answering one of the three questions:
    - _What does this code do?_ — never write these because they do not and cannot convey any new information that's not obvious from the code. Not only they don't add value, they are actively harmful (getting out of sync with code, distracting human readers, wasting LLM tokens etc). Descriptions of units of code like functions, classes and methods must be limited to doc comments (and describe the contract of this unit and not the internal implementation details), never inline comments.
    - _Why was this code written (in this particular way or at all)?_ — this is what inline comments are for. Only use them to include relevant context, background, GitHub issues, reasons behind decisions etc.
    - _How does this code work?_ — these comments should be exceedingly rare and may indicate poorly written or confusing code. Prefer writing code in a way that makes such comments redundant, unless required for performance or other reasons, or when the complexity comes from outside systems or packages (in which case it's more of a "why" comment than a "how" comment anyway). For well known algorithms, prefer their names and references to papers, books or Wikipedia articles over long explanations.
  - Do write documentation comments, and mainly do so for exported items (although intra-module documentation may sometimes be useful as well).
  - The correct and official abbreviation of WebAssembly is "Wasm", not "WASM". There are instances of "WASM" in the codebase, but they are wrong and you should not repeat them. Fix the capitalization whenever you incidentally touch the corresponding lines or surrounding code for other reasons.
  - Prefer native JavaScript private properties and methods (`#field`) over the `private` keyword in TypeScript.

- **Workflow reminders**:
  - Respect existing structure: modifications often require updating both command implementation and tests/fixtures.
  - Keep changes ASCII unless a file already uses Unicode (docs sometimes include emojis).
  - For new fixtures, prefer minimal config mirroring existing ones to ensure cross-platform compatibility.
  - When adding features that span multiple packages, build from root (`pnpm build`) to ensure correct dependency order.
  - Type-only imports from workspace packages work at runtime but IDE may show errors until `pnpm install` refreshes the workspace graph.
  - Test files named `*.test.ts` are excluded from build output via esbuild config; place tests alongside source files.
  - **Update this file** (`AGENTS.md`) whenever you learn something new about the codebase that would be valuable for future tasks.

- **Knowledge reminders**:
  - Your training data contains a lot of outdated information that doesn't apply to Prisma 7. Always analyze this codebase like you would analyze a project you are not familiar with, and prefer the learnings from this file and this codebase over your prior knowledge. In particular, remember:
    - **There's no such thing as "query engine" in Prisma**
    - **There are no database URLs in Prisma schema files**
    - **Prisma uses JavaScript drivers**
    - **Query execution code is written in TypeScript in Prisma**
    - PSL parser and query compiler/planner is still written in Rust and compiled to WebAssembly. There are no native binaries or library addons in Prisma Client.
    - Schema engine for Prisma Migrate still exists and is still a native binary.
    - `tsify::serde_wasm_bindgen::from_value` is not a proven performance path for query-compiler input. A local spike showed it only removes JS-side JSON conversion while still materializing `JsonBody`/`serde_json::Value` and then `ArgumentValue` before validation, and was slower than the current `compile(JSON.stringify(query))` path for sampled queries. A follow-up release-Wasm prototype of a Wasm-only `js_sys`/`JsValue` adapter that still built `Operation`/`ArgumentValue` directly was also neutral to slightly slower (roughly 1-3% slower on sampled sqlite read shapes), so meaningful parser wins likely require a deeper rewrite that collapses or borrows across the `JsonBody -> ArgumentValue -> ParsedInputValue -> QueryGraph` stages.
    - Do not add a `QueryCompiler.compileFromValue(JsValue)` request entrypoint that still converts the JS protocol object into owned `JsonSingleQuery` / `serde_json::Value` / `Operation` maps. A 2026-06-10 release-Wasm spike was neutral to slower than the string request path on sampled compile misses (`findUnique` 404.42 -> 422.08 us/op, `findMany` 299.61 -> 300.55, blog-page 2306.57 -> 2348.81). The useful JS-owned-query proof needs to skip larger phases together rather than only replacing request-string transport.
    - A Rust-side one-pass `JsonArgumentValue` serde visitor that deserialized `FieldQuery.arguments` directly into `ArgumentValue` removed the adapter's second `serde_json::Value -> ArgumentValue` walk, but Criterion was mixed: some parameterized filter/nested rows improved while other common compile rows regressed or became noisy. Do not revive that shape without a stricter A/B benchmark and a plan for preserving raw/custom-tag error semantics.
    - A more radical JS-reference-backed query compiler pipeline may be practical only as a project-level redesign: pass the query object as a `JsValue`/externref, fuse parameterization/cache-keying/compile-hit handling while walking JS data, and return/cache a JS plan object on cache hits instead of serializing plans through Wasm memory. Wrapper types like `PrismaString` are likely useful only after request parsing, schema lookup, validation, and query graph building can consume borrowed/input-backed values while keeping only internal IRs Rust-owned. Avoiding Rust-owned SQL strings is a separate larger IR/rendering project: SQL rendering would need to emit borrowed/interned/template pieces or JS-backed strings without making FFI property access slower than the current JSON path.
    - Do not start a broad query-compiler `Arc` purge as an allocation optimization without a more specific profile. Local 2026-06-07 allocation profiling showed hot compile allocations concentrated in graph_build and translate_ir owned data structures; schema/model `Arc` clones are common but do not themselves allocate per clone, and schema construction is outside the measured compile hot loop.
    - `query-compiler/query-compiler/examples/allocation_profile.rs` in `prisma-engines` supports `ALLOC_PROFILE_BUCKETS=1` as of engines commit `da30829df76`. Use it before ownership/arena work. A 2026-06-18 refresh with `CARGO_TARGET_DIR=/tmp/prisma-engines-profile-target`, `ALLOC_PROFILE_ITERATIONS=20`, and nested/read/aggregate fixtures kept both checkouts clean and again showed graph_build/translate_ir bytes dominated by mid-sized allocations, not `Arc` clone heap churn. `nested-upsert-nested-only` measured full_compile `3008 allocs/op`, `361.2 KiB`, with graph_build `1107 allocs/op`, `191.6 KiB`, and translate_ir `1723 allocs/op`, `149.5 KiB`; prominent buckets were 257-384 B, 513-768 B, 1-1.5 KiB, 385-512 B, and 129-192 B. Name a concrete graph/translation container target before starting arena/borrowing work.
    - `QueryGraph::reserve_visited_capacity()` is intentionally called once before translation root traversal. Engines commit `b2377d67f39` saved 1-2 `translate_ir` / `full_compile` allocations on nested-write rows (`nested-upsert-nested-only` 1723/3008 -> 1721/3006, `update-set-nested` 1087/1833 -> 1085/1831, `create-nested-connectOrCreate-mixed` 1496/2434 -> 1494/2432), while `create-nested-create`, `query-m2o`, `query-many-m2m`, and `aggregate` controls stayed allocation-neutral. Criterion was neutral-to-positive after patched/control/reapplied checks; do not remove the reserve without close allocation and timing evidence.
    - Direct placeholder storage for `Flow` / `Diff` projected inputs is intentionally kept. Engines commit `fd906df5c3d` replaced the old `Vec<SelectionResult>` inputs for `If`, `Return`, and `Diff` with direct `Option<Placeholder>` slots through `RowSink::ProjectedPlaceholder`, with no old/new compatibility reader. It saved `translate_ir` / `full_compile` allocations on nested-write rows (`update-set-nested` 1085/1831 -> 1075/1821, `create-nested-connectOrCreate-mixed` 1494/2432 -> 1484/2422, `nested-upsert-nested-only` 1721/3006 -> 1717/3002) while `create-nested-create`, `query-m2o`, `query-many-m2m`, and `aggregate` controls stayed allocation-neutral; focused Criterion was neutral-to-positive. Avoid retrying `DataRule` SmallVec, `Let.bindings` SmallVec, broad typed binding names, or `SelectionResult::from_selected_pairs()` as standalone changes.
    - `NodeTranslator::process_child_with_dependencies()` intentionally skips identity parent bindings for projected dependencies that have no validation expectation and no unique wrapper. Engines commit `02404611763` saved `translate_ir` / `full_compile` allocations on nested-write rows (`update-set-nested` 1075/1821 -> 1061/1807, `create-nested-connectOrCreate-mixed` 1484/2422 -> 1474/2412, `nested-upsert-nested-only` 1717/3002 -> 1713/2998) while `create-nested-create`, `query-m2o`, `query-many-m2m`, and `aggregate` controls stayed allocation-neutral. Close Criterion was noisy but neutral-to-positive; do not reintroduce the old construct-then-filter identity binding shape without allocation and timing evidence.
    - Other Rust follow-up leads are larger design work: fuse one-to-many nested `set`'s two diff computation nodes only if the new internal expression shape is replaced lockstep across Rust/TS/tests with no old/new compatibility; branch-aware nested-upsert target-read sharing only with a proof that missing-target error order is preserved; selective projected-binding-name reuse only if it avoids the rejected broad `BindingName` and manual projected-binding-loop regressions.
    - `PrismaValue::Placeholder` compact serialization in `libs/prisma-value/src/lib.rs` should avoid calling `PrismaValueType::to_string()` for scalar placeholder types; serialize scalar type names directly and keep the `List(_)` fallback. A 2026-06-08 local allocation profile reduced native plan `serialize_json` allocations by 1 on `query-m2o`, 4 on `create-nested-connectOrCreate-mixed`, and 6 on `update-set-nested`; cached request rows are not expected to move because this only runs on compile/serialization misses.
    - Do not replace query-builder `RelationLinkage`'s `BTreeMap<ScalarField, Vec<ScalarCondition>>` with sorted `Vec<(ScalarField, SmallVec<_>)>` just to remove singleton condition vectors/map nodes. A 2026-06-08 spike saved 1-3 full-compile allocations on relation fixtures but Criterion regressed key rows (`create-nested-connectOrCreate-one2m` about +10.6%, `query-many-m2m` about +3.1%, `nested-pagination-query` about +2.3%, `query-m2o` about +1.5%).
    - `query_graph_builder::read::utils` intentionally moves parsed field names into `selection_order` once the field list is consumed. Keep the owned helper shape (`collect_selection_order_owned()` and `collect_selection_order_and_nested_queries()`): a 2026-06-08 profile reduced graph-build allocations by 1-14/op across sampled read/returning/nested fixtures, and reverse Criterion was neutral-to-positive.
    - Do not replace returning write paths' borrowed `collect_selected_scalars()` plus consuming `collect_selection_order_owned()` calls with a one-pass scalar helper as a standalone cleanup. A 2026-06-18 spike left `create-many-and-return` and `update-one-returning` allocation counts unchanged (`425` and `456` full-compile allocations) and regressed `delete-one` `332 -> 333`, so it was reverted before Criterion.
    - `query_graph_builder::read::utils::extract_selected_fields()` intentionally builds selected fields, selection order, and nested read queries in one consuming loop. Engines commit `825e696ac5c` saved 3 `graph_build` / `compile_ir` / `full_compile` allocations on nested/write rows (`create-nested-connectOrCreate-mixed`, `nested-upsert-nested-only`, `update-set-nested`, `create-nested-create`) while sampled read/aggregate controls stayed unchanged; close Criterion favored the patch or stayed neutral (`update-set-nested` control 223.25 us vs patched 219.62 us). Keep the helper-local merge unless new evidence shows a localized timing regression.
    - Do not broaden the consuming `extract_selected_fields()` helper into `read::many` and `read::related` as a standalone allocation cleanup. A 2026-06-18 spike saved 3-6 full-compile allocations on read/nested rows (`query-m2o 549 -> 544`, `query-many-m2m 714 -> 709`, `nested-upsert-nested-only 3008 -> 3003`), but Criterion regressed common rows such as `query-m2o 79.479 us`, `query-many-m2m 89.261 us`, `nested-pagination-query 76.557 us`, and `update-set-nested 227.52 us` versus the accepted baseline. Keep the narrower committed helper-local merge.
    - `query_graph_builder::read::utils::merge_relation_selections()` intentionally streams nested linking fields into `FieldSelection::union_iter()` instead of collecting a temporary `Vec<FieldSelection>`. Engines commit `8778c84c831` saved one graph-build/full-compile allocation on sampled nested read/write rows, with close Criterion patched vs reverted medians positive: `query-m2o` 28.13 vs 30.84 us, `query-many-m2m` 33.68 vs 36.60, `nested-pagination-query` 30.10 vs 33.55, and `update-set-nested` 101.78 vs 107.07.
    - `query_graph_builder::read::aggregations` intentionally builds aggregation `selection_order` and `AggregationSelection` values in one consuming pass. Keep `collect_selection_tree_and_selectors()` unless fresh evidence says otherwise: engines commit `cc75ecb1ae7` reduced graph-build allocations by 4/op for `aggregate`, 13/op for `aggregate-custom`, and 4/op for `group-by`; final A-B-A Criterion was neutral-to-positive after noisy controls.
    - Do not change `QueryGraph::subgraph_contains_result()` from sorted `outgoing_edges()` traversal to direct `edges_directed()` traversal just to remove the intermediate vector. A 2026-06-07 spike reduced full-compile allocation counts by 9 allocs/op on `create-nested-connectOrCreate-mixed` and 21 allocs/op on `update-set-nested`, but same-session Criterion was slower: about 143.4 us vs 140.6 us for connect-or-create and 109.4 us vs 101.3 us for `update-set-nested`.
    - Do not pre-size query-compiler result-node `IndexMap`s from `selection_order.len()` as a standalone allocation cleanup. A 2026-06-10 spike saved two translate/full-compile allocations on several rows, but close Criterion was mixed: `query-many-one2m` improved 31.88 -> 31.11 us and `nested-pagination-query` 31.60 -> 30.97, while `query-many-m2m` regressed 34.23 -> 35.24, `query-m2o-lateral` 28.36 -> 28.88, and `query-one2m` 31.34 -> 31.85.
    - `query-compiler/query-compiler/src/result_node.rs::Object` intentionally stores ordered result fields in a capacity-sized `Vec` rather than an `IndexMap`. The result object is already built from `selection_order`, so hash lookup is unnecessary on this internal representation. Engines commit `21a8db27dfd` saved `translate_ir` allocation bytes on sampled reads/nested writes (for example `query-m2o` 27.4 -> 26.0 KiB, `aggregate-custom` 33.0 -> 31.2 KiB, `create-nested-connectOrCreate-mixed` 149.3 -> 147.8 KiB) and Criterion improved representative compile rows by about 2-6%, with `nested-pagination-query` neutral rather than regressed. Do not reintroduce `IndexMap` here unless a future duplicate-key/order requirement is proven.
    - Do not change `raw_result_column_mappings()` in `query-compiler/src/translate/query/read.rs` from `Vec::new()` to `Vec::with_capacity(selection_order.len())` as a standalone allocation optimization. Local allocation profiling saved only ~0.2 KiB/op and no allocation counts on sampled read shapes. A refreshed 2026-06-11 close A/B was worse on the targeted compile rows: patched vs reverted-control medians were `nested-pagination-query` 42.920 vs 41.162 us, `query-m2o` 40.565 vs 38.197, and `query-many-m2m` 47.394 vs 45.323.
    - Do not replace raw nested `RawColumnIndexes`'s `Vec<(Cow<str>, usize)>` with a borrowed `FieldSelection` ordinal view as a standalone allocation cleanup. A 2026-06-12 spike saved 2-3 `translate_ir`/`full_compile` allocations on raw-nested rows (`query-m2o`, `query-many-m2m`, `query-many-one2m`, pagination variants), but the refined close Criterion A/B regressed `query-many-m2m` by about 1.75% and softened other target rows by about 0.6-0.9%. The current short-lived vector is the better CPU/allocation tradeoff unless a new design avoids both the heap allocation and the slower m2m ordinal lookups.
    - Do not replace the raw-nested read borrowed/fallback builder with an owned builder plus an exact preflight as a standalone clone-removal cleanup. A 2026-06-12 spike in `query-compiler/src/translate/query/read.rs` passed `cargo check -p query-compiler`, but allocation profiling regressed the raw-nested translate rows: `query-m2o` / `query-many-m2m` / `query-many-one2m` `translate_ir` allocs/op moved `327/425/326 -> 331/429/330`, while pagination rows softened by one allocation. The next raw-nested ownership attempt should remove a larger consume-once phase or transfer fallback ownership without duplicating the old support checks.
    - Do not extend the raw-nested root result-map skip to single graph result nodes as a standalone cleanup. A 2026-06-18 spike added a result-node preflight and allowed raw-nested `ThrowOnEmpty` validation wrappers, passed `cargo check -p query-compiler`, but allocation counts stayed unchanged on sampled nested write/result-read rows (`nested-upsert-nested-only 3011`, `create-nested-connectOrCreate-mixed 2437`, `create-nested-connectOrCreate-one2m 2145`, `update-set-nested 1836`, `query-one2m-pagination 1153` full-compile allocations). A useful follow-up needs to bypass the graph-level mapper for the whole write expression, not only prove one descendant read is raw-nested.
    - In query-compiler translation, `binding::projected_dependency()` should use `NodeRef::index()` when embedding the raw graph node id in a larger placeholder string. This avoids the intermediate `NodeRef::id()` allocation and lowered dependency-heavy write translation by 4-12 allocs/op in local allocation profiling, with net-favorable Criterion. Do not broadly replace user-facing/debug uses of `NodeRef::id()` or `Display`; the existing `Display` formatter intentionally prints labels such as `Node 3`.
    - In `query-compiler/core/src/query_graph_builder/write/nested/create_nested.rs`, keep the one-to-many inlined-in-child `handle_one_to_many()` linking field selections hoisted outside the child-create loop. This did not reduce allocation counts, but Criterion improved `compile/create-nested-create` by about 1.8% while `create-nested-create-with-composite-id` stayed neutral. Do not generalize this into broad linking-field clone churn without fresh allocation and Criterion evidence.
    - Do not defer non-result `CreateRecord.selection_order` construction as a standalone allocation cleanup. A 2026-06-11 spike changed create ordering to `Option<Vec<String>>` and saved 2-6 graph-build/full-compile allocations on create-heavy rows, but close Criterion favored the reverted control on larger rows (`create-nested-connectOrCreate-mixed` 318.85 vs 310.62 us, one2m 281.71 vs 274.32, composite nested create 170.15 vs 165.77, update-create-child 117.00 vs 115.29, upsert 77.81 vs 76.51).
    - Do not do branch-local `Filter` clone elision in `nested_update()` / `nested_upsert()` with `Option<Filter>` ownership plumbing as a standalone cleanup. A 2026-06-18 spike passed `cargo check -p query-compiler`, but allocation counts stayed unchanged on sampled rows (`nested-upsert-nested-only 3008`, `upsert-nested-only-update 1892`, `update-set-nested-prisma#27650 1687`, `update-set-nested 1833`, `create-nested-connect 1336`, `query-m2o 549`, `aggregate 607` full-compile allocations). The apparent `upsert-nested-only-update 1895 -> 1892` delta was stale-baseline drift.
    - Do not route `insert_find_children_by_parent_node()` parent projected selection through `get_selected_fields()` as a standalone merge-skip cleanup. A 2026-06-18 spike passed `cargo check -p query-compiler`, but allocation counts stayed unchanged on sampled nested-write rows (`create-nested-connect 1336`, `update-set-nested 1833`, `nested-upsert-nested-only 3008`, `create-nested-connectOrCreate-mixed 2434` full-compile allocations).
    - Fusing one-to-many nested `set` diff computations is still a plausible larger design lead, but not a small graph-node swap. The current graph creates separate `DiffLeftToRight` and `DiffRightToLeft` nodes because downstream `If` / update branches need separately addressable left/right delta results, while `ProjectedDataDependency` can only bind a source node's whole result or projected fields. A real fused design needs named computation outputs or a new internal expression/runtime shape, updated lockstep with no compatibility branch.
    - `build_raw_nested_read_relations()` intentionally supports safe implicit many-to-many compact raw reads. Engines commit `e8854f60bc0` switched M:N nested reads from `dataMap`/`join` plans to compact `rawNestedRead` using `build_read_m2m_query()` and its hidden `linking_field_alias` as the child column. Local allocation profile reduced `full_compile` allocations by 144/op for `query-m2m`, 117/op for `query-many-m2m`, and 321/op for M:N-containing connect-or-create rows; Criterion improved those rows by about 9-11%. Keep fallback guardrails for compound relation keys, in-memory pagination/distinct, explicit join-table wrapper shapes, and alias leak risk.
    - Compact raw nested reads intentionally compile numeric direct/many-to-many relation attachers once per plan in `query-interpreter.ts`, and raw mappings now always carry field-type metadata. Prisma commit `79d29b2fa` moved the focused raw compact blog-page row from 6.69/6.71 us/op unpatched to 6.59 and then 6.40 us/op patched, with Workerd generated blog-page rows still sane. Keep generic fallbacks for path mappings, scalar metadata, enum/date conversion, and zero-column empty results; named column refs and no-metadata raw mappings are no longer part of the internal format. Further raw nested runtime work should target larger plan-shape/object-allocation changes, not more column-ref-resolution micro-branches.
    - Compact raw nested direct wrapper relations have an exact-wrapper fast path in `query-interpreter.ts` as of Prisma commit `de6735cbf`: when a direct relation's child query has no scalar mappings and exactly one unique direct child relation, the interpreter attaches `{ wrapperField: child | null }` objects directly instead of materializing wrapper records and then running generic direct attachment. A 2026-06-08 local A/B moved `raw result-set exact compact node blog page / nested rows` from 6.49 to 6.25 us/op over 100k iterations and Workerd generated blog-page warmed cache from 17.55 to 17.05 us/op. Keep generic fallback for dynamic shapes; the remaining raw-nested gap still needs a flatter result-set assembler or compiler-emitted plan shape.
    - `packages/client/src/__tests__/benchmarks/query-performance/client-engine-cache-timing.ts` includes a benchmark-only `raw result-set direct assembler blog page / nested rows` lower bound as of Prisma commit `470698a26`. It executes the same seven blog-page DB query slots but writes the final exact result graph directly, avoiding `RawNestedReadResult` nodes and generic attach passes. Local 2026-06-08 runs measured 4.06 us/op over 300k iterations versus exact prototype 5.62 and exact compact node 7.34; the async-adapter row measured 7.78 us/op versus direct plan 11.96 and exact compact node 12.07. Treat this as evidence for a compiled/materialized raw result-set assembly program, not as product code or a reason to hard-code query shapes.
    - `client-engine-cache-timing.ts` includes benchmark-only `raw result-set writer program blog page / nested rows` and `raw result-set static-wave writer program blog page / nested rows`. The static-wave row reuses the relation-phase writer schedule but unrolls the known two waves instead of allocating generic `phases`/`pending` arrays per execution. A 2026-06-08 300k Node run measured direct assembler 3.90 us/op, generic writer 4.15, static-wave writer 3.91, compact node 6.42, and exact compact node 6.51. Treat static generated schedules as the current best product-shape raw-nested lead: compile static writer waves with fallback gates, not wrapper trees, generic writer interpreters, or generic descriptor interpreters.
    - `workerd-query-compiler-memory.ts` includes Worker-visible raw result-set lower-bound modes controlled by `WORKERD_RAW_RESULT_SET_ITERATIONS`: `raw-result-set-assembly`, `raw-result-set-direct-assembler`, `raw-result-set-writer-program`, and `raw-result-set-static-wave-writer-program`. A 2026-06-08 smoke measured raw assembly at 0.69 us/op host / 0.55 us/op worker loop, raw direct assembler at 2.26 host / 2.15 worker, generic writer at 2.59 host / 2.40 worker, and static-wave writer at 2.29 host / 2.15 worker over 20k iterations with 140000 `queryRaw` calls for the direct/writer rows. This is lower-bound coverage only; compact/exact compact Worker comparisons still need a worker path that can import the interpreter/runtime helpers.
    - Do not add a unique-root compact `n` fast path that still recursively materializes child `RawNestedReadResult` records and then uses generic/direct attach helpers. A 2026-06-08 runtime spike passed `query-interpreter.test.ts` but only moved compact blog-page rows from 6.39 to 6.29 us/op at 300k and regressed exact compact rows from 6.54 to 6.78, while the direct assembler lower bound was 3.83-4.04 us/op. The next raw-nested product prototype needs a flat/materialization schedule that writes final records directly for every relation phase, including list children and wrapper records.
    - Do not build a raw nested product runtime around a generic execution-object tree plus recursive materializer. A 2026-06-08 benchmark spike that compiled eligible `RawNestedReadQuery` tuples into raw result-set slots and then recursively projected/filling relation fields measured 8.34 us/op after fixing per-iteration compilation, slower than compact node 6.55 and exact compact node 6.74, while the direct assembler lower bound was 3.95. The next attempt needs compiled relation-specific writer closures or instruction arrays that mutate final owner objects directly as relation phases resolve.
    - Do not add a raw nested "assembly" wrapper over compact `n` trees that still returns child `records` arrays and calls the existing attach helpers. A 2026-06-08 `query-interpreter.ts` spike added a strict numeric `{ rows, records }` assembly-result path and passed `query-interpreter.test.ts`, but same-session focused rows were effectively unchanged: side compact/exact compact 6.44/6.66 us/op versus main 6.45/6.75, with direct assembler 3.92-4.08. The next useful raw-nested runtime shape must avoid materializing child records purely for attachment or mutate final owner objects as relation phases resolve.
    - Do not productize a raw-nested runtime writer path as a generic execution-object tree that still builds child record arrays and then calls generic attach helpers. A 2026-06-08 side worktree passed `pnpm --filter @prisma/client-engine-runtime test src/interpreter/query-interpreter.test.ts` and `pnpm --filter @prisma/client... build`, and moved compact/exact compact blog-page rows from 6.80/6.96 us/op to 6.49/6.59 over 300k iterations, but it added about 337 lines to `query-interpreter.ts` for a narrow 5-10% win while the benchmark-only writer program remained around 4.1-4.3 us/op. The next product shape should be compiler-emitted or compile-time-built flat writer schedules that mutate final owner objects directly.
    - Do not productize a guarded raw-nested static writer path that still derives a generic runtime executor from compact `n` trees without a truly unrolled/generated final-owner schedule. A 2026-06-08 side worktree passed interpreter tests and client/runtime builds, but only moved the compact raw nested blog-page row from 6.37 to 6.19 us/op over 50k iterations while the static-wave lower bound was about 4.0 us/op; exact compact stayed on fallback at 6.99 us/op and Workerd was not verified. The next product path should compile plan-specific static waves or instruction arrays, not add another guarded wrapper around the existing tree.
    - Do not productize a flat raw-nested node/wave/instruction-array executor that still uses generic per-run `resultSets`/`records`/`scopes` arrays plus generic row mappers and attach instructions. A 2026-06-08 side worktree passed focused interpreter tests and runtime/client builds, but regressed the 300k compact-node blog-page row to 6.89 us/op versus a read-only current-main 6.31 us/op baseline; exact compact stayed on fallback and still measured 6.93 us/op versus 6.63. The next product path must compile more direct writer opcodes/closures that own state slots and avoid generic result/record arrays.
    - Do not productize runtime-only flattened raw-nested source/link execution that still maps per-source records and uses generic wave/link loops. A 2026-06-10 side-worktree spike passed the full `@prisma/client-engine-runtime` test set under the focused command, but the best fixed-width 4-link/2-link wave run only moved generated blog-page 11.75 -> 11.41 us/op and exact descriptor helper 10.21 -> 10.12, while product-shaped compact node regressed 6.27 -> 6.58 and stayed far from the 3.86 us/op static-wave lower bound. The next raw-nested proof needs final-owner writer ownership, not just a flatter interpreter.
  - Do not add a final-owner root scope renderer as a standalone follow-up to the kept relation query-leaf renderer. A 2026-06-11 patch improved focused compact raw-nested rows, but generated stable blog-page regressed in close A/B: patched repeats were 11.61 and 11.45 us/op versus reverted control 10.72, so it was reverted. Root rendering is too small/noisy unless combined with a larger generated/static-wave path.
  - Do not pre-seed final-owner root relation fields/list arrays as a standalone V8 shape optimization. A 2026-06-11 patch allocated `author`/`category`/`comments`/`tags` slots before child-query waves and filled preallocated lists in place, but close control beat patched on all focused rows: compact/exact/direct/generated were 4.50/4.52/4.68/10.37 us/op reverted versus 4.97/4.87/5.01/11.37 patched.
  - Do not replace final-owner `RawNestedFinalOwnerChildTarget` objects with parallel target arrays or small-list scope dedupe as a standalone allocation cleanup. A 2026-06-10 slice passed focused interpreter tests/build, but adjacent timing was neutral-to-slightly worse: patched direct/generated nested rows were 5.39 / 11.37 us/op on repeats versus reverted control 5.39 / 11.30. A refreshed 2026-06-11 retry also failed the product gate: raw compact/exact rows were 4.40/4.69 patched versus 4.39/4.72 control, and generated stable/alternating blog-page rows were 10.38/10.57 patched versus 10.27/10.41 control. A harness-restart narrow repeat rejected the target-object half again: baseline direct/compact rows were 4.47/4.33 us/op, patched rows were 4.66/4.51. Revisit only as part of a larger slot/index attachment rewrite or final-owner phase query runner.
    - Do not add a direct leaf-row writer fast path only for raw nested unique wrapper relations. A 2026-06-09 partial side-worktree patch passed `query-interpreter.test.ts` and `@prisma/client-engine-runtime` build, but close 100k A/B rejected it: compact node blog-page moved 7.05 -> 7.21 us/op and exact compact moved 6.88 -> 7.40. This shape still scans wrapper/child rows and only avoids some child record materialization; it does not approach the static-wave/direct-assembler lower bound.
    - Do not add a unique-root compact `n` writer that still derives relation execution from the existing raw-nested tuple tree, even if leaf direct/M:N/wrapper children map rows directly into parent records. A 2026-06-09 spike passed `query-interpreter.test.ts`, but close 100k A/B was too weak: compact node was unchanged at 6.42 vs 6.41 us/op, and exact compact improved only 6.66 -> 6.14 us/op. The next viable writer path needs compiler-emitted or generated static waves with owned state slots and final-object writes, not another runtime derivation from compact `n`.
    - Do not productize a serializable flat writer schedule by deriving generic phase closures from the current compact `RawNestedReadQuery` tree. A 2026-06-09 side-worktree benchmark prototype compiled explicit state slots and relation waves for the exact blog-page shape and avoided `RawNestedReadResult` wrappers, but it still measured only 6.07 us/op versus compact node 6.49, exact compact 6.59, and static-wave lower bound 4.06. The remaining win needs truly generated/static final-object write schedules, not a data-driven phase interpreter.
    - Do not specialize raw nested numeric row mapping with exact-width computed-key object literals without new evidence. A 2026-06-08 spike for widths 1, 3, and 7 regressed `raw result-set exact compact node blog page / nested rows` to 7.97 us/op and non-exact compact rows to 7.87 us/op, much worse than the accepted direct assignment loop.
    - Do not add a runtime-derived "simple flat" executor over existing compact raw nested `n` trees without changing the actual assembly algorithm/protocol. A 2026-06-08 numeric-only tuple-returning spike passed focused interpreter tests but regressed exact compact blog-page rows to 6.74 us/op versus the accepted wrapper-fast-path row's 6.25 us/op. The next raw-nested project should be a real flat result-set assembly program or a different hot path, not another wrapper around the current tree.
    - Do not change `applyModel.ts` lazy descriptor object nodes from `fields: Record<string, LazyDescriptorNode>` to key-aligned field arrays without new evidence. A 2026-06-08 spike regressed generated blog-page precomputed fast paths to 13.00 us/op for engine-precomputed and 14.66 us/op for request-precomputed, so the current record shape is better on V8.

## Debugging and making changes to Rust/WebAssembly code

When you need to check the code or make some changes in Rust codebase, assume the repository is checked out in the `prisma-engines` directory above the root of this repo. Determine the absolute path to the current project on the filesystem (e.g. `/home/user/work/prisma`) and infer the directory of the `prisma-engines` repo (e.g. `/home/user/work/prisma-engines`, let's call it `$PRISMA_ENGINES_ROOT`).

After you make some changes there, use these commands in the prisma-engines repo to build the Wasm modules:

```sh
make build-schema-wasm
make build-qc-wasm
```

Then, back in `prisma` repo:

```sh
pnpm upgrade -r @prisma/prisma-schema-wasm@file:$PRISMA_ENGINES_ROOT/target/prisma-schema-wasm
pnpm upgrade -r @prisma/query-compiler-wasm@file:$PRISMA_ENGINES_ROOT/query-compiler/query-compiler-wasm/pkg
pnpm build
```

You may only need to build and update one of these modules if your changes are isolated in scope:

1. Build only `prisma-schema-wasm` if your changes are isolated to schema and DMMF, and you are only going to run the CLI and generator tests, not Client.
2. Build only `query-compiler-wasm` if your changes are related to query planning and execution but do not touch the schema or DMMF in any way.

When in doubt, build both to avoid unexpected behavior. Time and cost of compilation is always less than of debugging.

`make build-qc-wasm` runs `query-compiler/query-compiler-wasm/build.sh`, which expects `jq` on `PATH` for `cargo metadata` and package version rewriting. In minimal harnesses without `jq`, provide a temporary local shim or install `jq` before running the build.

For query compiler heap allocation splits, run `ALLOC_PROFILE_ITERATIONS=50 ALLOC_PROFILE_WARMUP=5 cargo run -p query-compiler --example allocation_profile --release` in `/home/aqrln.guest/prisma-engines`. The example reports `parse_json`, `into_doc`, `graph_build`, `translate_ir`, `compile_ir`, `full_compile`, and native JSON serialization allocation counts/bytes; use `ALLOC_PROFILE_QUERIES=query-m2o,nested-pagination-query` to narrow the fixture set.

`cargo test -p request-handlers protocols::json::protocol_adapter --lib` can fail in the default local engines checkout before exercising code because JSON protocol adapter fixtures use `provider = "mongodb"` while that provider is not enabled. Use `cargo test -p request-handlers --features mongodb protocols::json::protocol_adapter -- --nocapture` for the focused adapter tests.
