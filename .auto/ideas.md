- Profile CLI journey config loading and scratch-directory setup. Many journey files repeatedly evaluate TypeScript config and recreate fixture directories, but cache invalidation must preserve tests that mutate configs mid-run.
- Hoist identical `createControlStack(...)` / family-instance construction out of individual tests in the family integration suites. Exploration found roughly 172 creations across 20+ files; per-test drivers and database cleanup must remain isolated. Benchmark the family-focused files before expanding because stack construction may be cheap relative to database work.
