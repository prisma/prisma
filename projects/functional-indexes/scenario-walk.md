# Scenario walk — A–J coverage (DoD-5)

Every scenario row in [spec.md § Supported scenarios](spec.md) mapped to the automated tests that assert its stated behavior. Rows cite the test file and the test name (or describe block) whose assertions carry the claim; where a scenario's behavior spans layers, the end-to-end journey is listed first and the unit evidence after.

| Scenario | Evidence |
| --- | --- |
| **A** — Sign a database directly: infer → emit → verify zero issues, plan zero ops | `test/integration/test/cli-journeys/sign-the-database.e2e.test.ts` — "sign a database this toolchain has never seen, then transition to managed" → "infer → emit → verify zero issues → sign → dry-run zero ops" (byte-asserted emissions incl. a RESTRICTIVE policy). Re-infer stability: `contract-infer-workflow.e2e.test.ts`; fidelity instrument: `infer-roundtrip-fidelity.e2e.test.ts`. |
| **B** — Greenfield managed authoring migrates a fresh database | `test/integration/test/cli-journeys/expression-index-migration.e2e.test.ts` — "expression-index authoring journey — PSL" and "— TS twin" (wire-named `CREATE INDEX` byte-asserted, verify passes by name identity, out-of-band drop fails verify). |
| **C** — Exact → managed transition renames, never drop+create | `sign-the-database.e2e.test.ts` — "map:-to-managed transition plans exactly two renames, applies, verifies clean" (`map:`→`name:` on one index and one policy, byte-asserted); `rls-exact-name-adoption.e2e.test.ts` — "exact-named policy adoption converges to managed via one rename"; planner unit: `packages/3-targets/3-targets/postgres/test/migrations/index-rename-planner.test.ts` — content-pairing block ("pairs on expression and where bodies byte-for-byte", "an exact-named missing index never content-pairs (managed only)"). Degradation under additive-only: "degrades to a bare create of the new name under an additive-only policy". |
| **D** — Prefix rename on managed: same hash → RENAME | `index-rename-planner.test.ts` — "plans exactly one ALTER INDEX … RENAME TO — no drop, no create", "plans the rename without the destructive allowance", "multi-candidate groups pair deterministically by sorted name". Policy edition: `rls-rename-planner.test.ts` (hash pairing). |
| **E** — Content edit on managed: create + drop, no pairing | `index-rename-planner.test.ts` — "a content edit (same prefix, different hash, different content) stays create + drop", "a byte-different body does not pair — create + drop". |
| **F** — Out-of-band drift on an exact-named object → not-equal | `packages/3-targets/6-adapters/postgres/test/migrations/rls-exact-drift.integration.test.ts` — "verify is clean before the ALTER and reports not-equal after it", plus the destructive-replacement and conflict-naming cases; index edition: `node-issue-planner.test.ts` (`indexIncompatible` conflict surface), `rls-not-equal-planner.test.ts` (policy planner verdicts). |
| **G** — Hand-authored body under `map:`: degraded by design, D9 warning | Warning: `packages/2-sql/1-core/contract/test/index-naming.test.ts` — "emits one warning per item up to the threshold, each naming its index" (+ threshold cases); policy edition in `psl-policy-map-authoring.test.ts`. The false not-equal mechanics that motivate the warning are the drift cases under F. |
| **H** — Out-of-band structured drift on managed is caught | `packages/2-sql/1-core/schema-ir/test/sql-index-ir.test.ts` — "false when options differ; loose String() coercion still applies", "a unique index and a non-unique index are not equal (symmetric)" (managed `isEqualTo` compares structured attributes); adapter-level: `packages/3-targets/6-adapters/postgres/test/migrations/planner.unique-index-structural.test.ts`. |
| **I** — Pre-project database upgrade: renames only | `test/integration/test/cli-journeys/index-name-convergence.e2e.test.ts` — "pre-wire-name default index names converge via renames only" (first widening plan is renames-only against a database migrated under the old scheme). |
| **J** — Same-tuple twins coexist | `packages/2-sql/1-core/contract/test/validators.test.ts` — "two content-identical EXACT indexes under different names validate (legal twins)", "two content-identical MANAGED indexes under different prefixes still reject"; introspection dedup deletion covered by full-fidelity capture in `packages/3-targets/6-adapters/postgres/test/control-adapter.test.ts`; the Supabase reference's same-tuple `objects` pair (differs in `unique`) round-trips in the regenerated contract. |

No row is uncovered. The one deliberate evidence asymmetry: scenario G's "false not-equal noise" is asserted through the same drift machinery as F rather than a dedicated G-labeled journey — the D9 warning tests carry G's distinct behavior (the emit-time guidance).
