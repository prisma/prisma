# Brief: D7 Round 3 — correct the final operation classification

## Task

Change the React Router example's ignored non-returning insert from row `query` to statistics `execute`.

## Scope

**In:** `examples/react-router-demo/app/routes/users.tsx` action only; focused example typecheck/test/lint and hard-cut scan.

**Out:** Any other source, replay work, instruction changes, cleanup, full-workspace reruns, or API changes.

## Completed when

- [ ] The loader remains on `query`; the non-returning insert uses `execute`.
- [ ] Focused React Router example validation and diff checks pass.
- [ ] One signed commit, clean tracked/index state, no push.
