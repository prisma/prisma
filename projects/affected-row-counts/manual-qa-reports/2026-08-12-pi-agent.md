# Manual QA report — TML-3166 (affected row counts) — 2026-08-12

> **Script:** `manual-qa.md` (commit `983e093156a075d9868b802a0a0d1e95e5a5aa78` at run time)
> **Runner:** pi-agent
> **Environment:** Linux; repository checkout at `983e093156a075d9868b802a0a0d1e95e5a5aa78`; close-out docs branch; no database or network access used
> **Started / finished:** 2026-08-13T10:37:00Z / 2026-08-13T10:42:00Z
> **Verdict:** Pass

## Summary

The bounded read-only QA pass passed. An application reader can follow the count-terminal journey from the scorecards to the write-derived runtime statistic and target-specific semantics. An extension author can follow the two-method `SqlQueryable` SPI and the operation-specific middleware lifecycle without encountering the retired generic interception shape or the abolished `ADAPTER.PREPARE_FAILED` reference. No live database was used because this dispatch changes durable documentation and QA artifacts only; automated behavior obligations remain CI-owned.

## Findings

None.

## Per-scenario log

| # | Scenario | Isolation | Wallclock | Result | Findings |
| - | --- | --- | --- | --- | --- |
| 1 | Read the count-terminal journey | read-only | 2 min | Pass | — |
| 2 | Read the extension-author lifecycle | read-only | 2 min | Pass | — |
| 3 | Exploratory: bounded coherence pass | read-only | 1 min | Pass; no surprises found | — |

## Evidence

### Pre-flight

```text
$ git rev-parse HEAD
983e093156a075d9868b802a0a0d1e95e5a5aa78

$ git status --short
<no output>
```

### Scenario 1

The scorecard and durable-doc read-through found the following current evidence and semantics:

```text
scorecard/06-sql-orm-client.md:30:| `updateAndCount` | ✅ | 🟡 | — | ... one write statement ...; ... write-derived count ... |
scorecard/06-sql-orm-client.md:33:| `deleteAndCount` | ✅ | ✅ | — | ... one write statement ...; ... `deleteAndCount` ... |
scorecard/06-sql-orm-client.md:68:Count terminals use the write statement's native statistic. Postgres reports command-tag matched rows, including no-op updates; SQLite reports `StatementSync.run().changes` (`sqlite3_changes64()`), so its count is target-specific rather than a cross-database normalization.
scorecard/07-mongodb-query-and-orm.md:39:| ORM `updateAndCount` | — | — | 🟡 | Shipped count terminal; Mongo runtime maps `modifiedCount` to `affectedRows`; integration evidence pending |
scorecard/07-mongodb-query-and-orm.md:42:| ORM `deleteAndCount` | — | — | 🟡 | Shipped count terminal; Mongo runtime maps `deletedCount` to `affectedRows`; integration evidence pending |
```

The runtime subsystem gives the consumer-facing distinction directly: `query()` is the row stream, while `execute()` returns `{ affectedRows }`; its target section describes Postgres command-tag matched rows, SQLite `StatementSync.run().changes`, and Mongo `modifiedCount` / `deletedCount`. The Mongo family page repeats the Mongo terminal semantics where Mongo users look for them.

### Scenario 2

The amended ADR and source-aligned lifecycle read-through found:

```text
$ grep -RInE 'ADAPTER\.PREPARE_FAILED|ADR 027|intercept\?\(' <amended durable docs>
<no output>

ADR 210:
interface SqlQueryable {
  query<Row>(request: SqlExecuteRequest): AsyncIterable<Row>;
  execute(request: SqlExecuteRequest): Promise<{ affectedRows: number }>;
}

ADR 215:
query:   beforeQuery → encodeParams → interceptQuery → driver query → onRow → afterQuery
execute: beforeExecute → encodeParams → interceptExecute → driver execute → afterExecute
```

The amended runtime docs also state that query interception returns `{ rows }`, execute interception returns `{ stats }`, `onRow` is query-only, and the context has no operation discriminator. ADR 210 points stale-handle failure at `DRIVER.PREPARE_FAILED` and ADR 239.

### Scenario 3

A five-minute bounded scan sampled the amended ADRs, both runtime subsystem pages, both scorecards, and their immediate source/test links. The local-link check reported:

```text
checked 7 docs; missing local links: 0
```

No factual contradiction or discoverability issue was found. The live application/driver journey was not executed against a database; that would duplicate automated obligations and exceed the read-only documentation charter.

## Coverage outcome

| AC ID | Scenario(s) | Result | Notes |
| --- | --- | --- | --- |
| AC-1 | 1 | Pass | Count terminals are documented as write-derived and scorecard evidence names the one-statement/interleaving tests. |
| AC-2 | 1, 2 | Pass | Driver/runtime vocabulary and target semantics agree across durable docs. |
| AC-3 | 2 | Pass | ADR 210 accurately documents the two-method `SqlQueryable` and request handle. |
| AC-4 | 1 | Pass | Postgres, SQLite, and Mongo semantics are documented in durable target-facing pages. |
| AC-5 | 2 | Pass | ADR 215 and subsystem 4 describe operation-specific middleware lifecycles and result shapes. |
| AC-6 | 1 | Pass | Scorecards record shipped support and distinguish integration-qualified evidence from pending evidence. |
| Automated implementation/test obligations | CI; not manual-QA scope | N/A | The script explicitly excludes test-suite re-runs; no fake automated result is claimed. |

## Suggested follow-ups

None from this bounded pass. CI should continue to own the automated source and integration obligations listed in the project DoD.
