# Brief: S4-D1 — engines change and PR (Half A)

## Task

In prisma-engines, move `TypedSql` from the `hidden` to the `active` preview-feature set and
open the PR. This is cross-repo work in `/tmp/prisma-engines-compact-plan-format` (gh push
access verified).

## Probe first (retro rule — before any edit)

1. Freshen: `git fetch origin` and confirm `origin/main` is reachable; check the worktree is
   clean; branch `chore/unhide-typed-sql-preview-feature` (or the repo's convention if its
   CONTRIBUTING says otherwise) from `origin/main`. If the checkout looks unusable, clone
   fresh to `/tmp` instead and say so.
2. Toolchain: does `cargo` exist and does `cargo check -p psl-core` (or the crate's actual
   name) complete? If no toolchain, validation surface is CI — proceed, but state it.
3. Conventions: skim CONTRIBUTING.md / CI workflows for commit-message or fmt gates.

## Completed when

- [ ] `psl/psl-core/src/common/preview_features.rs`: `TypedSql` removed from the `hidden`
      bitflags (`ReactNative` stays hidden) and added to the `active` set, preserving the
      file's ordering conventions.
- [ ] Engines tests pinning the feature classification found (grep for e.g. `typedSql` /
      `TypedSql` / hidden-features assertions/snapshots) and updated; run the narrowest
      covering test target if cargo is available; otherwise enumerate what CI will run.
- [ ] Branch pushed to prisma/prisma-engines; PR opened against `main` titled per repo
      convention (suggest: `chore(psl): unhide the typedSql preview feature`), body written
      for engines reviewers cold: what changes (hidden → active; visibility only, no
      behavior change — the feature already works when enabled), why now (discoverability;
      the ORM's agent-native work recommends TypedSQL and users cannot discover a hidden
      flag; link prisma/prisma TML-2970 context via the PR list #29689/#29690/#29691 only
      if natural), what stays (reactNative hidden; GA out of scope), and the validation
      surface used.
- [ ] PR URL returned in your report.

## Standing instruction

Stay focused on the goal; control scope. This is a one-line semantic change plus its test
fallout — resist any temptation to tidy adjacent Rust. Anything structural (I12) halts.

## Constraints (terse)

- Explicit staging; conventional commit per the engines repo's style; no force-push.
- Heartbeats to `/home/aqrln.guest/worktrees/prisma/solid-otter/prisma/wip/heartbeats/implementer.txt`, especially around cargo runs (they can be slow).
- **Time-box:** 45 minutes. **Halt conditions:** the classification file's shape differs
  materially from the spec's line references (re-ground, and halt if the hidden/active
  model itself changed); cargo builds fail for pre-existing reasons (surface, don't fix);
  push rejected.

Same return shape. Begin.
