{
  "name": "netlify",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.6.2",
    "netlify-lambda": "^1.4.13",
    "prisma2": "^2.0.0-preview-1"
  },
  "scripts": {
    "dev": "source .env && export PLATFORM=native && netlify dev",
    "deploy": "rm -f functions-build/index.zip && cp -R ./prisma ./functions/prisma && cd functions/ && zip -r index.zip * && mv index.zip ../functions-build/index.zip && cd .. && netlify deploy --dir=. --prod --functions=functions-build && rm -rf functions/prisma",
    "postinstall": "prisma2 generate"
  }
}
