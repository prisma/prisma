# Migration `watch-20190612143457`

This migration has been generated by timsuchanek at 6/12/2019, 2:34:57 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "db_file"."Blog" ("id" INTEGER NOT NULL, "viewCount2" INTEGER NOT NULL, PRIMARY KEY ("id"));

CREATE TABLE "db_file"."Post" ("id" INTEGER NOT NULL, "anotherString2" TEXT, PRIMARY KEY ("id"));

CREATE TABLE "db_file"."Post2" ("id" INTEGER NOT NULL, "anotherString2" TEXT, PRIMARY KEY ("id"));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20190612143457
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,20 @@
+datasource my_db {
+  provider = "sqlite"
+  url = "file:./db/db_file.db"
+  default = true
+}
+
+model Blog {
+  id Int @id
+  viewCount2 Int
+}
+
+model Post {
+  id Int @id
+  anotherString2 String?
+}
+
+model Post2 {
+  id Int @id
+  anotherString2 String?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20190612143457)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20190612143457'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
