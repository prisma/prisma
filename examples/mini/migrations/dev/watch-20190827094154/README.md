# Migration `watch-20190827094154`

This migration has been generated by timsuchanek at 8/27/2019, 9:41:54 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User"("id" INTEGER NOT NULL DEFAULT 0 ,"name" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"))
;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20190827094154
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,13 @@
+datasource db {
+  url = "file:dev.db"
+  provider = "sqlite"
+}
+
+generator proton {
+  provider = "photonjs"
+}
+
+model User {
+  id Int @id
+  name String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20190827094154)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20190827094154'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
