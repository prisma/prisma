{
  "name": "multi-extension-monorepo",
  "version": "8.0.0-rc.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "description": "Worked example: monorepo with two internal contract-space packages and an aggregating app. Exercises the framework's per-space planner/runner/verifier with multiple extensions in one application.",
  "scripts": {
    "emit": "pnpm build:contract-spaces",
    "build:contract-spaces": "cd packages/audit && prisma contract emit && cd ../feature-flags && prisma contract emit && cd ../../app && prisma contract emit",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "biome check . --error-on-warnings"
  },
  "dependencies": {
    "@prisma/orm-postgres": "workspace:8.0.0-rc.3"
  },
  "devDependencies": {
    "@prisma/cli-engine": "0.2.0",
    "@repo/test-utils": "workspace:8.0.0-rc.3",
    "@repo/tsconfig": "workspace:8.0.0-rc.3",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
