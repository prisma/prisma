# Migration `20190618161822-option-author`

This migration has been generated by Divyendu Singh at 6/18/2019, 4:18:22 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Post"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"published" BOOLEAN NOT NULL DEFAULT false ,"title" TEXT NOT NULL DEFAULT '' ,"content" TEXT   ,"author" TEXT   REFERENCES User(id),PRIMARY KEY ("id"));

INSERT INTO "new_Post" ("id","createdAt","updatedAt","published","title","content","author") SELECT "id","createdAt","updatedAt","published","title","content","author" from "Post"

DROP TABLE "lift"."Post";

ALTER TABLE "lift"."new_Post" RENAME TO "Post";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190617192343-init..20190618161822-option-author
--- datamodel.dml
+++ datamodel.dml
@@ -7,13 +7,8 @@
 generator photon {
   provider = "photonjs"
 }
-generator nexus_prisma {
-  provider = "nexus-prisma"
-  output   = "node_modules/@generated/nexus-prisma"
-}
-
 model User {
   id    String  @default(cuid()) @id @unique
   email String  @unique
   name  String?
@@ -26,6 +21,6 @@
   updatedAt DateTime @updatedAt
   published Boolean
   title     String
   content   String?
-  author    User
+  author    User?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190618161822-option-author)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190618161822-option-author'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
