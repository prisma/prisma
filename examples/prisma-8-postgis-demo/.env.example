# Connection string used by `prisma-next db init`, the seed script, and the e2e tests.
# Matches the docker-compose.yml service definition (port 5435 to avoid clashing with
# the workspace-level postgres at port 5433).
DATABASE_URL=postgres://postgres:postgres@localhost:5435/postgis_demo
