### This file was autogenerated by GraphQL Nexus
### Do not make changes to this file directly


type Mutation {
  createDraft(authorEmail: String, content: String, title: String): Post!
  deletePost(id: ID): Post
  publish(id: ID): Post
  signupUser(email: String, name: String): User!
}

type Post {
  author: User!
  content: String
  id: ID!
  published: Boolean!
  title: String!
}

type Query {
  feed: [Post!]!
  filterPosts(searchString: String): [Post!]!
  hello: String!
  post(id: ID): Post
}

type User {
  email: String!
  id: ID!
  name: String
  posts(after: String, before: String, first: Int, last: Int, skip: String): [Post!]
}
