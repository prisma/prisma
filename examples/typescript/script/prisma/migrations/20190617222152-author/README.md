# Migration `20190617222152-author`

This migration has been generated by Divyendu Singh at 6/17/2019, 10:21:52 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Post" ("id" TEXT NOT NULL, "createdAt" DATE NOT NULL, "updatedAt" DATE DEFAULT '1970-01-01 00:00:00' NOT NULL, "published" BOOLEAN DEFAULT false NOT NULL, "title" TEXT DEFAULT '' NOT NULL, "content" TEXT, "author" TEXT REFERENCES User(id), PRIMARY KEY ("id"));

INSERT INTO "new_Post" ("id","createdAt","updatedAt","published","title","content","author") SELECT "id","createdAt","updatedAt","published","title","content","author" from "Post"

DROP TABLE "lift"."Post";

ALTER TABLE "lift"."new_Post" RENAME TO "Post";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190617102350-init..20190617222152-author
--- datamodel.dml
+++ datamodel.dml
@@ -22,6 +22,6 @@
   updatedAt DateTime @updatedAt
   published Boolean
   title     String
   content   String?
-  author    User
+  author    User?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190617222152-author)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190617222152-author'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
