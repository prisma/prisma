{
  "name": "mongo-blog-leaderboard",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "emit": "prisma orm contract emit",
    "migration:plan": "prisma orm migration plan",
    "migration:apply": "prisma orm migrate",
    "db:seed": "tsx scripts/seed.ts",
    "test": "vitest run",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "biome check . --error-on-warnings"
  },
  "dependencies": {
    "@prisma/orm-mongo": "workspace:8.0.0-rc.3",
    "mongodb": "catalog:"
  },
  "devDependencies": {
    "@prisma/cli-engine": "0.2.0",
    "@repo/tsconfig": "workspace:8.0.0-rc.3",
    "@types/node": "catalog:",
    "mongodb-memory-server": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  },
  "version": "8.0.0-rc.3"
}
