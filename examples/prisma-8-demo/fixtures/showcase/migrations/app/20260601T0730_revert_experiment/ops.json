[
  {
    "id": "dropColumn.widget.count",
    "label": "Drop column \"count\" from \"widget\"",
    "operationClass": "destructive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "__unbound__",
        "objectType": "column",
        "name": "count",
        "table": "widget"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"count\" exists",
        "sql": "SELECT EXISTS (\n  SELECT 1\n  FROM information_schema.columns\n  WHERE table_schema = current_schema()\n    AND table_name = 'widget'\n    AND column_name = 'count'\n)"
      }
    ],
    "execute": [
      {
        "description": "drop column \"count\"",
        "sql": "ALTER TABLE \"widget\" DROP COLUMN \"count\""
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"count\" does not exist",
        "sql": "SELECT NOT EXISTS (\n  SELECT 1\n  FROM information_schema.columns\n  WHERE table_schema = current_schema()\n    AND table_name = 'widget'\n    AND column_name = 'count'\n)"
      }
    ]
  }
]
