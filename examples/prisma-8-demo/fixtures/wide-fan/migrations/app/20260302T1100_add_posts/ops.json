[
  {
    "id": "column.user.posts",
    "label": "Add column \"posts\" to \"user\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "__unbound__",
        "objectType": "column",
        "name": "posts",
        "table": "user"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"posts\" is missing",
        "sql": "SELECT NOT EXISTS (\n  SELECT 1\n  FROM information_schema.columns\n  WHERE table_schema = current_schema()\n    AND table_name = 'user'\n    AND column_name = 'posts'\n)"
      }
    ],
    "execute": [
      {
        "description": "add column \"posts\"",
        "sql": "ALTER TABLE \"user\" ADD COLUMN \"posts\" text"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"posts\" exists",
        "sql": "SELECT EXISTS (\n  SELECT 1\n  FROM information_schema.columns\n  WHERE table_schema = current_schema()\n    AND table_name = 'user'\n    AND column_name = 'posts'\n)"
      }
    ]
  }
]
