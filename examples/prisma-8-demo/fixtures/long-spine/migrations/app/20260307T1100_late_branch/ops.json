[
  {
    "id": "column.user.category",
    "label": "Add column \"category\" to \"user\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "__unbound__",
        "objectType": "column",
        "name": "category",
        "table": "user"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"category\" is missing",
        "sql": "SELECT NOT EXISTS (\n  SELECT 1\n  FROM information_schema.columns\n  WHERE table_schema = current_schema()\n    AND table_name = 'user'\n    AND column_name = 'category'\n)"
      }
    ],
    "execute": [
      {
        "description": "add column \"category\"",
        "sql": "ALTER TABLE \"user\" ADD COLUMN \"category\" text"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"category\" exists",
        "sql": "SELECT EXISTS (\n  SELECT 1\n  FROM information_schema.columns\n  WHERE table_schema = current_schema()\n    AND table_name = 'user'\n    AND column_name = 'category'\n)"
      }
    ]
  }
]
