{
  "$schema": "https://turborepo.com/schema.json",
  "envMode": "loose",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "generator-build/**", "runtime/**"],
      "env": ["DEV", "MINIFY"]
    }
  }
}
